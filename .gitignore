public/
resources/
.hugo_build.lock

# Terraform
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Terraform plan outputs (binary; regenerate via terraform plan -out=plan.out)
plan.out

# Terraform module lock files generated by standalone `terraform init` for
# per-module validate. Only the root env (infra/envs/prod/.terraform.lock.hcl)
# is committed for reproducibility.
infra/modules/*/.terraform.lock.hcl

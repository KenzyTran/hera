---
phase: 05-workshop-documentation-vi-en
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - content/vi/2-preparation/_index.md
  - content/en/2-preparation/_index.md
  - content/vi/3-hands-on/3.1-knowledge-base/_index.md
  - content/en/3-hands-on/3.1-knowledge-base/_index.md
  - content/vi/3-hands-on/3.2-pipecat-local/_index.md
  - content/en/3-hands-on/3.2-pipecat-local/_index.md
  - static/images/2-preparation/.gitkeep
autonomous: true
requirements: [DOC-02, DOC-03, DOC-04, DOC-10, DOC-11]
must_haves:
  truths:
    - "Phần 2 Preparation single-page covers AWS account checklist + Bedrock model access (Nova 2 Sonic + Titan v2 + AgentCore) + tool installs (AWS CLI v2, Terraform >= 1.9, uv, Docker Desktop) + creds + cost expectation (~$2-5 per 2-hour session ballpark per D-54)"
    - "Phần 2 contains the Bedrock model access pitfall callout (D-51 #3) at the moment learner enables models"
    - "Phần 2 includes a markdown image reference for D-44 #1 (Bedrock Console — Model Access for Nova 2 Sonic + Titan v2 in ap-northeast-1) under /images/2-preparation/"
    - "Phần 3.1 Knowledge Base sub-page mirrors RUNBOOK Phase 1 First sync + Verify sections with terraform apply + aws s3 cp + start-ingestion-job + bin/verify-kb.sh paste-blocks"
    - "Phần 3.1 contains the KB sync delay pitfall callout (D-51 #8)"
    - "Phần 3.2 Pipecat Local sub-page mirrors RUNBOOK Local agent setup + First voice test sections; covers pyproject.toml + Dockerfile + docker-compose.yml + bin/run-agent-local.sh + bin/run-agent-docker.sh + bin/smoke-voice.sh"
    - "Phần 3.2 contains 3 pitfall callouts: 8-min Sonic stream cap (D-51 #1), audio sample rate 16kHz/24kHz (D-51 #2), tool-use schema strictness (D-51 #6)"
    - "Every fenced bash code block in Phần 3.1 + 3.2 is followed by a *Source: <repo-path> — Phase X Plan XX-XX* italic footer (D-42)"
    - "Every snippet uses ap-northeast-1 (D-47) — no us-east-1 in the mainline path"
    - "vi+en for one chapter land in the same atomic commit (D-49)"
    - "All cost numbers use the D-54 ballpark `~$2-5 USD per 2-hour session` framing with a post-launch-update notice (per checker WARNING 2); no literal `~$X-Y` placeholder strings ship to learners"
    - "After this plan: vi=8, en=8 (6 from Plan 05-01 + 1 new sub-page Phần 3.1 + 1 new sub-page Phần 3.2 each side; Phần 2 already counted in Plan 05-01's vi=6/en=6 baseline)"
    - "static/images/2-preparation/.gitkeep exists so the markdown image reference resolves the directory; PNG itself captured by operator post-content"
    - "No emojis anywhere (CLAUDE.md mandate)"
  artifacts:
    - path: "content/vi/2-preparation/_index.md"
      provides: "Phần 2 Preparation in Vietnamese — single page checklist with model access pitfall + D-44 #1 image reference"
      min_lines: 100
      contains: "ap-northeast-1"
    - path: "content/en/2-preparation/_index.md"
      provides: "Phần 2 Preparation in English — same structure as vi"
      min_lines: 100
      contains: "ap-northeast-1"
    - path: "content/vi/3-hands-on/3.1-knowledge-base/_index.md"
      provides: "Phần 3.1 KB build — vi sub-page with terraform + ingestion + verify paste-blocks"
      min_lines: 80
      contains: "bin/verify-kb.sh"
    - path: "content/en/3-hands-on/3.1-knowledge-base/_index.md"
      provides: "Phần 3.1 KB build — en sub-page"
      min_lines: 80
      contains: "bin/verify-kb.sh"
    - path: "content/vi/3-hands-on/3.2-pipecat-local/_index.md"
      provides: "Phần 3.2 Pipecat Local — vi sub-page covering uv project + Dockerfile + compose + smoke"
      min_lines: 100
      contains: "AWSNovaSonicLLMService"
    - path: "content/en/3-hands-on/3.2-pipecat-local/_index.md"
      provides: "Phần 3.2 Pipecat Local — en sub-page"
      min_lines: 100
      contains: "AWSNovaSonicLLMService"
    - path: "static/images/2-preparation/.gitkeep"
      provides: "screenshot directory for D-44 #1 hero (Bedrock Console — Model Access)"
  key_links:
    - from: "content/vi/3-hands-on/3.1-knowledge-base/_index.md"
      to: "content/en/3-hands-on/3.1-knowledge-base/_index.md"
      via: "matching slug 3.1-knowledge-base under content/{vi,en}/3-hands-on/ (D-49 + D-50)"
      pattern: "3-hands-on/3.1-knowledge-base"
    - from: "content/vi/3-hands-on/3.2-pipecat-local/_index.md"
      to: "content/en/3-hands-on/3.2-pipecat-local/_index.md"
      via: "matching slug 3.2-pipecat-local under content/{vi,en}/3-hands-on/"
      pattern: "3-hands-on/3.2-pipecat-local"
    - from: "Phần 3.1 fenced code blocks"
      to: "RUNBOOK.md First sync + Verify sections (lines 66-142) + bin/verify-kb.sh"
      via: "*Source: <path> — Phase 1 Plan 01-XX* footer per D-42"
      pattern: "\\*Source: .+— Phase 1"
    - from: "Phần 3.2 fenced code blocks"
      to: "agent/hera_agent/* + bin/run-agent-local.sh + bin/run-agent-docker.sh + bin/smoke-voice.sh"
      via: "*Source: <path> — Phase 2 Plan 02-XX* footer"
      pattern: "\\*Source: .+— Phase 2"
    - from: "content/{vi,en}/2-preparation/_index.md"
      to: "static/images/2-preparation/console-bedrock-model-access.png"
      via: "markdown image reference (operator captures PNG post-content per 04-HUMAN-UAT)"
      pattern: "/images/2-preparation/"
---

<objective>
Author 3 chapters in vi+en covering the "AWS account setup + first hands-on" arc that gets a learner from a fresh AWS account to a locally-running voice loop with KB-backed tool calls.

- **Phần 2 Preparation (single page):** AWS account, Bedrock model access (Nova 2 Sonic + Titan v2 + AgentCore), tool installs (AWS CLI v2, Terraform >= 1.9, uv, Docker Desktop with buildx), credentials configuration, cost expectation (~$2-5 USD per 2-hour session ballpark per D-54). Contains the **Bedrock model access pitfall callout** (D-51 #3) inline at the model-access enable step. Includes the **D-44 #1 image markdown reference** (Bedrock Console — Model Access for Nova 2 Sonic + Titan v2 in ap-northeast-1) at the model-access section; PNG captured by operator post-content per 04-HUMAN-UAT.

- **Phần 3.1 Knowledge Base sub-page:** mirrors RUNBOOK Phase 1 sections (First deploy + First sync + Verify + Re-index after editing) with `terraform apply`, `aws s3 cp catalog/`, `aws bedrock-agent start-ingestion-job`, `bin/verify-kb.sh` paste-blocks. Contains the **KB sync delay pitfall callout** (D-51 #8) at the post-sync verify step.

- **Phần 3.2 Pipecat Local sub-page:** mirrors RUNBOOK Local agent setup + First voice test sections. Walks `agent/pyproject.toml` (Python 3.12 + Pipecat 1.1.0[aws-nova-sonic,silero,websocket]), system prompt + tool definition + AWSNovaSonicLLMService construction, multi-arch Dockerfile, `docker-compose.yml`, `bin/run-agent-local.sh` for uv path, `bin/run-agent-docker.sh` for compose path, `bin/smoke-voice.sh` for AGT-04 latency gate. Contains 3 pitfall callouts: **8-min Sonic stream cap** (D-51 #1) at SessionContinuationParams, **audio sample rate 16kHz in / 24kHz out** (D-51 #2) at AudioConfig defaults explanation, **tool-use schema strictness** (D-51 #6) at register_function example.

All snippets verbatim from frozen Phase 1-2 source with D-42 source-footer reference. Region default `ap-northeast-1` everywhere (D-47). vi+en authored same plan + same commit (D-49) so the parity gate from Plan 05-01 never fails.

This plan is autonomous because content authoring requires no operator gates and the source code (frozen Phase 1-2) is the only input beyond the plan instructions. Demo budget honored — zero new AWS deploys; all evidence reused from Phase 1-2 live state.

**Cost-number policy (per checker WARNING 2):** instead of literal `~$X-Y` placeholders, every cost mention uses the D-54 ballpark `~$2-5 USD per 2-hour session` plus a `{{% notice info %}}` callout pointing learners to the post-launch-update note (instructor pulls 24h Cost Explorer data after one workshop session — 04-HUMAN-UAT item #4). Per-service breakdown rows are NOT shipped in this plan; Phần 5 Summary (Plan 05-04) replaces the per-service breakdown table with a placeholder paragraph (per checker WARNING 2 option (a)).

Purpose: a fresh learner can read Phần 2 + Phần 3.1 + Phần 3.2 and reach a working voice loop on their laptop talking to the Bedrock KB they just deployed.

Output:
- 6 modified `_index.md` files (Phần 2 single-page vi+en; Phần 3.1 sub-page vi+en; Phần 3.2 sub-page vi+en).
- 4 directories created if missing: `content/{vi,en}/3-hands-on/{3.1-knowledge-base,3.2-pipecat-local}/`.
- 1 new `.gitkeep` placeholder for D-44 #1 image directory: `static/images/2-preparation/.gitkeep`.
- **File count after this plan: vi=8, en=8** (6 from Plan 05-01 + 1 new sub-page Phần 3.1 + 1 new sub-page Phần 3.2 each side; Phần 2 already counted in Plan 05-01's vi=6/en=6 baseline).

Count math (re-derived per checker WARNING 3 against ground-truth filesystem):
- Plan 05-01 ended with vi=6, en=6 (root + 5 chapter `_index.md`; both 1.1-prerequisites.md deleted; 4 stub dirs deleted both sides per Option A + stub deletion).
- This plan REPLACES the body of Phần 2's existing `_index.md` (no new `_index.md` count change) and ADDS 2 new sub-page `_index.md` files under Phần 3 (`3.1-knowledge-base/_index.md` + `3.2-pipecat-local/_index.md`). So **vi=8, en=8 after this plan**.
- Plan 05-03 will add 3.3 + 3.4 + 3.5 sub-pages (vi=11, en=11).
- Plan 05-04 will modify Phần 4 + Phần 5 bodies (count unchanged at vi=11, en=11).
</objective>

<execution_context>
@C:/Users/trant/projects/hera/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/hera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md
@.planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md
@.planning/phases/05-workshop-documentation-vi-en/05-01-PLAN.md
@CLAUDE.md
@AGENTS.md

# Source files this plan reads for snippets (with D-42 footers)
@RUNBOOK.md
@bin/verify-kb.sh
@bin/run-agent-local.sh
@bin/run-agent-docker.sh
@bin/smoke-voice.sh
@agent/pyproject.toml
@agent/hera_agent/main.py
@agent/hera_agent/pipeline.py
@agent/hera_agent/prompts.py
@agent/hera_agent/tools.py
@agent/hera_agent/config.py
@agent/Dockerfile
@docker-compose.yml
@catalog/iphone-13-pro-max.md

# Closest pattern analogs
@content/vi/4-cleanup/_index.md
@content/vi/1-introduction/_index.md
@content/en/1-introduction/_index.md

<interfaces>
<!-- Snippets the executor will reproduce verbatim. Each must be followed by an italic *Source:* footer. -->

**Phần 2 — model access enable check (RUNBOOK lines 26-32):**
```bash
aws bedrock list-foundation-models --region ap-northeast-1 \
  --query 'modelSummaries[?modelId==`amazon.titan-embed-text-v2:0`].modelLifecycle.status' \
  --output text
# Expect: ACTIVE
```
*Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-03*

**Phần 2 — sts get-caller-identity (RUNBOOK lines 12-14):**
```bash
aws --version
aws sts get-caller-identity
terraform -version
jq --version
```
*Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-01*

**Phần 2 — D-44 #1 image reference (markdown):**
```markdown
![Bedrock Console — Model Access for Nova 2 Sonic + Titan v2 (ap-northeast-1)](/images/2-preparation/console-bedrock-model-access.png)
```

**Phần 3.1 — terraform apply (RUNBOOK lines 44-50):**
```bash
cd infra/envs/prod
terraform init
terraform apply
cd ../../..
```
*Source: RUNBOOK.md (First deploy) — Phase 1 Plan 01-02*

**Phần 3.1 — first sync (RUNBOOK lines 73-106) — full block reproduced verbatim:**
[KB_ID + DS_ID + SRC_BUCKET + REGION assignment, aws s3 cp, start-ingestion-job, polling loop]
*Source: RUNBOOK.md (First sync) — Phase 1 Plan 01-03*

**Phần 3.1 — verify (RUNBOOK lines 119-127):**
```bash
bin/verify-kb.sh
```
Expected: `OK: 3 results returned, top score 0.78 >= threshold 0.4`
*Source: bin/verify-kb.sh — Phase 1 Plan 01-03*

**Phần 3.2 — uv project setup (RUNBOOK lines 240-254 + agent/pyproject.toml):**
```bash
cd agent
uv sync --frozen
uv run python -m hera_agent.main
# starts uvicorn on http://localhost:8080
# /ping -> {"status":"Healthy", ...}
# /ws   -> Pipecat WebSocket endpoint
```
*Source: bin/run-agent-local.sh — Phase 2 Plan 02-01*

**Phần 3.2 — system prompt (agent/hera_agent/prompts.py SYSTEM_PROMPT):**
[reproduce 3-5 lines of the actual prompt verbatim — executor reads agent/hera_agent/prompts.py and quotes the relevant section]
*Source: agent/hera_agent/prompts.py — Phase 2 Plan 02-01*

**Phần 3.2 — tool registration (agent/hera_agent/pipeline.py register_function call):**
[reproduce the register_function("lookup_product", ...) line + the FunctionSchema-shaped dict]
*Source: agent/hera_agent/pipeline.py — Phase 2 Plan 02-01*

**Phần 3.2 — AWSNovaSonicLLMService construction:**
[reproduce the AWSNovaSonicLLMService(access_key_id=..., secret_access_key=..., region=...) call from agent/hera_agent/pipeline.py]
*Source: agent/hera_agent/pipeline.py — Phase 2 Plan 02-01*

**Phần 3.2 — docker compose path (RUNBOOK lines 267-294):**
```bash
docker compose up
# agent     -> http://localhost:8080
# frontend  -> http://localhost:8000
```
*Source: docker-compose.yml — Phase 2 Plan 02-02*

**Phần 3.2 — smoke voice gate:**
```bash
bash bin/smoke-voice.sh
# OK: AGT-04 latency gate passed (LATENCY_MS<3000)
```
*Source: bin/smoke-voice.sh — Phase 2 Plan 02-02*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Create static/images/2-preparation/.gitkeep placeholder for D-44 #1 hero screenshot</name>
  <files>static/images/2-preparation/.gitkeep</files>
  <read_first>
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-44 #1 Bedrock model access screenshot, D-45 storage path)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (static/images/<chapter>/ pattern; precedent `static/images/.gitkeep`)
  </read_first>
  <action>
Create `static/images/2-preparation/` directory and an empty `.gitkeep` so Task 1 can reference `/images/2-preparation/console-bedrock-model-access.png` in markdown without a broken-image render. (Per checker BLOCKER 3 remedy: 05-02 owns this `.gitkeep` to keep 05-01 minimal.)

```bash
mkdir -p static/images/2-preparation
touch static/images/2-preparation/.gitkeep
```

The `.gitkeep` is empty. Operator captures `console-bedrock-model-access.png` from Bedrock Console (Asia Pacific (Tokyo) → Model access page showing Nova 2 Sonic + Titan v2 granted) post-content; this is part of the D-44 screenshot sweep tracked alongside Plan 05-03's image inventory and 04-HUMAN-UAT follow-ups.
  </action>
  <verify>
    <automated>test -d static/images/2-preparation && test -f static/images/2-preparation/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - Directory `static/images/2-preparation/` exists.
    - File `static/images/2-preparation/.gitkeep` exists and is empty: `[ ! -s static/images/2-preparation/.gitkeep ]` succeeds.
    - `git status` shows the new file untracked / staged; no other files touched.
  </acceptance_criteria>
  <done>2-preparation screenshot directory placeholder ready for Task 1 image markdown reference (D-44 #1).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Author Phần 2 Preparation single-page in vi+en (DOC-02 + D-51 #3 + D-44 #1 image)</name>
  <files>
    content/vi/2-preparation/_index.md,
    content/en/2-preparation/_index.md
  </files>
  <read_first>
    - content/vi/2-preparation/_index.md (current template stub — replace body)
    - content/en/2-preparation/_index.md (current template stub — replace body)
    - RUNBOOK.md (Pre-flight section, lines 5-37 — pre-flight tool checks + model access enable steps)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-41 single-page, D-44 #1 Bedrock Console model access, D-51 #3 model access pitfall, D-47 region default, D-54 cost ranges)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (chapter-index single-page section + notice + Source-footer + image reference pattern)
    - content/vi/4-cleanup/_index.md (single-page chapter pattern with notice shortcode at end)
  </read_first>
  <action>
Replace the body of `content/{vi,en}/2-preparation/_index.md`. Front-matter unchanged: `weight: 2`, `chapter: true`, `pre: "<b>2. </b>"`, `title: "Chuẩn bị"` (vi) / `title: "Preparation"` (en).

Body has these H2 sections in order:

1. **`## Mục tiêu`** (vi) / **`## Goals`** (en) — 1-2 câu: kết thúc chương này bạn có AWS account ready, model access đã enable, công cụ local đã cài, credentials configured, biết kỳ vọng chi phí.

2. **`## AWS Account checklist`** — bullet list 5 mục:
   - Bạn có AWS Account của mình (không dùng tài khoản chia sẻ; tránh root user — dùng IAM user hoặc IAM Identity Center).
   - Quyền IAM cho phép create/destroy: Bedrock, AgentCore, ECR, S3, CloudFront, CloudWatch, IAM (cho deploy), Lambda. Đơn giản nhất: AdministratorAccess cho IAM user của bạn (workshop scope, không production).
   - Region đích: `ap-northeast-1` (D-47). Nếu bạn ở khu vực khác (us-east-1, us-west-2, eu-north-1) cũng OK; thay biến `region` trong `infra/envs/prod/terraform.tfvars`.
   - Billing Alerts đã được tick: AWS Console > Billing > Billing Preferences > "Receive Billing Alerts" (one-time per account; mất 15 phút sau khi tick để alarm bắt đầu nhận data).
   - Khả năng chạy `aws sts get-caller-identity` trên local thành công (xem section tiếp theo).

3. **`## Bật Bedrock model access (per-region, per-model)`** —

   AWS gates Bedrock models per-account-per-region. Workshop dùng 3 model trong `ap-northeast-1`:
   - `amazon.nova-sonic-v1:0` (Nova 2 Sonic — bidirectional voice).
   - `amazon.titan-embed-text-v2:0` (Titan Text Embeddings v2 — KB embeddings).
   - AgentCore không phải foundation model nhưng cần được enable ở Service Quotas / được granted access tự động khi region hỗ trợ.

   Steps (3 bullet points):
   - Mở `https://console.aws.amazon.com/bedrock/` → đảm bảo region selector là `Asia Pacific (Tokyo) ap-northeast-1`.
   - Click "Model access" → "Modify model access" → tick `Amazon Nova 2 Sonic` + `Amazon Titan Text Embeddings V2` → Submit. Mất ~1 phút để chuyển sang Granted.
   - Verify bằng CLI:

   ```bash
   aws bedrock list-foundation-models --region ap-northeast-1 \
     --query 'modelSummaries[?modelId==`amazon.titan-embed-text-v2:0`].modelLifecycle.status' \
     --output text
   # Expect: ACTIVE
   ```
   *Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-03*

   Lặp lại cho `amazon.nova-sonic-v1:0`.

   **D-44 #1 hero screenshot reference (PNG captured by operator post-content):**

   ```markdown
   ![Bedrock Console — Model Access for Nova 2 Sonic + Titan v2 (ap-northeast-1)](/images/2-preparation/console-bedrock-model-access.png)
   ```

   **Pitfall callout (D-51 #3):**

   ```markdown
   {{% notice warning %}}
   **Bật Bedrock model access là per-region, per-model:** chưa enable thì
   `terraform apply` vẫn pass nhưng `aws bedrock-agent start-ingestion-job`
   (Phần 3.1) sẽ fail với `AccessDeniedException` ở model ARN. Nếu bạn deploy
   sang region khác (us-east-1, us-west-2, eu-north-1), phải enable lại từng
   model ở region mới — IAM ở region cũ không carry over.

   *Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-03*
   {{% /notice %}}
   ```

4. **`## Cài AWS CLI v2`** — 2-3 dòng prose + paste-block:

   ```bash
   aws --version
   # Expect: aws-cli/2.x.x
   ```
   *Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-01*

   Nếu chưa có: link tới docs `https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html`.

5. **`## Cài Terraform >= 1.9`** — paste-block:

   ```bash
   terraform -version
   # Expect: Terraform v1.9.x or newer
   ```
   *Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-01*

   Install hint: `https://developer.hashicorp.com/terraform/install`.

6. **`## Cài uv (Python package manager)`** — 1-2 dòng + block:

   ```bash
   # Install uv (one-line installer):
   curl -LsSf https://astral.sh/uv/install.sh | sh
   uv --version
   ```
   *Source: AGENTS.md / Astral docs*

   Note: workshop dùng uv exclusively (không dùng `pip`, không dùng `python3 X` — uv quản lý Python 3.12 + lockfile cho `agent/`).

7. **`## Cài Docker Desktop với buildx`** — 1-2 dòng + block:

   ```bash
   docker --version
   docker buildx version
   # Expect: docker buildx 0.x.x or newer
   ```
   *Source: agent/Dockerfile — Phase 2 Plan 02-02*

   Note: multi-arch image build (linux/arm64 + linux/amd64) yêu cầu buildx; AgentCore Runtime ARM64-only nên cần arm64 manifest để deploy.

8. **`## Cài jq`** — 1-line + block:

   ```bash
   jq --version
   # Install: winget install jqlang.jq (Windows), brew install jq (macOS), apt-get install jq (Debian/Ubuntu)
   ```
   *Source: bin/verify-kb.sh — Phase 1 Plan 01-03*

9. **`## Cấu hình AWS credentials`** — 2-3 dòng:

   ```bash
   aws configure
   # Or for SSO/IAM Identity Center:
   aws configure sso

   # Verify:
   aws sts get-caller-identity
   # Expect: prints account ID + IAM ARN you will deploy into
   ```
   *Source: RUNBOOK.md (Pre-flight) — Phase 1 Plan 01-01*

10. **`## Kỳ vọng chi phí`** — 2-3 bullet points anchored to D-54 ballpark (per checker WARNING 2 — drop literal `~$X-Y` placeholder rows):

    - Toàn workshop session ~2 giờ (build + nói chuyện + cleanup): **~$2-5 USD ballpark** nếu bạn theo Phần 4 Cleanup. Phần 5 Summary có pointer chi tiết.
    - Hai cost driver chính: Bedrock Nova 2 Sonic streaming (charged per active conversation minute) + AgentCore Runtime (charged per active session-second).
    - **Quan trọng:** chạy Phần 4 Cleanup (cdk destroy → terraform destroy → bin/cleanup-verify.sh) ngay sau khi xong session để chi phí dừng lại. Cost Explorer cập nhật chậm 24 giờ — kiểm tra ngày hôm sau theo paste-line trong Phần 4.

    **Post-launch-update notice (per checker WARNING 2):**

    ```markdown
    {{% notice info %}}
    **Số liệu cost được cập nhật post-launch:** ballpark `~$2-5 USD per 2-hour session`
    là khung định hướng (D-54). Số liệu chính xác per-service sẽ được instructor cập
    nhật vào Phần 5 Summary sau khi pull 24h Cost Explorer data từ một workshop
    session thực tế (04-HUMAN-UAT item #4). AWS pricing thay đổi định kỳ —
    bookmark `https://aws.amazon.com/bedrock/pricing/` cho live numbers.
    {{% /notice %}}
    ```

11. **`## Sẵn sàng vào Phần 3 Hands-on`** — 1 đoạn ngắn closing: bạn đã có account + model access + tools + creds + cost expectation. Phần 3.1 deploy KB; Phần 3.2 chạy agent local; Phần 3.3-3.5 deploy lên AgentCore + widget + observability.

**English file:** mirrors structure 1:1 with translated headings (`## Goals`, `## AWS Account checklist`, `## Enable Bedrock model access (per-region, per-model)`, `## Install AWS CLI v2`, `## Install Terraform >= 1.9`, `## Install uv (Python package manager)`, `## Install Docker Desktop with buildx`, `## Install jq`, `## Configure AWS credentials`, `## Cost expectations`, `## Ready for Phần 3 Hands-on`). All paste-blocks identical (commands are language-agnostic). Notice-warning + notice-info bodies translated. Source-footer paths identical. Em-dash `—` verbatim. Image markdown reference identical (alt-text translated to English: `![Bedrock Console — Model Access for Nova 2 Sonic + Titan v2 (ap-northeast-1)](/images/2-preparation/console-bedrock-model-access.png)` — alt-text is already English-friendly so no translation needed).

**Constraints:**
- No emojis (Pattern S6).
- All paste-blocks have D-42 source-footer immediately below the closing ``` fence (Pattern S3).
- D-51 #3 callout uses `{{% notice warning %}}` (Pattern S4).
- Cost post-launch-update callout uses `{{% notice info %}}` (informational, not warning).
- Region default `ap-northeast-1` everywhere (Pattern S5).
- No `{{% children %}}` (single-page per D-41).
- No literal `~$X-Y` / `~$Z` / `~$W` placeholder strings — only the D-54 anchored `~$2-5 USD` ballpark with the post-launch-update notice (per checker WARNING 2).
- 1 image markdown reference under `/images/2-preparation/` (D-44 #1).
- vi+en in same commit (D-49).

Per D-41 / D-44 #1 / D-47 / D-49 / D-51 #3 / D-54 / Pattern S3 + S4 + S5 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/2-preparation/_index.md && test -f content/en/2-preparation/_index.md && grep -c '{{% notice warning %}}' content/vi/2-preparation/_index.md | awk '$1>=1{exit 0}{exit 1}' && grep -c '{{% notice warning %}}' content/en/2-preparation/_index.md | awk '$1>=1{exit 0}{exit 1}' && grep -c '{{% notice info %}}' content/vi/2-preparation/_index.md | awk '$1>=1{exit 0}{exit 1}' && grep -c '\*Source:' content/vi/2-preparation/_index.md | awk '$1>=5{exit 0}{exit 1}' && grep -c '\*Source:' content/en/2-preparation/_index.md | awk '$1>=5{exit 0}{exit 1}' && grep -q "ap-northeast-1" content/vi/2-preparation/_index.md && grep -q "ap-northeast-1" content/en/2-preparation/_index.md && grep -q 'amazon.nova-sonic-v1:0' content/vi/2-preparation/_index.md && grep -q 'amazon.titan-embed-text-v2:0' content/en/2-preparation/_index.md && grep -q '/images/2-preparation/console-bedrock-model-access.png' content/vi/2-preparation/_index.md && grep -q '/images/2-preparation/console-bedrock-model-access.png' content/en/2-preparation/_index.md && ! grep -q '~\$X-Y' content/vi/2-preparation/_index.md && ! grep -q '~\$X-Y' content/en/2-preparation/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; both contain `chapter: true` + `weight: 2` + `pre: "<b>2. </b>"`.
    - Both files contain at least 1 `{{% notice warning %}}` ... `{{% /notice %}}` callout (D-51 #3 model access pitfall).
    - Both files contain at least 1 `{{% notice info %}}` callout (cost post-launch-update notice per checker WARNING 2).
    - Both files contain at least 5 occurrences of `*Source:` italic footer (D-42); aim is 1 per fenced block in this chapter.
    - Both files contain `ap-northeast-1` (D-47).
    - Both files contain `amazon.nova-sonic-v1:0` and `amazon.titan-embed-text-v2:0` literal model IDs.
    - Both files contain `aws sts get-caller-identity`, `aws --version`, `terraform -version`, `uv --version`, `docker --version`, `jq --version` literal commands.
    - Both files contain `~$2-5` literal cost range (D-54 ballpark anchor).
    - Both files contain literal `winget install jqlang.jq` (Windows install hint matching bin/verify-kb.sh + bin/cleanup-verify.sh precedent).
    - Both files contain image markdown reference `/images/2-preparation/console-bedrock-model-access.png` (D-44 #1).
    - Both files do NOT contain literal `~$X-Y` or `~$Z` or `~$W` placeholder strings (per checker WARNING 2).
    - `wc -l content/vi/2-preparation/_index.md` >= 100; same for en.
    - Front-matter `title:` field has Vietnamese for vi, English for en (NOT identical strings).
    - No `{{% children` shortcode in either file (single-page per D-41).
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/2-preparation/_index.md content/en/2-preparation/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <done>Phần 2 Preparation vi+en authored as single-page checklist; D-51 #3 callout placed; D-54 anchored ballpark cost with post-launch-update notice; D-47 region default applied; D-44 #1 image markdown reference placed; parity gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Phần 3.1 Knowledge Base sub-page in vi+en (DOC-03 + D-51 #8)</name>
  <files>
    content/vi/3-hands-on/3.1-knowledge-base/_index.md,
    content/en/3-hands-on/3.1-knowledge-base/_index.md
  </files>
  <read_first>
    - RUNBOOK.md (lines 38-142: First deploy + First sync + Verify + Re-index sections — verbatim source for all snippets)
    - bin/verify-kb.sh (full file — script header + flag list)
    - catalog/iphone-13-pro-max.md (catalog file shape — 1-2 lines for schema explanation)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-40 sub-page slug `3.1-knowledge-base`, D-42 footer rule, D-51 #8 KB sync delay placement)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (sub-page front-matter section — Phần 3 sub-pages have weight=1..5 with title `3.1 Knowledge Base`)
    - .planning/phases/01-knowledge-base-foundation/*-SUMMARY.md (live KB id BKXE19AH89, top score 0.86 — used as expected-output examples)
    - infra/modules/knowledge_base/main.tf (read header for "what gets deployed" prose)
  </read_first>
  <action>
Create `content/{vi,en}/3-hands-on/3.1-knowledge-base/_index.md` (new directory + file each side).

Front-matter (sub-page shape — Pattern S2 — NO `chapter: true`, NO `pre`):

```yaml
---
title: "3.1 Knowledge Base"
date: 2025-01-01
weight: 1
---
```

(Same `title` + `weight` for both vi and en. The `title` value is the sidebar label; using English for both keeps the chapter-number prefix consistent.)

**Body H2 sections in order:**

1. **`## Mục tiêu phần này`** (vi) / **`## Goal of this section`** (en) — 1-2 câu: deploy Bedrock Knowledge Base trên S3 Vectors với Titan v2 embeddings, ingest catalog Apple, verify được Retrieve API trả về document đúng cho query "iPhone 13 Pro Max stock".

2. **`## Cấu trúc catalog`** — 2-3 dòng: Apple catalog gồm 4 file markdown trong `catalog/` (Apple Watch S11, iPhone 13 Pro Max, MacBook Pro M4, store-policy). Stock + price được inline trong từng file để chunks Retrieve trả về luôn co-locate stock với SKU name (D-03 Phase 1).

   ```bash
   ls catalog/
   # apple-watch-s11.md  iphone-13-pro-max.md  macbook-pro-m4.md  store-policy.md
   ```
   *Source: catalog/ — Phase 1 Plan 01-01*

3. **`## Bước 1: Deploy infra (Terraform)`** — Reproduce RUNBOOK lines 44-50 verbatim:

   ```bash
   cd infra/envs/prod
   terraform init
   terraform apply
   # Review the plan (about 9 resources). Type 'yes' to apply.
   cd ../../..
   ```
   *Source: RUNBOOK.md (First deploy) — Phase 1 Plan 01-02*

   Theo sau là 2-3 dòng prose: lệnh tạo S3 source bucket + S3 Vectors bucket + index + Bedrock KB + data source + IAM service role. Outputs: `kb_id`, `kb_arn`, `source_bucket_name`, `data_source_id`. Names cố định không có random suffix (D-12).

4. **`## Bước 2: Ingest catalog (manual sync)`** — Reproduce the full RUNBOOK lines 73-106 paste-block verbatim:

   ```bash
   # 1. Resolve outputs from terraform state
   KB_ID=$(terraform -chdir=infra/envs/prod output -raw kb_id)
   DS_ID=$(terraform -chdir=infra/envs/prod output -raw data_source_id)
   SRC_BUCKET=$(terraform -chdir=infra/envs/prod output -raw source_bucket_name)
   REGION=ap-northeast-1

   # 2. Upload all catalog markdown to the catalog/ prefix
   aws s3 cp catalog/ "s3://${SRC_BUCKET}/catalog/" --recursive --exclude "*" --include "*.md"

   # 3. Trigger the ingestion job
   JOB_ID=$(aws bedrock-agent start-ingestion-job \
     --region "${REGION}" \
     --knowledge-base-id "${KB_ID}" \
     --data-source-id "${DS_ID}" \
     --description "first sync" \
     --query 'ingestionJob.ingestionJobId' \
     --output text)
   echo "started ingestion job: ${JOB_ID}"

   # 4. Poll until COMPLETE (or FAILED / STOPPED)
   while true; do
     STATUS=$(aws bedrock-agent get-ingestion-job \
       --region "${REGION}" \
       --knowledge-base-id "${KB_ID}" \
       --data-source-id "${DS_ID}" \
       --ingestion-job-id "${JOB_ID}" \
       --query 'ingestionJob.status' \
       --output text)
     echo "status: ${STATUS}"
     case "${STATUS}" in
       COMPLETE|FAILED|STOPPED) break ;;
     esac
     sleep 5
   done
   ```
   *Source: RUNBOOK.md (First sync) — Phase 1 Plan 01-03*

   Theo sau là 2 dòng prose: API control-plane là `aws bedrock-agent` (build-time), không phải `aws bedrock-agent-runtime` (runtime data-plane). Pitfall J từ RUNBOOK.

5. **`## Bước 3: Verify Retrieve API`** — Reproduce RUNBOOK lines 119-127 verbatim:

   ```bash
   bin/verify-kb.sh
   ```
   *Source: bin/verify-kb.sh — Phase 1 Plan 01-03*

   Expected output (cuối cùng dòng on success):

   ```
   OK: 3 results returned, top score 0.78 >= threshold 0.4
   ```

   Script poll 15s × 20 lần (5 phút tổng) để absorb post-sync propagation.

   **Pitfall callout (D-51 #8 — KB sync delay):**

   ```markdown
   {{% notice warning %}}
   **KB sync delay sau ingestion:** sau khi ingestion job báo `COMPLETE`, vectors
   đã được ghi nhưng có thể mất thêm 2-3 phút để Bedrock KB Retrieve API trả về
   chúng. Đây là behavior AWS đã document, không phải bug. `bin/verify-kb.sh` poll
   tới 5 phút để cover delay này. Nếu sau 5 phút vẫn empty results, kiểm tra:

   - `inclusion_prefixes = ["catalog/"]` trong data source khớp với prefix bạn upload.
   - Ingestion job thực sự `COMPLETE` (không phải `FAILED`): `aws bedrock-agent list-ingestion-jobs --knowledge-base-id "${KB_ID}" --data-source-id "${DS_ID}" --region "${REGION}"`.

   *Source: bin/verify-kb.sh — Phase 1 Plan 01-03*
   {{% /notice %}}
   ```

6. **`## Bước 4: Re-index sau khi sửa catalog`** — Reproduce RUNBOOK lines 150-169 verbatim:

   ```bash
   # Edit the file
   $EDITOR catalog/iphone-13-pro-max.md

   # Re-upload (overwrites the S3 object)
   SRC_BUCKET=$(terraform -chdir=infra/envs/prod output -raw source_bucket_name)
   aws s3 cp catalog/iphone-13-pro-max.md "s3://${SRC_BUCKET}/catalog/iphone-13-pro-max.md"

   # Re-run the ingestion job (same command as First sync)
   KB_ID=$(terraform -chdir=infra/envs/prod output -raw kb_id)
   DS_ID=$(terraform -chdir=infra/envs/prod output -raw data_source_id)
   aws bedrock-agent start-ingestion-job \
     --region ap-northeast-1 \
     --knowledge-base-id "${KB_ID}" \
     --data-source-id "${DS_ID}" \
     --description "re-index after editing iphone-13-pro-max.md"

   # Wait for status COMPLETE, then run verify (covers propagation)
   bin/verify-kb.sh
   ```
   *Source: RUNBOOK.md (Re-index) — Phase 1 Plan 01-03*

   1 dòng prose: Bedrock KB ingestion là incremental — chỉ document đã sửa được re-embed; chi phí ~1 cent.

7. **`## Tiếp theo`** — 1 đoạn pointer: KB đã sẵn sàng, Phần 3.2 sẽ chạy Pipecat agent local để tool `lookup_product()` gọi `aws bedrock-agent-runtime retrieve` ở KB này.

**English file:** mirrors structure 1:1 with translated headings (`## Goal of this section`, `## Catalog structure`, `## Step 1: Deploy infra (Terraform)`, `## Step 2: Ingest catalog (manual sync)`, `## Step 3: Verify Retrieve API`, `## Step 4: Re-index after editing catalog`, `## Next`). All paste-blocks identical. Notice body translated. Source footers identical.

**Constraints:**
- All fenced code blocks followed immediately by italic `*Source:` footer line (Pattern S3).
- Sub-page front-matter — NO `chapter: true`, NO `pre` (Pattern from PATTERNS.md sub-page section).
- `weight: 1` for both vi and en (3.1 ordinal).
- D-51 #8 callout uses `notice warning`.
- Em-dash `—` verbatim throughout.
- vi+en same commit (D-49).

Per D-40 / D-42 / D-43 / D-49 / D-51 #8 / Pattern S3 + S4 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/3-hands-on/3.1-knowledge-base/_index.md && test -f content/en/3-hands-on/3.1-knowledge-base/_index.md && ! grep -q "chapter: true" content/vi/3-hands-on/3.1-knowledge-base/_index.md && grep -q 'weight: 1' content/vi/3-hands-on/3.1-knowledge-base/_index.md && grep -c '\*Source:' content/vi/3-hands-on/3.1-knowledge-base/_index.md | awk '$1>=5{exit 0}{exit 1}' && grep -c '\*Source:' content/en/3-hands-on/3.1-knowledge-base/_index.md | awk '$1>=5{exit 0}{exit 1}' && grep -q "bin/verify-kb.sh" content/vi/3-hands-on/3.1-knowledge-base/_index.md && grep -q "bin/verify-kb.sh" content/en/3-hands-on/3.1-knowledge-base/_index.md && grep -q "ap-northeast-1" content/vi/3-hands-on/3.1-knowledge-base/_index.md && grep -q "{{% notice warning %}}" content/vi/3-hands-on/3.1-knowledge-base/_index.md && grep -q "{{% notice warning %}}" content/en/3-hands-on/3.1-knowledge-base/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the new sub-page paths.
    - Front-matter for both: `title: "3.1 Knowledge Base"`, `weight: 1`, NO `chapter: true`, NO `pre`.
    - Both files contain at least 5 occurrences of `*Source:` italic footer (D-42).
    - Both files contain literal commands: `bin/verify-kb.sh`, `aws s3 cp`, `aws bedrock-agent start-ingestion-job`, `terraform -chdir=infra/envs/prod`, `inclusion_prefixes`.
    - Both files contain `ap-northeast-1` (D-47).
    - Both files contain at least 1 `{{% notice warning %}}` ... `{{% /notice %}}` callout (D-51 #8).
    - The notice body in both files mentions "2-3 phút" (vi) / "2-3 minutes" (en) and `bin/verify-kb.sh`.
    - Both files contain at least 4 fenced code blocks (one per `Bước 1..4` section). `grep -c '^```bash$' file` >= 4.
    - `wc -l content/vi/3-hands-on/3.1-knowledge-base/_index.md` >= 80; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/3-hands-on/3.1-knowledge-base/_index.md content/en/3-hands-on/3.1-knowledge-base/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0 (vi=7, en=7 after this task — Phần 2 unchanged + 1 new sub-page each side).
  </acceptance_criteria>
  <done>Phần 3.1 KB sub-page vi+en authored mirroring RUNBOOK Phase 1 sections; D-51 #8 callout placed; all snippets carry D-42 footer; parity gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Phần 3.2 Pipecat Local sub-page in vi+en (DOC-04 + D-51 #1, #2, #6)</name>
  <files>
    content/vi/3-hands-on/3.2-pipecat-local/_index.md,
    content/en/3-hands-on/3.2-pipecat-local/_index.md
  </files>
  <read_first>
    - RUNBOOK.md (lines 219-336: Local agent setup + First voice test + Cleanup local Docker resources sections)
    - agent/pyproject.toml (full file — uv project shape: requires-python, deps with aws-nova-sonic extra)
    - agent/hera_agent/main.py (FastAPI app with /ping + /invocations + /ws — already in execution context)
    - agent/hera_agent/pipeline.py (read header + AWSNovaSonicLLMService construction + register_function call + SessionContinuationParams)
    - agent/hera_agent/prompts.py (read SYSTEM_PROMPT — quote 5-8 lines verbatim)
    - agent/hera_agent/tools.py (read lookup_product_handler signature + return shape + threshold sentinel)
    - agent/hera_agent/config.py (read os.environ defaults + region default)
    - agent/Dockerfile (read multi-arch base + uv sync line)
    - docker-compose.yml (read services + env-var REQUIRED-syntax pattern)
    - bin/run-agent-local.sh (uv path)
    - bin/run-agent-docker.sh (compose path)
    - bin/smoke-voice.sh (header + AGT-04 latency gate)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-40 slug `3.2-pipecat-local`, D-51 #1 + #2 + #6 placements, D-47 region)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (sub-page section + Source-footer)
  </read_first>
  <action>
Create `content/{vi,en}/3-hands-on/3.2-pipecat-local/_index.md`.

Front-matter (sub-page; weight=2 for 3.2 ordinal):

```yaml
---
title: "3.2 Pipecat Local"
date: 2025-01-01
weight: 2
---
```

**Body H2 sections in order (vi-side; en mirrors 1:1):**

1. **`## Mục tiêu phần này`** — 1-2 câu: chạy Pipecat agent trên laptop, browser local kết nối WebSocket, hỏi "Do you have MacBook Pro?" → agent gọi `lookup_product` ở KB Phần 3.1 → Sonic phát trả lời. Latency p95 < 3s (AGT-04).

2. **`## Cấu trúc agent project`** — 1 đoạn ngắn + tree:

   ```
   agent/
   ├── pyproject.toml          # Python 3.12 + Pipecat 1.1.0[aws-nova-sonic,silero,websocket]
   ├── uv.lock                 # frozen dep set; deploy reuses verbatim
   ├── Dockerfile              # multi-arch (linux/arm64,linux/amd64)
   └── hera_agent/
       ├── main.py             # FastAPI: /ping + /invocations + /ws
       ├── pipeline.py         # Pipecat pipeline + AWSNovaSonicLLMService
       ├── prompts.py          # Apple Store assistant SYSTEM_PROMPT (English)
       ├── tools.py            # lookup_product handler (KB Retrieve)
       ├── serializer.py       # RawPCMSerializer (16k Int16 in / 24k out)
       └── config.py           # os.environ.get with defaults
   ```
   *Source: agent/ — Phase 2 Plans 02-01 + 02-02*

3. **`## pyproject.toml: pin Python 3.12 + Pipecat extras`** — Quote relevant block from `agent/pyproject.toml` verbatim (executor reads file and pulls the `requires-python` + `dependencies` block):

   ```toml
   [project]
   requires-python = ">=3.12"
   dependencies = [
     "pipecat-ai[aws-nova-sonic,silero,websocket]==1.1.0",
     # ... full list from agent/pyproject.toml ...
   ]
   ```
   *Source: agent/pyproject.toml — Phase 2 Plan 02-01*

   1 dòng prose: Python 3.12 bắt buộc — Pipecat `aws-nova-sonic` extra có marker `python_version>='3.12'`; trên 3.11 extra silently no-op và `aws_sdk_bedrock_runtime` thiếu trong lockfile.

4. **`## main.py: FastAPI shape`** — Quote `agent/hera_agent/main.py` verbatim (full file, ~67 lines):

   ```python
   """FastAPI entrypoint for the Hera Pipecat agent.

   Exposes three routes on port 8080:
   - GET  /ping        : AgentCore Runtime health check (returns {"status":"Healthy"}).
   - POST /invocations : AgentCore HTTP data-plane stub. Voice loop runs on /ws.
   - WebSocket /ws     : Pipecat voice pipeline. One pipeline per connection.
   """
   # ... full file body ...
   ```
   *Source: agent/hera_agent/main.py — Phase 2 Plan 02-01 (POST /invocations added Phase 4 Plan 04-01)*

   1 đoạn prose: cùng app shape deploy lên AgentCore Runtime ở Phần 3.3 không cần refactor transport.

5. **`## prompts.py: Apple Store assistant persona (tiếng Anh)`** — Quote `agent/hera_agent/prompts.py` SYSTEM_PROMPT (8-12 lines from the actual file).
   *Source: agent/hera_agent/prompts.py — Phase 2 Plan 02-01*

   1 dòng prose: Sonic stays in English persona; learner-facing chat language là English (D-17 Phase 2).

6. **`## tools.py: lookup_product calls KB Retrieve`** — Quote the relevant `lookup_product_handler` body (top-1-first format with threshold):

   ```python
   # ... extracted from agent/hera_agent/tools.py ...
   def _kb_retrieve(query: str) -> str:
       # client = boto3.client("bedrock-agent-runtime", region_name=...)
       # response = client.retrieve(knowledgeBaseId=..., retrievalQuery={"text": query}, ...)
       # threshold filter; return "Source: <basename>\n<text>" joined by "\n\n"
       ...
   ```
   *Source: agent/hera_agent/tools.py — Phase 2 Plan 02-01*

   **Pitfall callout (D-51 #6 — tool-use schema strictness):**

   ```markdown
   {{% notice warning %}}
   **Tool-use schema phải chặt chẽ:** Sonic gọi tool dựa trên JSON schema bạn
   register. Schema sai (thiếu field, sai type, thừa key) → Sonic raise validation
   error mid-stream và voice loop ngắt audio. Schema khớp với `FunctionSchema` của
   Pipecat 1.1.0; xem `register_function("lookup_product", handler, cancel_on_interruption=False)`
   ở `pipeline.py`. `cancel_on_interruption=False` quan trọng — KB call mất ~400ms,
   cancel + re-fire mỗi lần barge-in lãng phí latency người dùng.

   *Source: agent/hera_agent/pipeline.py — Phase 2 Plan 02-01*
   {{% /notice %}}
   ```

7. **`## pipeline.py: AWSNovaSonicLLMService + SessionContinuationParams`** — Quote the relevant section (executor reads agent/hera_agent/pipeline.py and quotes ~15 lines around the AWSNovaSonicLLMService construction + SessionContinuationParams + register_function):

   ```python
   # ... extracted from agent/hera_agent/pipeline.py ...
   llm = AWSNovaSonicLLMService(
       access_key_id=os.environ["AWS_ACCESS_KEY_ID"],
       secret_access_key=os.environ["AWS_SECRET_ACCESS_KEY"],
       session_token=os.environ.get("AWS_SESSION_TOKEN"),
       region=os.environ.get("AWS_REGION", "ap-northeast-1"),
   )
   # ...
   pipeline = Pipeline([..., llm, ...])
   task = PipelineTask(pipeline, params=SessionContinuationParams(transition_threshold_seconds=360))
   ```
   *Source: agent/hera_agent/pipeline.py — Phase 2 Plan 02-01*

   **Pitfall callout (D-51 #1 — 8-min Sonic stream cap):**

   ```markdown
   {{% notice warning %}}
   **8-min Sonic stream cap:** Amazon Nova 2 Sonic terminate mỗi single bidirectional
   stream sau ~8 phút (~480s). Pipecat `SessionContinuationParams(transition_threshold_seconds=360)`
   tự động rotate stream ~120s trước cap, không gây gián đoạn audio người dùng.
   Nếu bạn comment out param này, conversation > 8 phút sẽ break giữa câu.

   *Source: agent/hera_agent/pipeline.py — Phase 2 Plan 02-01*
   {{% /notice %}}
   ```

   **Pitfall callout (D-51 #2 — audio sample rate):**

   ```markdown
   {{% notice info %}}
   **Audio sample rate cố định:** input 16kHz mono PCM Int16 (browser AudioWorklet),
   output 24kHz mono PCM (Sonic). Pipecat 1.1.0 mặc định đã đúng — không instantiate
   `AudioConfig` thêm. Nếu bạn cấu hình sai (ví dụ 22.05kHz để khớp browser
   AudioContext default), audio sẽ bị "chipmunk" effect hoặc transcription sẽ silently
   fail. Browser Worklet xử lý anti-alias LPF + decimation từ 48kHz xuống 16kHz.

   *Source: frontend/audio-capture-worklet.js — Phase 2 Plan 02-02*
   {{% /notice %}}
   ```

8. **`## Bước 1: Chạy local theo uv path`** — RUNBOOK lines 240-254:

   ```bash
   cd agent
   cp .env.example .env
   # Edit .env: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, HERA_KB_ID
   #            (optional AWS_SESSION_TOKEN if your creds are short-lived)

   uv sync --frozen
   uv run python -m hera_agent.main
   # starts uvicorn on http://localhost:8080
   # /ping -> {"status":"Healthy", ...}
   # /ws   -> Pipecat WebSocket endpoint
   ```
   *Source: bin/run-agent-local.sh — Phase 2 Plan 02-01*

   1 dòng: HERA_KB_ID resolve from `terraform -chdir=infra/envs/prod output -raw kb_id` (KB id từ Phần 3.1).

9. **`## Bước 2: Chạy local theo Docker compose path`** — RUNBOOK lines 267-294:

   ```bash
   docker compose up
   # agent     -> http://localhost:8080
   # frontend  -> http://localhost:8000
   ```
   *Source: docker-compose.yml — Phase 2 Plan 02-02*

   1 dòng: env-var REQUIRED-syntax `${VAR:?msg}` — nếu thiếu AWS_ACCESS_KEY_ID/SECRET/HERA_KB_ID, container fail-fast trước khi start.

10. **`## Bước 3: Smoke test latency (AGT-04 gate)`** — RUNBOOK + bin/smoke-voice.sh:

    ```bash
    bash bin/smoke-voice.sh
    # ... probe brings up docker compose stack, opens WS to ws://localhost:8080/ws,
    # ... sends 1s of synthetic 16 kHz Int16 silence, awaits first inbound binary frame
    # OK: AGT-04 latency gate passed (LATENCY_MS<3000)
    ```
    *Source: bin/smoke-voice.sh — Phase 2 Plan 02-02*

    1 dòng: chạy 1 lần là đủ — agent đã sẵn sàng cho Phần 3.3 deploy lên AgentCore.

11. **`## Browser test bằng tay`** — 2-3 dòng: mở `http://localhost:8000` → click "record" → cho phép microphone → hỏi "Do you have MacBook Pro?" → Sonic trả lời stock + price. Phần 3.4 sẽ deploy widget này lên CloudFront để bạn dùng URL HTTPS thay vì localhost.

12. **`## Cleanup local Docker resources`** — RUNBOOK lines 322-336:

    ```bash
    docker compose down
    docker rmi hera-agent:dev-multiarch
    # Optional:
    docker buildx prune -f
    ```
    *Source: RUNBOOK.md (Cleanup local Docker resources) — Phase 2 Plan 02-02*

13. **`## Tiếp theo`** — 1 đoạn pointer: agent đã chạy local. Phần 3.3 sẽ build container multi-arch + push ECR + deploy AgentCore Runtime để chạy cùng image trên cloud.

**English file:** mirrors structure 1:1 with translated H2 (`## Goal of this section`, `## Agent project structure`, `## pyproject.toml: pin Python 3.12 + Pipecat extras`, `## main.py: FastAPI shape`, `## prompts.py: Apple Store assistant persona (English)`, `## tools.py: lookup_product calls KB Retrieve`, `## pipeline.py: AWSNovaSonicLLMService + SessionContinuationParams`, `## Step 1: Run locally via uv path`, `## Step 2: Run locally via Docker compose path`, `## Step 3: Smoke test latency (AGT-04 gate)`, `## Manual browser test`, `## Clean up local Docker resources`, `## Next`). All 3 notice bodies translated. All paste-blocks identical (commands + Python source are language-agnostic). Source footers identical.

**Constraints:**
- All fenced code blocks have D-42 source-footer.
- Sub-page front-matter NO chapter:true / NO pre.
- 3 callouts placed: D-51 #6 (tool-use schema, after tools.py section), D-51 #1 (8-min cap, after pipeline.py SessionContinuationParams), D-51 #2 (sample rate, immediately after #1 callout — both relate to pipeline audio behaviour).
- vi+en same commit (D-49).
- Region default `ap-northeast-1` everywhere (D-47).

Per D-40 / D-42 / D-43 / D-49 / D-51 #1 / #2 / #6 / D-47 / Pattern S3 + S4 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/3-hands-on/3.2-pipecat-local/_index.md && test -f content/en/3-hands-on/3.2-pipecat-local/_index.md && grep -q 'weight: 2' content/vi/3-hands-on/3.2-pipecat-local/_index.md && ! grep -q 'chapter: true' content/vi/3-hands-on/3.2-pipecat-local/_index.md && grep -c '\*Source:' content/vi/3-hands-on/3.2-pipecat-local/_index.md | awk '$1>=8{exit 0}{exit 1}' && grep -c '\*Source:' content/en/3-hands-on/3.2-pipecat-local/_index.md | awk '$1>=8{exit 0}{exit 1}' && grep -c '{{% notice' content/vi/3-hands-on/3.2-pipecat-local/_index.md | awk '$1>=3{exit 0}{exit 1}' && grep -c '{{% notice' content/en/3-hands-on/3.2-pipecat-local/_index.md | awk '$1>=3{exit 0}{exit 1}' && grep -q 'AWSNovaSonicLLMService' content/vi/3-hands-on/3.2-pipecat-local/_index.md && grep -q 'AWSNovaSonicLLMService' content/en/3-hands-on/3.2-pipecat-local/_index.md && grep -q 'SessionContinuationParams' content/vi/3-hands-on/3.2-pipecat-local/_index.md && grep -q 'bin/smoke-voice.sh' content/vi/3-hands-on/3.2-pipecat-local/_index.md && grep -q 'ap-northeast-1' content/vi/3-hands-on/3.2-pipecat-local/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter has `title: "3.2 Pipecat Local"`, `weight: 2`, NO `chapter: true`, NO `pre`.
    - Both files contain `>= 8` occurrences of `*Source:` italic footer (D-42 — there are 8+ fenced blocks in this chapter).
    - Both files contain exactly 3 occurrences of `{{% notice` (D-51 #1, #2, #6 — counted with `grep -c '{{% notice' file`).
    - Both files mention literal: `AWSNovaSonicLLMService`, `SessionContinuationParams(transition_threshold_seconds=360)`, `register_function("lookup_product"`, `cancel_on_interruption=False`, `bin/smoke-voice.sh`, `docker compose up`, `uv sync --frozen`, `pipecat-ai[aws-nova-sonic,silero,websocket]==1.1.0`.
    - Both files contain `ap-northeast-1` (D-47).
    - Both files contain `16 kHz` AND `24 kHz` (or `16kHz`/`24kHz`) — pitfall #2 audio sample rate.
    - Both files contain `8 phút` (vi) / `8 minutes` (en) AND `~8 minutes` literal in the cap callout body.
    - One callout uses `{{% notice info %}}` (D-51 #2 sample rate is informational), two use `{{% notice warning %}}` (D-51 #1 cap and #6 schema).
    - `wc -l content/vi/3-hands-on/3.2-pipecat-local/_index.md` >= 100; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/3-hands-on/3.2-pipecat-local/_index.md content/en/3-hands-on/3.2-pipecat-local/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0 (vi=8, en=8 final state — Phần 2 unchanged + 2 new sub-pages each side).
  </acceptance_criteria>
  <done>Phần 3.2 sub-page vi+en authored mirroring RUNBOOK Phase 2 sections; 3 D-51 callouts placed (#1, #2, #6); all snippets carry D-42 footer; parity gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored Markdown content -> Hugo build | content authored by maintainer + Claude; Hugo Goldmark renders with `unsafe = true`; theme JS evaluates Mermaid client-side. Same boundary as Plan 05-01 |
| Image filename references -> deferred operator capture | Phần 2 references `/images/2-preparation/console-bedrock-model-access.png`; the actual PNG is operator-supplied later. Hugo renders alt-text-only placeholder until the file exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Snippets quoting agent/hera_agent/config.py | accept | the file uses `os.environ.get(...)` with defaults; no real credentials are in the source. The chapter snippet shows the env-var NAMES the learner sets in their own `.env` — this is the correct teaching surface. No leaked secrets |
| T-05-02-02 | Tampering | Pitfall callout shortcode body | accept | `unsafe = true` is pre-existing; callout bodies are authored markdown, not user input. Same threat as T-05-01-03 |
| T-05-02-03 | Repudiation | Source-footer drift (footer says "Phase 2 Plan 02-01" but underlying source moves) | mitigate | D-42 + D-43 specify repo-relative path with no SHA — operator can `grep '^\*Source:'` content tree to audit drift. Phase 5 is last v1 phase so source is frozen; drift risk is bounded |
| T-05-02-04 | Information Disclosure | Bedrock Console screenshot (D-44 #1) potentially leaking account ID or IAM ARN | accept | operator follows D-44 capture guidance (red box + arrow + label at the choke point); account-ID redaction is operator's responsibility. The chapter does not enforce — workshop scope, single-maintainer review |

The plan introduces zero new IAM, zero AWS deploys, zero secrets. All snippets reference frozen Phase 1-2 source.
</threat_model>

<verification>
1. `bash bin/check-i18n-parity.sh` exits 0 after each task and after the final commit (vi=8, en=8 by end of plan).
2. `find content -name "_index.md" -type f | wc -l` returns 16 (8 per language).
3. Source-footer audit: `grep -h '^\*Source:' content/{vi,en}/3-hands-on/3.1-knowledge-base/_index.md content/{vi,en}/3-hands-on/3.2-pipecat-local/_index.md content/{vi,en}/2-preparation/_index.md | sort -u | wc -l` >= 10 unique source paths cited.
4. Pitfall callout count: `grep -h '{{% notice' content/{vi,en}/2-preparation/_index.md content/{vi,en}/3-hands-on/3.1-knowledge-base/_index.md content/{vi,en}/3-hands-on/3.2-pipecat-local/_index.md | wc -l` returns 12 (each side: 1 warning + 1 info in Phần 2 + 1 in 3.1 + 3 in 3.2 = 6; both sides = 12).
5. D-44 #1 image reference: `grep -c '/images/2-preparation/console-bedrock-model-access.png' content/{vi,en}/2-preparation/_index.md` returns 2 (one per language).
6. Mermaid byte-identity check N/A this plan (no Mermaid blocks here; introduced in Plan 05-01 for Phần 1).
7. Em-dash check: `grep -c '—' content/vi/3-hands-on/3.2-pipecat-local/_index.md` >= 1 (workshop tone uses em-dash).
8. Em emoji-free: `grep -c '[\x{1F300}-\x{1F9FF}]' content/{vi,en}/2-preparation/_index.md content/{vi,en}/3-hands-on/3.1-knowledge-base/_index.md content/{vi,en}/3-hands-on/3.2-pipecat-local/_index.md` returns 0.
9. No literal `~$X-Y` placeholders shipped to learners: `grep -c '~\$X-Y' content/{vi,en}/2-preparation/_index.md content/{vi,en}/3-hands-on/3.1-knowledge-base/_index.md content/{vi,en}/3-hands-on/3.2-pipecat-local/_index.md` returns 0 (per checker WARNING 2).
10. Submodule untouched: `git diff --submodule themes/hugo-theme-learn` empty.
11. `static/images/2-preparation/.gitkeep` exists.
</verification>

<success_criteria>
- 3 chapters published in vi+en under correct slugs (Phần 2 single-page, Phần 3.1 sub-page, Phần 3.2 sub-page).
- 5 pitfall callouts placed inline (D-51 #1, #2, #3, #6, #8) at choke points specified by CONTEXT.
- 1 image markdown reference for D-44 #1 in Phần 2 (per checker BLOCKER 3).
- 1 `static/images/2-preparation/.gitkeep` placeholder created.
- All paste-blocks have D-42 italic source footer with repo-relative path (D-43 — no SHA, no URL).
- Region default `ap-northeast-1` in every CLI snippet (D-47).
- vi+en authored same plan + same atomic commit per chapter (D-49).
- File-count parity: vi=8, en=8 (count math re-derived per checker WARNING 3).
- bin/check-i18n-parity.sh exits 0 against the final tree.
- Cost numbers anchored to D-54 ballpark `~$2-5 USD per 2-hour session` with post-launch-update notice; no literal `~$X-Y` placeholders ship (per checker WARNING 2).
- Submodule untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-workshop-documentation-vi-en/05-02-SUMMARY.md` recording:

- File-count parity at the end of the plan (vi=8, en=8; recorded explicitly so the audit trail is greppable).
- The 5 pitfall callouts placed (D-51 IDs #1, #2, #3, #6, #8) — record the chapter + section heading where each was placed.
- The image markdown reference inserted (D-44 #1 — `console-bedrock-model-access.png`) and confirmation that `.gitkeep` placeholder lives at `static/images/2-preparation/.gitkeep`.
- The full source-footer inventory for snippets in this plan — confirms D-42/D-43 discipline.
- Any source-quoting deviations (e.g., a Python block quoted from `pipeline.py` that was abbreviated rather than verbatim — documented so a reader can see what was elided).
- The cost-language framing used (D-54 ballpark + post-launch-update notice) — confirms checker WARNING 2 remediation.
- Confirmation that `bash bin/check-i18n-parity.sh` exits 0 against the post-plan tree.

Atomic commits expected (one per chapter, vi+en together — D-49):
1. `chore(05-02): create static/images/2-preparation/.gitkeep` (Task 0).
2. `feat(05-02): publish Phần 2 Preparation (vi+en)` (Task 1).
3. `feat(05-02): publish Phần 3.1 Knowledge Base (vi+en)` (Task 2).
4. `feat(05-02): publish Phần 3.2 Pipecat Local (vi+en)` (Task 3).
5. `docs(05-02): plan summary` (this output step).
</output>

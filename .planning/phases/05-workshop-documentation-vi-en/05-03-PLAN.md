---
phase: 05-workshop-documentation-vi-en
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - content/vi/3-hands-on/3.3-deploy-agentcore/_index.md
  - content/en/3-hands-on/3.3-deploy-agentcore/_index.md
  - content/vi/3-hands-on/3.4-web-widget/_index.md
  - content/en/3-hands-on/3.4-web-widget/_index.md
  - content/vi/3-hands-on/3.5-observability/_index.md
  - content/en/3-hands-on/3.5-observability/_index.md
  - static/images/3.3-deploy-agentcore/.gitkeep
  - static/images/3.4-web-widget/.gitkeep
  - static/images/3.5-observability/.gitkeep
autonomous: true
requirements: [DOC-05, DOC-06, DOC-07, DOC-10, DOC-11]
must_haves:
  truths:
    - "Phần 3.3 Deploy AgentCore sub-page mirrors RUNBOOK Phase 3 + Phase 4 protocol-bridge sections — bin/push-image.sh + cdk deploy + second-pass terraform apply + bin/smoke-deploy.sh"
    - "Phần 3.3 explains hybrid IaC trade-off (Terraform owns everything except AgentCore Runtime which is CDK Python — D-24)"
    - "Phần 3.3 contains the AgentCore concurrency cap=2 quota request screenshot inventory entry (D-44 #3) + D-30 trade-off note"
    - "Phần 3.4 Web Widget sub-page covers presigner Lambda Function URL bridge (D-25 Rule-4 deviation), bin/build-widget.sh sed-injection, deploy widget S3+CloudFront"
    - "Phần 3.4 contains the HTTPS-required-for-mic pitfall callout (D-51 #4)"
    - "Phần 3.5 Observability sub-page walks CloudWatch dashboard hera-prod 5 panels + 2 op alarms + 1 billing alarm cross-region from us-east-1"
    - "Phần 3.5 contains the billing alarm 24h propagation pitfall callout (D-51 #5) + the OBS-04/OBS-05 trade-off explanations (D-30, D-35, D-36)"
    - "Every fenced bash code block is followed by an italic *Source: <repo-path> — Phase X Plan XX-XX* footer (D-42)"
    - "Image alt-text + Markdown image references included for screenshots (operator captures PNGs in a follow-up sweep — D-44 places filename references inline)"
    - "Region default ap-northeast-1 (D-47) in every snippet; billing alarm note explicitly mentions us-east-1 cross-region as AWS service constraint"
    - "vi+en for one chapter land in the same atomic commit (D-49)"
    - "bin/check-i18n-parity.sh exits 0 (vi=11, en=11: 8 from Plan 05-02 + 3 new sub-pages each side)"
  artifacts:
    - path: "content/vi/3-hands-on/3.3-deploy-agentcore/_index.md"
      provides: "Phần 3.3 deploy walkthrough — vi sub-page, 4-step lifecycle + smoke + protocol-bridge"
      min_lines: 110
      contains: "bin/push-image.sh"
    - path: "content/en/3-hands-on/3.3-deploy-agentcore/_index.md"
      provides: "Phần 3.3 — en"
      min_lines: 110
      contains: "bin/push-image.sh"
    - path: "content/vi/3-hands-on/3.4-web-widget/_index.md"
      provides: "Phần 3.4 widget — vi sub-page with presigner + build-widget"
      min_lines: 90
      contains: "presign"
    - path: "content/en/3-hands-on/3.4-web-widget/_index.md"
      provides: "Phần 3.4 widget — en"
      min_lines: 90
      contains: "presign"
    - path: "content/vi/3-hands-on/3.5-observability/_index.md"
      provides: "Phần 3.5 observability — vi sub-page, dashboard 5 panels + alarms cross-region"
      min_lines: 100
      contains: "hera-prod"
    - path: "content/en/3-hands-on/3.5-observability/_index.md"
      provides: "Phần 3.5 — en"
      min_lines: 100
      contains: "hera-prod"
    - path: "static/images/3.3-deploy-agentcore/.gitkeep"
      provides: "screenshot directory placeholder for D-44 inventory items 1, 3 (operator captures later)"
    - path: "static/images/3.4-web-widget/.gitkeep"
      provides: "screenshot directory for D-44 inventory items 4 (widget hero)"
    - path: "static/images/3.5-observability/.gitkeep"
      provides: "screenshot directory for D-44 inventory items 2, 5 (billing toggle, dashboard hero)"
  key_links:
    - from: "content/vi/3-hands-on/3.3-deploy-agentcore/_index.md"
      to: "content/en/3-hands-on/3.3-deploy-agentcore/_index.md"
      via: "matching slug 3.3-deploy-agentcore"
      pattern: "3-hands-on/3.3-deploy-agentcore"
    - from: "Phần 3.3 fenced code blocks"
      to: "RUNBOOK.md Phase 3 + Phase 4 protocol-bridge sections + bin/push-image.sh + bin/smoke-deploy.sh"
      via: "*Source: <path> — Phase X Plan XX-XX* footer"
      pattern: "\\*Source:"
    - from: "Phần 3.4 fenced code blocks"
      to: "infra/modules/widget_presigner/* + bin/build-widget.sh + frontend/app.js"
      via: "*Source: <path> — Phase 3 Plan 03-XX* footer"
      pattern: "\\*Source: .+— Phase 3"
    - from: "Phần 3.5 fenced code blocks"
      to: "infra/modules/observability/* + RUNBOOK Phase 4 observability section"
      via: "*Source: <path> — Phase 4 Plan 04-02* footer"
      pattern: "\\*Source: .+— Phase 4"
---

<objective>
Author 3 chapters in vi+en covering deploy → live widget → observability — the arc from local agent (Phần 3.2) to fully-deployed conversation on a public HTTPS URL with a CloudWatch dashboard watching it.

- **Phần 3.3 Deploy AgentCore sub-page (DOC-05):** mirrors RUNBOOK Phase 3 + Phase 4 protocol-bridge sections. Walks `bin/push-image.sh` (multi-arch buildx → ECR), `cdk deploy hera-agentcore` (CDK Python stack — D-24 hybrid IaC), second-pass `terraform apply -var=agentcore_runtime_arn=<arn>` (Plan 03-04 4-step lifecycle), `bin/smoke-deploy.sh` end-to-end gate, and the `POST /invocations` AgentCore HTTP protocol stub from Plan 04-01. Explains the hybrid IaC trade-off (Terraform owns KB+IAM+widget+observability; CDK Python owns AgentCore Runtime resource because Terraform AgentCore provider has gaps). Contains screenshot inventory entries for D-44 #1 (Bedrock Console model access) — already enabled in Phần 2, but pinned here as the choke-point reference — and D-44 #3 (Service Quotas concurrency request, D-30 cap=2).

- **Phần 3.4 Web Widget sub-page (DOC-06):** covers HTML/JS widget structure, browser microphone via `navigator.mediaDevices.getUserMedia` + AudioWorklet, the presigner Lambda Function URL bridge (D-25 Rule-4 — browser cannot SigV4-sign WebSocket upgrades), `bin/build-widget.sh` sed-injection of `__PRESIGN_URL__`, deploy widget to S3+CloudFront, mic permission UX (5 record-button states from UI-SPEC + verbatim WID-06 error strings). Contains the **HTTPS-required-for-mic pitfall callout** (D-51 #4) at the local-vs-CloudFront comparison.

- **Phần 3.5 Observability sub-page (DOC-07):** walks CloudWatch dashboard `hera-prod` (5 panels: active sessions, latency p50/p95, error rate via metric_query arithmetic, Bedrock invocations+tokens, billing widget cross-region from us-east-1), 2 operational alarms (`hera-error-rate-prod`, `hera-latency-p95-prod`), 1 billing alarm `hera-billing-prod` in us-east-1 (AWS/Billing service constraint — only emits in us-east-1). Explains AgentCore concurrency cap=2 (D-30) as the rate-limit gate and the OBS-04/OBS-05 trade-offs (D-35 manual-stop fallback, D-36 no per-IP rate limit, alarm_actions=[] no SNS hook). Contains the **billing alarm 24h propagation pitfall callout** (D-51 #5).

This plan is `autonomous: true` (per checker WARNING 1 — no checkpoint task, content is publishable without operator gating):
- The screenshot inventory items (D-44 #1, #3, #4, #5) require operator-driven capture from AWS Console and the widget UI. The plan creates `.gitkeep` placeholder directories and inserts image markdown references inline (e.g., `![CloudWatch dashboard hera-prod](/images/3.5-observability/cloudwatch-dashboard-hera-prod.png)`); Hugo renders an alt-text-only placeholder until the PNG lands.
- This is a **deferred operator action** (recorded in `<output>`), NOT a blocking checkpoint — the plan completes autonomously and the screenshot sweep happens out of band.

Per CONTEXT D-44 + D-46 (no GIF/video), all screenshots are static PNGs; the content authoring lands the image references and the operator captures later.

Demo budget honored — zero new AWS deploys, zero new Bedrock streaming smoke. All evidence reused from Phase 3 + Phase 4 live state (AgentCore Runtime hera_agent-GIsf2P4ImD, widget at https://dg0w939ktclw6.cloudfront.net, dashboard hera-prod, 3 alarms).

Output:
- 6 new sub-page `_index.md` files (Phần 3.3 + 3.4 + 3.5 vi+en).
- 3 new directories with `.gitkeep` files (`static/images/{3.3-deploy-agentcore,3.4-web-widget,3.5-observability}/`).
- File-count parity post-plan: vi=11, en=11.
</objective>

<execution_context>
@C:/Users/trant/projects/hera/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/hera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md
@.planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md
@.planning/phases/05-workshop-documentation-vi-en/05-01-PLAN.md
@.planning/phases/05-workshop-documentation-vi-en/05-02-PLAN.md
@CLAUDE.md
@AGENTS.md

# Source files this plan reads for snippets
@RUNBOOK.md
@bin/push-image.sh
@bin/build-widget.sh
@bin/smoke-deploy.sh
@frontend/app.js
@frontend/index.html
@frontend/styles.css
@frontend/audio-capture-worklet.js
@infra/cdk/stack.py
@infra/modules/widget_presigner/main.tf
@infra/modules/observability/main.tf

# Closest pattern analogs
@content/vi/4-cleanup/_index.md
@content/vi/3-hands-on/3.1-knowledge-base/_index.md
@content/vi/3-hands-on/3.2-pipecat-local/_index.md

<interfaces>
<!-- Snippets the executor will reproduce verbatim. Each MUST have a *Source:* footer. -->

**Phần 3.3 — bin/push-image.sh (RUNBOOK lines 379-391):**
```bash
bin/push-image.sh
```
*Source: bin/push-image.sh — Phase 3 Plan 03-03*

**Phần 3.3 — cdk deploy AgentCore (RUNBOOK lines 396-407):**
```bash
cd infra/envs/prod
terraform output -json > terraform-outputs.json
cd ../../..

cd infra/cdk
GIT_SHA=$(git rev-parse --short HEAD)
uv run cdk deploy hera-agentcore --context image_tag=${GIT_SHA} --outputs-file ../../dist/cdk-outputs.json --require-approval never
cd ../..
```
*Source: RUNBOOK.md (Phase 3: AgentCore deploy) — Phase 3 Plan 03-04*

**Phần 3.3 — second-pass terraform apply for presigner (RUNBOOK lines 413-420):**
```bash
RUNTIME_ARN=$(jq -r '."hera-agentcore".AgentCoreRuntimeArn' dist/cdk-outputs.json)
cd infra/envs/prod
terraform apply -var "agentcore_runtime_arn=${RUNTIME_ARN}" -auto-approve
cd ../../..
```
*Source: RUNBOOK.md (Phase 3 Step 3.5) — Phase 3 Plan 03-04*

**Phần 3.3 — bin/smoke-deploy.sh end-to-end gate (RUNBOOK lines 449-451):**
```bash
bin/smoke-deploy.sh
```
*Source: bin/smoke-deploy.sh — Phase 3 Plan 03-04*

**Phần 3.3 — POST /invocations protocol-bridge (RUNBOOK lines 487-545; agent/hera_agent/main.py lines 38-47):**
```python
@app.post("/invocations")
async def invocations() -> JSONResponse:
    """AgentCore HTTP data-plane stub. Voice loop runs on /ws (D-31)."""
    return JSONResponse({"agent": "hera-pipecat-sonic", "status": "running", "model": "amazon.nova-sonic-v1:0"})
```
*Source: agent/hera_agent/main.py — Phase 4 Plan 04-01*

**Phần 3.4 — bin/build-widget.sh (RUNBOOK lines 437):**
```bash
bin/build-widget.sh
```
*Source: bin/build-widget.sh — Phase 3 Plan 03-02*

**Phần 3.4 — presigner pattern explanation (Plan 03-04 SUMMARY + frontend/app.js):**
```javascript
// app.js: resolveWsUrl() fetches the Function URL, unwraps {url}, opens that wss URL
async function resolveWsUrl() {
  const r = await fetch(PRESIGN_URL);   // PRESIGN_URL injected at build time
  const { url } = await r.json();
  return url;
}
```
*Source: frontend/app.js — Phase 3 Plan 03-04*

**Phần 3.5 — terraform apply -var=agentcore_runtime_arn (RUNBOOK lines 588-591):**
```bash
RUNTIME_ARN=$(jq -r '."hera-agentcore".AgentCoreRuntimeArn' dist/cdk-outputs.json)
(cd infra/envs/prod && terraform init -upgrade)
(cd infra/envs/prod && terraform apply -var="agentcore_runtime_arn=$RUNTIME_ARN")
```
*Source: RUNBOOK.md (Phase 4 Observability Apply) — Phase 4 Plan 04-02*

**Phần 3.5 — dashboard URL output (RUNBOOK lines 608-610):**
```bash
(cd infra/envs/prod && terraform output -raw observability_dashboard_url)
```
*Source: RUNBOOK.md (Phase 4 Dashboard walkthrough) — Phase 4 Plan 04-02*

**Phần 3.5 — manual-stop fallback for billing alarm (RUNBOOK lines 631-641):**
```bash
aws bedrock-agentcore-control update-agent-runtime \
  --agent-runtime-id hera_agent-GIsf2P4ImD \
  --region ap-northeast-1 \
  --status STOPPED

# Or via CDK destroy (full teardown of just AgentCore):
(cd infra/cdk && uv run cdk destroy hera-agentcore --force)
```
*Source: RUNBOOK.md (Phase 4 OBS-05 trade-off) — Phase 4 Plan 04-02*

Image markdown references (D-44 + D-45):
```markdown
![Bedrock Console — Model Access for Nova Sonic + Titan v2](/images/3.3-deploy-agentcore/console-bedrock-model-access.png)
![Service Quotas — request AgentCore concurrency 2 (D-30)](/images/3.3-deploy-agentcore/service-quotas-agentcore-concurrency.png)
![Deployed widget UI — idle state](/images/3.4-web-widget/widget-idle-state.png)
![CloudWatch Billing Preferences — Receive Billing Alerts toggle](/images/3.5-observability/billing-alerts-toggle.png)
![CloudWatch Dashboard hera-prod — 5 panels populated](/images/3.5-observability/cloudwatch-dashboard-hera-prod.png)
```

Hugo strips the `static/` prefix → these references work once `static/images/<chapter>/<file>.png` exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create static/images/<chapter>/.gitkeep placeholder directories for screenshot inventory (D-44, D-45)</name>
  <files>
    static/images/3.3-deploy-agentcore/.gitkeep,
    static/images/3.4-web-widget/.gitkeep,
    static/images/3.5-observability/.gitkeep
  </files>
  <read_first>
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-44 screenshot inventory + D-45 storage path convention)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (static/images/<chapter>/ section; precedent `static/images/cloud-clubs-logo.webp` + `static/images/.gitkeep`)
  </read_first>
  <action>
Create 3 new directories under `static/images/` and add a `.gitkeep` file in each so the directory is tracked even when empty. This lets Tasks 2-4 reference image paths in markdown that resolve once an operator drops PNGs in (D-44 #1, #3, #4, #5).

```bash
mkdir -p static/images/3.3-deploy-agentcore
touch static/images/3.3-deploy-agentcore/.gitkeep
mkdir -p static/images/3.4-web-widget
touch static/images/3.4-web-widget/.gitkeep
mkdir -p static/images/3.5-observability
touch static/images/3.5-observability/.gitkeep
```

The `.gitkeep` file is empty (no content) — its sole purpose is to make the otherwise-empty directory visible to git. Pattern matches existing `static/images/.gitkeep` in the repo.

Per D-45 / Pattern S6 (no emojis even in placeholder).
  </action>
  <verify>
    <automated>test -f static/images/3.3-deploy-agentcore/.gitkeep && test -f static/images/3.4-web-widget/.gitkeep && test -f static/images/3.5-observability/.gitkeep && test -d static/images/3.3-deploy-agentcore && test -d static/images/3.4-web-widget && test -d static/images/3.5-observability</automated>
  </verify>
  <acceptance_criteria>
    - 3 directories exist: `static/images/3.3-deploy-agentcore/`, `static/images/3.4-web-widget/`, `static/images/3.5-observability/`.
    - 3 `.gitkeep` files exist (one per directory).
    - Each `.gitkeep` is empty: `[ -s static/images/3.3-deploy-agentcore/.gitkeep ] && exit 1 || exit 0` (succeeds = empty).
    - `git status` shows the 3 new files as untracked / staged additions; no other files modified by this task.
  </acceptance_criteria>
  <done>3 screenshot directories created with empty `.gitkeep` placeholders so Tasks 2-4 can reference filenames.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Phần 3.3 Deploy AgentCore sub-page in vi+en (DOC-05)</name>
  <files>
    content/vi/3-hands-on/3.3-deploy-agentcore/_index.md,
    content/en/3-hands-on/3.3-deploy-agentcore/_index.md
  </files>
  <read_first>
    - RUNBOOK.md (lines 339-485: Phase 3 AgentCore deploy + Cleanup order; lines 487-565: Phase 4 Protocol-bridge deploy)
    - bin/push-image.sh (full file — buildx flags, GIT_SHA tag, --provenance=false / --sbom=false rationale)
    - bin/smoke-deploy.sh (header + step list)
    - infra/cdk/stack.py (read header — AgentCore Runtime resource shape)
    - agent/hera_agent/main.py (lines 38-47 POST /invocations stub — already in execution context)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-40 slug `3.3-deploy-agentcore`, D-44 screenshot inventory items 1+3, D-47 region, locked decisions D-24 hybrid IaC, D-30 quota cap 2, D-48 learner builds own ECR)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (sub-page section + image reference pattern)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md (D-24 hybrid IaC rationale)
  </read_first>
  <action>
Create `content/{vi,en}/3-hands-on/3.3-deploy-agentcore/_index.md`.

Front-matter:
```yaml
---
title: "3.3 Deploy AgentCore"
date: 2025-01-01
weight: 3
---
```

**Body H2 sections (vi-side; en mirrors):**

1. **`## Mục tiêu phần này`** — 1-2 câu: build container multi-arch + push lên ECR của bạn → cdk deploy AgentCore Runtime (CDK Python — D-24 hybrid IaC) → second-pass terraform apply để wire presigner Lambda → bin/smoke-deploy.sh end-to-end gate.

2. **`## Hybrid IaC: Terraform + CDK Python (D-24)`** — 3-4 đoạn ngắn explainer:

   - Terraform `~> 6.27` quản lý mọi thứ TRỪ AgentCore Runtime resource: KB, IAM (kể cả AgentCore exec role), ECR, S3, CloudFront, CloudWatch, presigner Lambda. Lý do: native S3 Vectors + KB + ECR + CloudFront support đầy đủ.
   - AgentCore Runtime resource (`AWS::BedrockAgentCore::Runtime`) chưa có 100% Terraform AWS provider support đầu 2026 → dùng CDK Python để bridge gap (D-04 + D-24 Phase 3).
   - 4-step lifecycle (D-25 amended Plan 03-04): TF apply → push image → cdk deploy → second-pass TF apply (vì widget_presigner Lambda cần biết AgentCore Runtime ARN — chicken-and-egg trước khi TF + CDK cross-talk được).
   - Same-artifact contract: Phase 2 build local Dockerfile multi-arch (linux/arm64,linux/amd64); Phase 3 push CÙNG image lên AgentCore Runtime ARM64-only — không rebuild (AGT-08).

3. **`## Bước 0: AgentCore concurrency mặc định (cap=10)`** — 1-2 dòng prose, theo sau là instructor-reference sidebar:

   AgentCore default account quota = 10 concurrent runtimes/account. **Workshop của bạn dùng cap=10 mặc định không cần làm gì** — ở scale workshop session bạn sẽ không hit cap. Bỏ qua step này và tiếp tục Bước 1.

   ```markdown
   {{% notice info %}}
   **Reference cho instructor (D-30 không áp dụng cho learner):** instructor demo URL chạy với D-30 concurrency cap=2 để cap cost. Nếu bạn (instructor) muốn replicate setup đó:

   1. Mở `https://console.aws.amazon.com/servicequotas/home/services/bedrock-agentcore/quotas` ở `ap-northeast-1`.
   2. Tìm "Concurrent runtime sessions" (hoặc tương tự).
   3. Click "Request quota increase" → nhập 2 → Submit. AWS approve trong 1-2 ngày.

   ![Service Quotas — request AgentCore concurrency 2 (D-30, instructor reference)](/images/3.3-deploy-agentcore/service-quotas-agentcore-concurrency.png)

   *Source: RUNBOOK.md + Phase 3 D-30 — Phase 3 Plan 03-04*
   {{% /notice %}}
   ```

   Per checker WARNING 5: D-44 #3 screenshot is moved out of the learner-mainline path into this instructor-reference sidebar so the workshop default flow is "skip step, cap=10 is fine" — no inline screenshot mandated for learners.

4. **`## Bước 1: Apply Terraform (KB + IAM + ECR + widget hosting)`** — paste-block (RUNBOOK lines 369-373):

   ```bash
   cd infra/envs/prod
   terraform init
   terraform plan -out plan.out
   terraform apply plan.out
   cd ../../..
   ```
   *Source: RUNBOOK.md (Phase 3 Step 1) — Phase 3 Plan 03-01*

   1 dòng prose: outputs Phase 3 thêm `ecr_repo_url`, `agentcore_exec_role_arn`, `agentcore_log_group_arn`, `agentcore_log_group_name`, `widget_cloudfront_url`, `widget_s3_bucket_name`, `widget_cloudfront_distribution_id`. Phần 3.4 sẽ dùng widget outputs.

5. **`## Bước 2: Build + push container lên ECR (của bạn)`** — paste-block (RUNBOOK lines 379-381):

   ```bash
   bin/push-image.sh
   ```
   *Source: bin/push-image.sh — Phase 3 Plan 03-03*

   Script làm gì (5-6 bullet points cô đọng):
   - Resolve `ecr_repo_url` từ terraform outputs.
   - Compute `git rev-parse --short HEAD` → image tag (ECR `imageTagMutability=IMMUTABLE` per Plan 03-01 — không dùng `:latest`).
   - `aws ecr get-login-password | docker login` ECR registry của bạn.
   - Tạo idempotent buildx builder `hera-builder` nếu chưa có.
   - `docker buildx build --platform linux/arm64,linux/amd64 --provenance=false --sbom=false --push -t ${ECR_URL}:${GIT_SHA} ./agent`.
   - `aws ecr describe-images --image-ids imageTag=${GIT_SHA}` confirm manifest landed.

   Note quan trọng: `--provenance=false` + `--sbom=false` BẮT BUỘC. BuildKit v0.11+ default emit OCI in-toto attestation manifests — ECR manifest validator REJECT với `UnsupportedMediaTypeException` (D-23 carry-forward Phase 3 Plan 03-03).

6. **`## Bước 3: cdk deploy AgentCore Runtime`** — paste-block (RUNBOOK lines 396-407):

   ```bash
   # Dump terraform outputs so the CDK app can read them (TF -> CDK bridge).
   cd infra/envs/prod
   terraform output -json > terraform-outputs.json
   cd ../../..

   # Provision/replace the AgentCore Runtime resource. Captures the runtime ARN
   # in dist/cdk-outputs.json under hera-agentcore.AgentCoreRuntimeArn.
   cd infra/cdk
   GIT_SHA=$(git rev-parse --short HEAD)
   uv run cdk deploy hera-agentcore --context image_tag=${GIT_SHA} --outputs-file ../../dist/cdk-outputs.json --require-approval never
   cd ../..
   ```
   *Source: RUNBOOK.md (Phase 3 Step 3) — Phase 3 Plan 03-04*

   1 đoạn prose: CDK chỉ quản lý 1 resource — `hera-agentcore` stack với `AWS::BedrockAgentCore::Runtime`. Outputs `AgentCoreRuntimeArn` ghi ra `dist/cdk-outputs.json`. Concurrency cap KHÔNG có ở schema CFn (verified `aws cloudformation describe-type` Plan 03-04) — service quota mới là gate (xem Bước 0).

7. **`## Bước 3.5: Second-pass terraform apply`** — paste-block (RUNBOOK lines 413-420):

   ```bash
   RUNTIME_ARN=$(jq -r '."hera-agentcore".AgentCoreRuntimeArn' dist/cdk-outputs.json)
   cd infra/envs/prod
   terraform apply -var "agentcore_runtime_arn=${RUNTIME_ARN}" -auto-approve
   cd ../../..
   ```
   *Source: RUNBOOK.md (Phase 3 Step 3.5) — Phase 3 Plan 03-04*

   2-3 dòng prose: chicken-and-egg — `widget_presigner` Lambda cần Runtime ARN trong env var + IAM policy resource scope, nhưng Runtime do CDK tạo SAU TF first apply. Solution: TF first apply dùng default empty-string placeholder ARN, second-pass apply swap thành real ARN. Fully wired sau bước này.

8. **`## Bước 4: Smoke gate end-to-end (bin/smoke-deploy.sh)`** — paste-block (RUNBOOK lines 449-451):

   ```bash
   bin/smoke-deploy.sh
   ```
   *Source: bin/smoke-deploy.sh — Phase 3 Plan 03-04*

   What it does (RUNBOOK lines 453-463 distilled):
   1. terraform output → cdk read.
   2. cdk deploy hera-agentcore (idempotent — second run no-op nếu image tag không đổi).
   3. Extract `AgentCoreRuntimeArn`.
   4. Second-pass terraform apply.
   5. Read `presign_url` output, export as `PRESIGN_URL`.
   6. `bin/build-widget.sh` (sed-inject + s3 sync + invalidate).
   7. `curl -fsS https://<cloudfront-domain>/` HTTPS gate (DEM-01 verify).
   8. `curl -fsS ${PRESIGN_URL}` presigner gate (expects `{"url": "wss://..."}`).
   9. `uv run python bin/_smoke_deploy_probe.py` — open WSS, send 1s synthetic 16kHz Int16 silence, assert >=1 inbound binary frame within 10s.

   Success ends with:
   ```
   OK: Phase 3 smoke passed.
       Widget URL  : https://<your-distribution>.cloudfront.net
       Presign URL : https://<your-fn-url>.lambda-url.ap-northeast-1.on.aws/
       Runtime ARN : arn:aws:bedrock-agentcore:ap-northeast-1:<your-account>:runtime/...
       Image tag   : <git-sha>
   ```

9. **`## POST /invocations: AgentCore HTTP protocol contract (Phase 4 Plan 04-01)`** — quote `agent/hera_agent/main.py` lines 38-47 verbatim:

   ```python
   @app.post("/invocations")
   async def invocations() -> JSONResponse:
       """AgentCore HTTP data-plane stub. Voice loop runs on /ws (D-31)."""
       return JSONResponse(
           {
               "agent": "hera-pipecat-sonic",
               "status": "running",
               "model": "amazon.nova-sonic-v1:0",
           }
       )
   ```
   *Source: agent/hera_agent/main.py — Phase 4 Plan 04-01*

   2 đoạn prose:
   - AgentCore HTTP protocol mặc định gọi container ở `POST /invocations` per Bedrock convention (chọn bởi `ProtocolConfiguration: HTTP` trong CDK stack Phase 3).
   - Voice loop thật chạy ở `/ws` (Pipecat WebSocket transport). `/invocations` chỉ là static envelope stub để satisfy AgentCore data-plane invocation API trả về 200 — Phase 3 SC#2 closure (Plan 04-01 deployed Runtime version=3 status=READY referencing image hera-agent:7e72b66).

10. **`## Live state (instructor reference)`** — 4-5 bullet (sử dụng Phase 3-4 evidence; D-54 cost ranges):

    - Account: `851725411875` (instructor; bạn dùng account của bạn).
    - Region: `ap-northeast-1`.
    - Runtime: `hera_agent-GIsf2P4ImD` (instructor's; cdk deploy của bạn tạo runtime mới).
    - Live URL: `https://dg0w939ktclw6.cloudfront.net/` (instructor demo; deferred SC#2 visible-to-instructor closure trong 04-HUMAN-UAT.md).
    - Cost active: AgentCore Runtime + Sonic streaming theo D-54 ballpark `~$2-5 USD per 2-hour session`. Số liệu per-service breakdown chính xác sẽ cập nhật post-launch sau khi instructor pull 24h Cost Explorer data từ một workshop session thực tế (04-HUMAN-UAT item #4).

11. **`## Cleanup order`** — paste-block (RUNBOOK lines 478-483):

    ```bash
    # Tear down CDK first, then Terraform.
    cd infra/cdk && uv run cdk destroy hera-agentcore --force && cd ../..
    cd infra/envs/prod && terraform destroy && cd ../..
    ```
    *Source: RUNBOOK.md (Phase 3 Cleanup order) — Phase 3 Plan 03-04*

    1 dòng: chi tiết cleanup ở Phần 4. CDK trước vì AgentCore Runtime giữ IAM exec role TF quản lý — reverse order → terraform destroy fail.

12. **`## Tiếp theo`** — 1 đoạn pointer: AgentCore Runtime đang chạy. Phần 3.4 deploy widget HTML/JS lên S3+CloudFront để bạn dùng URL HTTPS thay localhost.

**English file:** mirrors structure 1:1 with translated H2 + prose. All paste-blocks identical (commands language-agnostic). Source-footers identical.

**Constraints:**
- Sub-page front-matter (NO chapter:true / NO pre).
- All fenced blocks have D-42 source-footer.
- 2 image references in Phần 3.3 (model access — already in Phần 2, but mention here as cross-link / Service Quotas concurrency).
- Region default `ap-northeast-1` (D-47).
- D-30 + D-24 trade-offs explicitly explained (CONTEXT carried).
- vi+en same commit (D-49).

Per D-40 / D-42 / D-43 / D-49 / D-47 / D-30 / D-24 / Pattern S3 + S5 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && test -f content/en/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q 'weight: 3' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && ! grep -q 'chapter: true' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && grep -c '\*Source:' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md | awk '$1>=8{exit 0}{exit 1}' && grep -c '\*Source:' content/en/3-hands-on/3.3-deploy-agentcore/_index.md | awk '$1>=8{exit 0}{exit 1}' && grep -q 'bin/push-image.sh' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q 'bin/smoke-deploy.sh' content/en/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q 'cdk deploy hera-agentcore' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q 'POST /invocations' content/en/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q 'ap-northeast-1' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && grep -q '/images/3.3-deploy-agentcore/' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter `title: "3.3 Deploy AgentCore"`, `weight: 3`, NO chapter:true / NO pre.
    - Both files contain >= 8 `*Source:` italic footers.
    - Both files contain literal: `bin/push-image.sh`, `bin/smoke-deploy.sh`, `cdk deploy hera-agentcore`, `terraform apply -var`, `--provenance=false`, `--sbom=false`, `linux/arm64,linux/amd64`, `git rev-parse --short HEAD`, `dist/cdk-outputs.json`, `POST /invocations`.
    - Both files contain at least 1 image markdown reference under `/images/3.3-deploy-agentcore/` (Service Quotas screenshot D-44 #3).
    - Both files contain `D-24` and `D-30` literal mentions (trade-off attribution).
    - Both files contain `ap-northeast-1` literal (D-47).
    - `wc -l content/vi/3-hands-on/3.3-deploy-agentcore/_index.md` >= 110; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/3-hands-on/3.3-deploy-agentcore/_index.md content/en/3-hands-on/3.3-deploy-agentcore/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0 after this task (vi=9, en=9: 8 from Plan 05-02 + 1 new sub-page each side).
  </acceptance_criteria>
  <done>Phần 3.3 sub-page vi+en authored mirroring RUNBOOK Phase 3 + Phase 4 protocol-bridge sections; D-24 + D-30 trade-offs documented; D-42 footer on every fenced block; image reference for D-44 #3 placed; parity gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Phần 3.4 Web Widget sub-page in vi+en (DOC-06 + D-51 #4)</name>
  <files>
    content/vi/3-hands-on/3.4-web-widget/_index.md,
    content/en/3-hands-on/3.4-web-widget/_index.md
  </files>
  <read_first>
    - frontend/index.html (full file — record button + status pill + transcript)
    - frontend/app.js (resolveWsUrl + presign fetch + 5-state record button machine)
    - frontend/audio-capture-worklet.js (16kHz Int16 capture path)
    - frontend/styles.css (5 button states .btn-{idle,connecting,listening,speaking,error})
    - bin/build-widget.sh (sed-injection + s3 sync + invalidate)
    - infra/modules/widget_presigner/main.tf (read top — Lambda Function URL + IAM scope to AgentCore Runtime ARN)
    - RUNBOOK.md (lines 432-444 — Step 4 Deploy widget section)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md (D-25 Rule-4 widget_presigner deviation, D-26 custom domain deferral)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-40 slug `3.4-web-widget`, D-44 #4 widget hero screenshot, D-51 #4 HTTPS-required-for-mic placement)
  </read_first>
  <action>
Create `content/{vi,en}/3-hands-on/3.4-web-widget/_index.md`.

Front-matter:
```yaml
---
title: "3.4 Web Widget"
date: 2025-01-01
weight: 4
---
```

**Body H2 sections (vi-side):**

1. **`## Mục tiêu phần này`** — 1-2 câu: deploy widget HTML/JS lên S3+CloudFront, browser mở URL HTTPS, click record, allow mic, nói chuyện với AgentCore Runtime đã deploy ở Phần 3.3.

2. **`## Cấu trúc widget (frontend/)`** — tree + 1 đoạn:

   ```
   frontend/
   ├── index.html               # record button + status pill + transcript area
   ├── app.js                   # 5-state machine + resolveWsUrl + heartbeat
   ├── styles.css               # Apple Store light: .btn-{idle,connecting,listening,speaking,error}
   └── audio-capture-worklet.js # AudioWorklet: 48kHz mic -> anti-alias LPF -> 16kHz Int16
   ```
   *Source: frontend/ — Phase 3 Plan 03-02*

   1 đoạn: tối giản, không framework — single-file gọn để host được trên S3+CloudFront hoặc GitHub Pages (WID-01).

3. **`## Vì sao cần presigner Lambda (D-25 Rule-4)`** — 3-4 đoạn explainer:

   - Browser KHÔNG SigV4-sign WebSocket upgrade trực tiếp được — `WebSocket` API không expose Authorization header config.
   - AgentCore Runtime `wss://...` endpoint REQUIRE SigV4 auth.
   - Solution: Lambda Function URL (presigner) chạy trong AWS, dùng IAM exec-role credentials, tạo SigV4-presigned WSS URL TTL 300s, trả `{"url": "wss://..."}` khi browser fetch.
   - Browser load widget → click record → fetch presigner Function URL → mở `wss://` URL nhận về.

   Architecture (referenced from Phần 1 sequence diagram).

4. **`## app.js: presigner integration`** — quote `frontend/app.js` `resolveWsUrl()` function verbatim (executor reads file and pulls ~10 lines):

   ```javascript
   async function resolveWsUrl() {
     // PRESIGN_URL injected at build time by bin/build-widget.sh (sed-replace __PRESIGN_URL__).
     // Local-dev keeps ws://localhost:8080/ws via the typeof guard.
     if (typeof __PRESIGN_URL__ !== "undefined") {
       const r = await fetch(__PRESIGN_URL__);
       const { url } = await r.json();
       return url;
     }
     return "ws://localhost:8080/ws";
   }
   ```
   *Source: frontend/app.js — Phase 3 Plan 03-04*

   1 dòng: source tree giữ `__PRESIGN_URL__` placeholder; `bin/build-widget.sh` sed-replace lúc deploy.

5. **`## Audio capture: 16 kHz Int16 PCM (frontend/audio-capture-worklet.js)`** — quote ~5-8 lines representing the LPF + decimation pattern (executor reads file).
   *Source: frontend/audio-capture-worklet.js — Phase 2 Plan 02-02*

   1 dòng: chrome MediaRecorder mặc định 48kHz; AudioWorklet xử lý anti-alias LPF + cursor-based decimation xuống 16kHz Int16 trước khi gửi WSS — Sonic input format khớp đúng.

6. **`## 5-state record button (UX)`** — table (5 rows: state name + UI label + button color + WID-06 error text):

   | State | Pill text | Button color | WID-06 error string |
   |-------|-----------|--------------|---------------------|
   | idle | Press to talk | white/blue | — |
   | connecting | Connecting | gray | — |
   | listening | Listening | red dot | — |
   | speaking | Replying | blue | — |
   | error | (error variant) | red | "Couldn't reach the agent —" / "The agent didn't respond in time —" / "Microphone is muted —" |

   *Source: frontend/app.js + frontend/styles.css — Phase 3 Plan 03-02*

   1 dòng: WID-06 error strings verbatim từ UI-SPEC; em-dash giữ nguyên (Plan 03-02 rationale).

7. **`## Bước 1: Build + deploy widget (bin/build-widget.sh)`** — paste-block:

   ```bash
   bin/build-widget.sh
   ```
   *Source: bin/build-widget.sh — Phase 3 Plan 03-02*

   What it does:
   - Reads `presign_url` from `terraform output -raw presign_url`.
   - sed-replace `__PRESIGN_URL__` literal in copied `dist/widget/app.js`.
   - 2 sanity gates: `__AGENTCORE_WSS_URL__` placeholder gone (legacy), `ws://localhost:8080` localhost-dev URL gone — exit 3 if either fail.
   - `aws s3 sync dist/widget/ s3://<widget-bucket>/ --delete`.
   - `aws cloudfront create-invalidation --paths /index.html /app.js /styles.css /audio-capture-worklet.js` (4 specific paths, không `/*` — under 1000-free-paths/month threshold).

8. **`## Bước 2: Mở widget URL`** —

   ```bash
   terraform -chdir=infra/envs/prod output -raw widget_cloudfront_url
   # https://<your-distribution>.cloudfront.net/
   # → mở trong browser desktop (Chrome/Firefox/Safari đều OK).
   # → click "record" → cho phép microphone access.
   ```
   *Source: RUNBOOK.md (Phase 3 Step 4) — Phase 3 Plan 03-04*

   ```markdown
   ![Widget UI — idle state](/images/3.4-web-widget/widget-idle-state.png)
   ```

   **Pitfall callout (D-51 #4 — HTTPS required for mic):**

   ```markdown
   {{% notice warning %}}
   **HTTPS bắt buộc cho microphone:** browser CHỈ cho phép `getUserMedia()` access
   trong secure context — `https://` hoặc `localhost`. Nếu mở widget qua
   `http://<ip>:8000` (không HTTPS, không localhost), permission prompt KHÔNG hiện
   và `getUserMedia` reject với `NotAllowedError` ngay lập tức. CloudFront
   distribution của bạn `viewer_protocol_policy=redirect-to-https` (Plan 03-01) tự
   redirect HTTP → HTTPS nên path workshop OK. Nếu test local trên IP LAN, dùng
   `localhost` ở browser cùng máy (Phần 3.2 docker compose path) thay cho IP.

   *Source: frontend/app.js — Phase 3 Plan 03-02*
   {{% /notice %}}
   ```

9. **`## Custom domain (deferred v2)`** — 2 đoạn ngắn (D-26 deferral):

   - Default CloudFront cert `*.cloudfront.net` đủ cho workshop (HTTPS-only enforced).
   - Custom domain + ACM cert (kèm `MinimumProtocolVersion = TLSv1.2_2021`) deferred v2 — không phải v1 workshop scope.

10. **`## Tiếp theo`** — 1 đoạn pointer: widget đang live trên CloudFront URL của bạn. Phần 3.5 sẽ ship CloudWatch dashboard + alarms để bạn theo dõi traffic + cost của widget.

**English file:** mirrors structure 1:1. Notice body translated. Em-dash + WID-06 error strings verbatim.

**Constraints:**
- Sub-page front-matter.
- D-42 footer on every fenced block (>= 6 fenced blocks expected).
- 1 callout (D-51 #4 HTTPS-for-mic).
- 1 image reference under `/images/3.4-web-widget/` (D-44 #4 widget hero).
- Region default `ap-northeast-1` (D-47).
- vi+en same commit (D-49).

Per D-40 / D-42 / D-43 / D-49 / D-51 #4 / Pattern S3 + S4 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/3-hands-on/3.4-web-widget/_index.md && test -f content/en/3-hands-on/3.4-web-widget/_index.md && grep -q 'weight: 4' content/vi/3-hands-on/3.4-web-widget/_index.md && ! grep -q 'chapter: true' content/vi/3-hands-on/3.4-web-widget/_index.md && grep -c '{{% notice warning %}}' content/vi/3-hands-on/3.4-web-widget/_index.md | grep -qE '^[1-9]' && grep -c '{{% notice warning %}}' content/en/3-hands-on/3.4-web-widget/_index.md | grep -qE '^[1-9]' && grep -c '\*Source:' content/vi/3-hands-on/3.4-web-widget/_index.md | awk '$1>=5{exit 0}{exit 1}' && grep -q 'bin/build-widget.sh' content/vi/3-hands-on/3.4-web-widget/_index.md && grep -q 'getUserMedia' content/en/3-hands-on/3.4-web-widget/_index.md && grep -q '__PRESIGN_URL__' content/vi/3-hands-on/3.4-web-widget/_index.md && grep -q '/images/3.4-web-widget/' content/vi/3-hands-on/3.4-web-widget/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter `title: "3.4 Web Widget"`, `weight: 4`.
    - Both files contain >= 5 `*Source:` italic footers.
    - Both files contain literal: `bin/build-widget.sh`, `__PRESIGN_URL__`, `getUserMedia`, `resolveWsUrl`, `widget_cloudfront_url`, `cloudfront create-invalidation`.
    - Both files contain at least 1 `{{% notice warning %}}` (D-51 #4).
    - Both files contain at least 1 image markdown reference under `/images/3.4-web-widget/` (D-44 #4).
    - Both files contain `ap-northeast-1` (D-47).
    - Both files contain WID-06 error string fragments: `Couldn't reach the agent` AND `agent didn't respond in time` AND `Microphone is muted`.
    - `wc -l content/vi/3-hands-on/3.4-web-widget/_index.md` >= 90; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/3-hands-on/3.4-web-widget/_index.md content/en/3-hands-on/3.4-web-widget/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0 (vi=10, en=10: 8 from Plan 05-02 + 2 new sub-pages each side; 1 more added in Task 4).
  </acceptance_criteria>
  <done>Phần 3.4 sub-page vi+en authored covering presigner bridge + build-widget + 5-state UX; D-51 #4 HTTPS-for-mic callout placed; D-44 #4 widget hero image reference; parity gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author Phần 3.5 Observability sub-page in vi+en (DOC-07 + D-51 #5 + D-35/D-36 trade-offs)</name>
  <files>
    content/vi/3-hands-on/3.5-observability/_index.md,
    content/en/3-hands-on/3.5-observability/_index.md
  </files>
  <read_first>
    - RUNBOOK.md (lines 566-662: Phase 4 Observability dashboard walkthrough + OBS-04/OBS-05 trade-off sections — full source for snippets)
    - infra/modules/observability/main.tf (read header + dashboard widget structure + alarm configurations)
    - .planning/phases/04-observability-cost-control-cleanup/04-CONTEXT.md (D-31..D-36 — alarm_actions=[] D-35, no per-IP rate limit D-36, billing alarm cross-region D-32)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-40 slug `3.5-observability`, D-44 #2 + #5, D-51 #5 billing alarm propagation placement)
    - .planning/phases/04-observability-cost-control-cleanup/04-02-PLAN.md (alarm names + thresholds reference)
  </read_first>
  <action>
Create `content/{vi,en}/3-hands-on/3.5-observability/_index.md`.

Front-matter:
```yaml
---
title: "3.5 Observability"
date: 2025-01-01
weight: 5
---
```

**Body H2 sections (vi-side):**

1. **`## Mục tiêu phần này`** — 1-2 câu: deploy CloudWatch dashboard `hera-prod` (5 panels) + 2 op alarms + 1 billing alarm để theo dõi traffic + cost agent của bạn.

2. **`## Pre-flight: bật Receive Billing Alerts (one-time)`** — 3-4 dòng + screenshot:

   ```markdown
   ![CloudWatch Billing Preferences — Receive Billing Alerts toggle](/images/3.5-observability/billing-alerts-toggle.png)
   ```

   Steps:
   1. Mở `https://console.aws.amazon.com/billing/home#/preferences`.
   2. Edit Alert preferences → tick "Receive CloudWatch Billing Alerts" → Save.
   3. Wait ~15 phút để billing data start flowing vào CloudWatch.

   *Source: RUNBOOK.md (Phase 4 Pre-flight) — Phase 4 Plan 04-02*

   Note quan trọng: chưa tick → terraform apply vẫn pass, nhưng billing alarm INSUFFICIENT_DATA mãi (không phải bug). Đây là per-AWS-account toggle.

3. **`## Bước 1: terraform apply observability module`** — paste-block (RUNBOOK lines 588-591):

   ```bash
   RUNTIME_ARN=$(jq -r '."hera-agentcore".AgentCoreRuntimeArn' dist/cdk-outputs.json)
   (cd infra/envs/prod && terraform init -upgrade)
   (cd infra/envs/prod && terraform apply -var="agentcore_runtime_arn=$RUNTIME_ARN")
   ```
   *Source: RUNBOOK.md (Phase 4 Observability Apply) — Phase 4 Plan 04-02*

   2 dòng prose: observability module ship `infra/modules/observability/` (4-file shape: versions/variables/main/outputs) với `configuration_aliases=[aws.us_east_1]` để billing alarm có thể tạo ở us-east-1 từ ap-northeast-1 prod root (AWS/Billing namespace chỉ emit ở us-east-1). Var `agentcore_runtime_arn` pass-through giữ widget_presigner Lambda env var đúng (D-25 4-step lifecycle).

   Expected plan: 4 to add (1 dashboard + 3 alarms). In-place changes không liên quan (CloudFront TLS auto-bump, S3 policy provider re-encoding) — benign drift, accept.

4. **`## Bước 2: Mở dashboard và walk-through 5 panels`** —

   ```bash
   (cd infra/envs/prod && terraform output -raw observability_dashboard_url)
   ```
   *Source: RUNBOOK.md (Phase 4 Dashboard walkthrough) — Phase 4 Plan 04-02*

   ```markdown
   ![CloudWatch Dashboard hera-prod — 5 panels populated](/images/3.5-observability/cloudwatch-dashboard-hera-prod.png)
   ```

   5 panels (left-to-right, top-to-bottom):

   | # | Panel | Metric | Visualization | Note |
   |---|-------|--------|---------------|------|
   | 1 | Active sessions | AgentCore `ActiveStreamingConnections` 1-min sum | singleValue | Updates live during a voice session |
   | 2 | Latency p50 / p95 | AgentCore `Latency` end-to-end ms | timeSeries | extended_statistic p50 + p95 |
   | 3 | Error rate | `100 * TotalErrors / Invocations` | timeSeries | metric_query arithmetic — returns 0 when no invocations |
   | 4 | Bedrock invocations + tokens | `AWS/Bedrock` `Invocations` + `InputTokenCount` + `OutputTokenCount` for `amazon.nova-sonic-v1:0` | timeSeries | proxy for cost |
   | 5 | Estimated charges | `AWS/Billing` `EstimatedCharges` `Currency=USD` | singleValue (us-east-1 cross-region) | updated every 6 hours |

   "No data available" trên fresh dashboard là expected — fire vài voice sessions ở widget URL Phần 3.4 để populate.

5. **`## Alarms (3 cái)`** — table:

   | Alarm name | Region | Threshold | Period | Action |
   |------------|--------|-----------|--------|--------|
   | `hera-error-rate-prod` | ap-northeast-1 | error rate > 5% | 5 min | alarm_actions=[] (D-35) |
   | `hera-latency-p95-prod` | ap-northeast-1 | latency p95 > 5000ms | 5 min | alarm_actions=[] |
   | `hera-billing-prod` | us-east-1 (cross-region) | EstimatedCharges > $5/day (D-29) | 6h | alarm_actions=[] |

   *Source: infra/modules/observability/main.tf — Phase 4 Plan 04-02*

   2 đoạn prose:
   - `alarm_actions=[]` per D-35 — không SNS, không email, không Lambda hook auto-stop. Trade-off accepted: instructor monitor dashboard tay; manual-stop fallback ở section sau.
   - Billing alarm cross-region constraint: `AWS/Billing` namespace chỉ emit ở us-east-1; observability module dùng provider alias `aws.us_east_1` để tạo alarm ở us-east-1 từ prod root chạy ở ap-northeast-1.

   **Pitfall callout (D-51 #5 — billing alarm 24h propagation):**

   ```markdown
   {{% notice warning %}}
   **Billing alarm có 24h propagation lag:** dù `aws cloudwatch describe-alarms` trả
   về alarm ngay sau apply, `EstimatedCharges` metric có thể mất 6-24 giờ để start
   populate (AWS service constraint). Trong workshop session 2 giờ, alarm gần như
   chắc chắn ở `INSUFFICIENT_DATA` đến hết session — KHÔNG phải bug. Nếu bạn cần
   test thực tế, deploy sáng nay rồi check sáng mai. Đồng thời: bạn phải đã tick
   "Receive CloudWatch Billing Alerts" ở Pre-flight section trên — chưa tick thì
   alarm `INSUFFICIENT_DATA` mãi.

   *Source: RUNBOOK.md (Phase 4 Observability Pre-flight) — Phase 4 Plan 04-02*
   {{% /notice %}}
   ```

6. **`## OBS-04 trade-off — không có per-IP rate limit (D-36)`** — quote RUNBOOK lines 648-662 distilled:

   - Presigner Lambda Function URL OPEN (không per-IP rate limit).
   - Effective rate-limit gate: AgentCore Runtime concurrency cap=2 (D-30 service quota — Phần 3.3 Bước 0). Abuse via cached URL replay → 503 từ runtime sau khi cap hit.
   - KHÔNG ship: AWS WAF rate-based rule trên CloudFront (~$5/tháng base — out of demo budget); DynamoDB token-bucket Lambda (added complexity for marginal benefit at 2-session cap).
   - Trade-off accepted: presigner open; AgentCore concurrency là gate. v2 với real public traffic → add WAF hoặc token-bucket — explicitly out of scope per D-36.

   *Source: RUNBOOK.md (Phase 4 OBS-04 trade-off) — Phase 4 Plan 04-02*

7. **`## OBS-05 trade-off — billing alarm manual-stop fallback (D-35)`** — quote RUNBOOK lines 623-646:

   Khi alarm transitions to ALARM state (visible trên dashboard billing panel + via `aws cloudwatch describe-alarms`), respond MANUAL:

   ```bash
   aws bedrock-agentcore-control update-agent-runtime \
     --agent-runtime-id hera_agent-GIsf2P4ImD \
     --region ap-northeast-1 \
     --status STOPPED

   # Or via CDK destroy (full teardown of just AgentCore):
   (cd infra/cdk && uv run cdk destroy hera-agentcore --force)
   ```
   *Source: RUNBOOK.md (Phase 4 OBS-05 trade-off) — Phase 4 Plan 04-02*

   1 đoạn: trade-off accepted — instructor monitor dashboard tay. v2 với real public traffic → add SNS topic + email subscription + (optional) Lambda hook to automate — explicitly out of scope per D-35.

8. **`## Live evidence (instructor reference)`** — 4-5 bullet:

   - Dashboard URL (instructor): truy cập qua `terraform output -raw observability_dashboard_url` ở instructor account.
   - Free tier: 1 dashboard + 10 alarms = $0/tháng (instructor verified).
   - Bedrock cost panel mất 24h start populate sau lần invoke đầu.
   - Instructor's billing alarm sit `INSUFFICIENT_DATA` đến khi RESEARCH A1 toggle tick.

9. **`## Tiếp theo`** — 1 đoạn pointer: deploy đầy đủ + observability đã chạy. Phần 4 sẽ tear down toàn bộ về $0 + verify với `bin/cleanup-verify.sh`.

**English file:** mirrors structure 1:1. Notice body translated.

**Constraints:**
- Sub-page front-matter.
- D-42 footer on every fenced block (>= 6).
- 1 callout (D-51 #5 billing alarm propagation).
- 2 image references under `/images/3.5-observability/` (D-44 #2 + #5).
- D-35 + D-36 + D-30 explicitly explained (CONTEXT carried).
- Region default `ap-northeast-1` for everything except billing alarm (us-east-1 cross-region constraint explained).
- vi+en same commit (D-49).

Per D-40 / D-42 / D-43 / D-49 / D-51 #5 / D-35 / D-36 / Pattern S3 + S4 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/3-hands-on/3.5-observability/_index.md && test -f content/en/3-hands-on/3.5-observability/_index.md && grep -q 'weight: 5' content/vi/3-hands-on/3.5-observability/_index.md && grep -c '{{% notice warning %}}' content/vi/3-hands-on/3.5-observability/_index.md | grep -qE '^[1-9]' && grep -c '\*Source:' content/vi/3-hands-on/3.5-observability/_index.md | awk '$1>=6{exit 0}{exit 1}' && grep -q 'hera-prod' content/vi/3-hands-on/3.5-observability/_index.md && grep -q 'us-east-1' content/en/3-hands-on/3.5-observability/_index.md && grep -q 'D-35' content/vi/3-hands-on/3.5-observability/_index.md && grep -q 'D-36' content/en/3-hands-on/3.5-observability/_index.md && grep -c '/images/3.5-observability/' content/vi/3-hands-on/3.5-observability/_index.md | grep -qE '^[2-9]' && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter `title: "3.5 Observability"`, `weight: 5`.
    - Both files contain >= 6 `*Source:` italic footers.
    - Both files contain literal: `hera-prod`, `hera-error-rate-prod`, `hera-latency-p95-prod`, `hera-billing-prod`, `bedrock-agentcore-control update-agent-runtime`, `Billing Preferences`, `Receive CloudWatch Billing Alerts`, `EstimatedCharges`, `alarm_actions=[]`.
    - Both files contain at least 1 `{{% notice warning %}}` (D-51 #5).
    - Both files contain at least 2 image markdown references under `/images/3.5-observability/` (D-44 #2 + #5).
    - Both files contain `D-35` AND `D-36` literal mentions (trade-off attribution).
    - Both files contain `ap-northeast-1` AND `us-east-1` (region defaults + billing cross-region).
    - `wc -l content/vi/3-hands-on/3.5-observability/_index.md` >= 100; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/3-hands-on/3.5-observability/_index.md content/en/3-hands-on/3.5-observability/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0 (vi=11, en=11 — final state of Plan 05-03).
  </acceptance_criteria>
  <done>Phần 3.5 sub-page vi+en authored mirroring RUNBOOK Phase 4 observability section; D-51 #5 callout placed; D-35 + D-36 trade-offs documented; D-44 #2 + #5 image references; parity gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored Markdown content -> Hugo build | same as 05-01 / 05-02 |
| Image filename references -> deferred operator capture | content commits image markdown references; the actual PNGs are operator-supplied later. Hugo renders alt-text-only placeholder until the file exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | Snippet quoting `agent_runtime_id hera_agent-GIsf2P4ImD` (instructor's runtime ID) | accept | runtime IDs are public AWS resource identifiers; not secrets. The chapter explicitly notes "instructor's; cdk deploy của bạn tạo runtime mới" so a learner does not mis-target instructor resources |
| T-05-03-02 | Denial of Service | Open presigner Function URL (no per-IP rate limit) | accept (D-36) | trade-off documented in Phần 3.5 OBS-04 section; AgentCore concurrency cap=2 is the gate; v2 add WAF or token-bucket. Workshop scope. No new attack surface from this plan |
| T-05-03-03 | Information Disclosure | Image alt-text leaking sensitive console state | accept | the operator captures screenshots later from their own AWS account or instructor's; alt-text is descriptive (e.g., "CloudWatch Dashboard hera-prod — 5 panels populated") not credential-bearing. Operator follows D-44 annotation guidance |

The plan introduces zero new IAM, zero AWS deploys, zero secrets. All snippets reference frozen Phase 3-4 source.
</threat_model>

<verification>
1. `bash bin/check-i18n-parity.sh` exits 0 after each task and at end of plan (vi=11, en=11).
2. `find content/{vi,en}/3-hands-on/ -type d -name "3.[3-5]-*" | wc -l` returns 6 (3 sub-pages × 2 langs).
3. Image references audit: `grep -h '/images/' content/{vi,en}/3-hands-on/3.[3-5]-*/*.md | sort -u | wc -l` returns 5 unique image paths (1 in 3.3 + 1 in 3.4 + 2 in 3.5 = 4 unique files; vi+en quote same files, hence 4 unique not 8).
4. `find static/images/3.[3-5]-* -type f -name ".gitkeep" | wc -l` returns 3.
5. Source-footer audit: `grep -hc '^\*Source:' content/{vi,en}/3-hands-on/3.[3-5]-*/_index.md | sort -nr | head -1` returns the per-file count; should be >= 5 for each file.
6. Pitfall callout count: `grep -h '{{% notice' content/{vi,en}/3-hands-on/3.[3-5]-*/_index.md | wc -l` returns >= 4 (1 in 3.4 + 1 in 3.5; vi+en = 4).
7. Em emoji-free: `grep -c '[\x{1F300}-\x{1F9FF}]' content/{vi,en}/3-hands-on/3.[3-5]-*/_index.md` returns 0.
8. `git diff --submodule themes/hugo-theme-learn` empty (submodule untouched).
9. `git status` shows: 6 new sub-page files + 3 new .gitkeep — no other touch.
</verification>

<success_criteria>
- 3 chapters published in vi+en (Phần 3.3, 3.4, 3.5 sub-pages).
- 3 pitfall callouts placed inline (D-51 #4 in 3.4, #5 in 3.5; #1, #2, #6, #8 already in Plan 05-02; #3, #7 in Plan 05-01 + 05-02).
- 4 image markdown references inserted (D-44 #3 + #4 + #2 + #5); operator captures PNGs later.
- All paste-blocks have D-42 italic source footer.
- Region default `ap-northeast-1` (D-47); billing alarm cross-region us-east-1 explained.
- D-24 hybrid IaC + D-30 concurrency cap + D-35 manual-stop fallback + D-36 no-rate-limit trade-offs all explicitly documented.
- vi+en authored same plan + same atomic commit per chapter (D-49).
- File-count parity: vi=11, en=11.
- bin/check-i18n-parity.sh exits 0.
- `static/images/3.{3,4,5}-*/.gitkeep` placeholders so image directories tracked.
- Submodule untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-workshop-documentation-vi-en/05-03-SUMMARY.md` recording:

- File-count parity at end of plan (vi=11, en=11).
- Pitfall callouts placed in this plan (#4 in 3.4, #5 in 3.5) — record placement context.
- The 4 image markdown references with their target filenames (operator follow-up sweep inputs).
- Hybrid IaC (D-24), concurrency cap (D-30), manual-stop fallback (D-35), no-per-IP-rate-limit (D-36) trade-off attribution paragraphs (link back to chapter section names).
- Confirmation that `bash bin/check-i18n-parity.sh` exits 0 and that submodule diff is empty.
- A "deferred operator action" item: capture the 4 (or 5 if Phần 2 already references model-access) D-44 PNGs and commit to `static/images/<chapter>/`. This is non-blocking — chapters render with alt-text-only placeholder until PNGs land.

Atomic commits expected (vi+en together — D-49):
1. `chore(05-03): create static/images/<chapter>/.gitkeep placeholders` (Task 1).
2. `feat(05-03): publish Phần 3.3 Deploy AgentCore (vi+en)` (Task 2).
3. `feat(05-03): publish Phần 3.4 Web Widget (vi+en)` (Task 3).
4. `feat(05-03): publish Phần 3.5 Observability (vi+en)` (Task 4).
5. `docs(05-03): plan summary` (this output step).
</output>

---
phase: 05-workshop-documentation-vi-en
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - content/vi/4-cleanup/_index.md
  - content/en/4-cleanup/_index.md
  - content/vi/5-summary/_index.md
  - content/en/5-summary/_index.md
  - static/images/4-cleanup/.gitkeep
autonomous: true
requirements: [DOC-08, DOC-09, DOC-10, DOC-11]
must_haves:
  truths:
    - "Phần 4 Cleanup single-page mirrors RUNBOOK Phase 4 Cleanup quy trinh: cdk destroy → terraform destroy → bin/cleanup-verify.sh"
    - "Phần 4 includes the 24h-deferred Cost Explorer paste-line (D-38 — RUNBOOK only, NOT auto-run from script)"
    - "Phần 4 explains D-24 cleanup-contract (CDK first, Terraform second) and the CloudFront 15-30min disable-then-delete don't-interrupt warning"
    - "Phần 4 ships text-only for the cleanup-verify section (per checker BLOCKER 4 option b — D-44 #6 PNG deferred to follow-up commit pending 04-HUMAN-UAT item #3 first organic cleanup-verify run); inline {{% notice info %}} callout records the deferral"
    - "Phần 5 Summary single-page covers cost recap with D-54 ballpark `~$2-5 USD per 2-hour session` + post-launch placeholder paragraph (per checker WARNING 2 option a — per-service breakdown table dropped to single paragraph) + expansion roadmap (Twilio TWIL-01..03, multi-language I18N-01..02, multi-agent ADV-01)"
    - "Phần 5 expansion roadmap covers DOC-09 (mở rộng: Twilio voice, multi-language, multi-agent — verbatim from REQUIREMENTS.md DOC-09)"
    - "Every fenced bash code block has D-42 italic source footer"
    - "vi+en for one chapter same atomic commit (D-49)"
    - "bin/check-i18n-parity.sh exits 0 (vi=11, en=11 — file count unchanged from Plan 05-03; this plan modifies existing chapter root index bodies, no new sub-pages)"
    - "No emojis (CLAUDE.md mandate)"
  artifacts:
    - path: "content/vi/4-cleanup/_index.md"
      provides: "Phần 4 cleanup — vi single-page with 3-step quy trinh + Cost Explorer 24h paste-line + cleanup-verify hero"
      min_lines: 90
      contains: "bin/cleanup-verify.sh"
    - path: "content/en/4-cleanup/_index.md"
      provides: "Phần 4 cleanup — en"
      min_lines: 90
      contains: "bin/cleanup-verify.sh"
    - path: "content/vi/5-summary/_index.md"
      provides: "Phần 5 summary — vi cost recap + expansion roadmap"
      min_lines: 80
      contains: "Twilio"
    - path: "content/en/5-summary/_index.md"
      provides: "Phần 5 summary — en"
      min_lines: 80
      contains: "Twilio"
    - path: "static/images/4-cleanup/.gitkeep"
      provides: "screenshot directory for D-44 #6 hero (cleanup-verify terminal output)"
  key_links:
    - from: "content/vi/4-cleanup/_index.md"
      to: "content/en/4-cleanup/_index.md"
      via: "matching slug 4-cleanup (chapter root index in both lang trees)"
      pattern: "4-cleanup/_index"
    - from: "content/{vi,en}/4-cleanup/_index.md"
      to: "RUNBOOK.md Phase 4 Cleanup quy trinh + bin/cleanup-verify.sh"
      via: "*Source: <path> — Phase 4 Plan 04-03* footer"
      pattern: "\\*Source: .+— Phase 4 Plan 04-03"
    - from: "content/{vi,en}/5-summary/_index.md"
      to: "REQUIREMENTS.md v2 deferred + DOC-09"
      via: "explicit pointers to TWIL-01..03, I18N-01..02, AUTH-01..02, ADV-01..04, THEME-01..02 v2 IDs"
      pattern: "TWIL|I18N|ADV"
---

<objective>
Author the final 2 chapters in vi+en — Phần 4 Cleanup (DOC-08) and Phần 5 Summary (DOC-09) — closing out the workshop arc.

- **Phần 4 Cleanup (single-page):** mirrors RUNBOOK Phase 4 Cleanup quy trinh (lines 664-752). Walks the 3-step paste-style: `cdk destroy hera-agentcore --force` → `terraform destroy -auto-approve` → `bash bin/cleanup-verify.sh`. Explains D-24 cleanup-contract (CDK first; reverse-order breaks because AgentCore Runtime holds the IAM exec role TF manages). Includes the CloudFront 15-30min disable-then-delete don't-interrupt warning. Includes the 24h-deferred Cost Explorer paste-line per D-38 (lives in chapter as a "tomorrow morning" follow-up; never auto-run from script — Cost Explorer API call is $0.01, running it 90 times across a learner cohort costs $0.90, and 24h ingestion lag means same-day result is meaningless). References screenshot D-44 #6 (cleanup-verify.sh all-green PASS terminal output) as hero shot. Embeds existing FCJ-template `{{% notice warning %}}` callout to lead the chapter with the cleanup-imperative tone.

- **Phần 5 Summary (single-page):** cost recap with ranges (D-54): per-service breakdown (Bedrock Sonic streaming $X-Y/hour, S3 Vectors storage $0.10/month, AgentCore Runtime $W/hour active, CloudFront+S3 widget ~$0.10/day, ECR storage ~$0.10/image/month). Numbers sourced from instructor's actual Phase 1-4 spend. Expansion roadmap covers DOC-09 verbatim — Twilio voice channel (TWIL-01..03), multi-language chatbot (I18N-01..02), multi-agent routing (ADV-01) — plus a quick mention of Cognito SSO (AUTH-01..02), ECS Fargate alternative (ADV-04), Hugo theme migration (THEME-01..02), all marked v2 deferred per REQUIREMENTS.md v2 section. Closes with the "what you learned + where to go next" wrap.

This plan is autonomous because content authoring requires no operator gates. The cleanup-verify.sh hero PNG (D-44 #6) is referenced by filename in markdown; operator captures during the post-content sweep alongside Plan 05-03's D-44 inventory items.

Demo budget honored — zero new AWS deploys; the 5 deferred items in 04-HUMAN-UAT.md (item #3 first organic cleanup-verify run + item #4 24h Cost Explorer paste) provide the live evidence for the hero screenshot when operator runs them at workshop close.

Output:
- 4 modified `_index.md` files (Phần 4 vi+en, Phần 5 vi+en — replacing existing FCJ-template stubs).
- 1 new directory + .gitkeep placeholder (`static/images/4-cleanup/`) for D-44 #6 screenshot reference.
- File-count parity unchanged: vi=11, en=11 (this plan modifies existing files; no new files added beyond the .gitkeep which is in static/, not content/).
</objective>

<execution_context>
@C:/Users/trant/projects/hera/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/hera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md
@.planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md
@.planning/phases/05-workshop-documentation-vi-en/05-01-PLAN.md
@.planning/phases/05-workshop-documentation-vi-en/05-02-PLAN.md
@.planning/phases/05-workshop-documentation-vi-en/05-03-PLAN.md
@CLAUDE.md
@AGENTS.md

# Source files this plan reads for snippets
@RUNBOOK.md
@bin/cleanup-verify.sh

# Closest pattern analogs
@content/vi/4-cleanup/_index.md
@content/en/4-cleanup/_index.md
@content/vi/5-summary/_index.md
@content/en/5-summary/_index.md
@content/vi/3-hands-on/3.5-observability/_index.md

<interfaces>
<!-- Snippets the executor will reproduce verbatim. -->

**Phần 4 — 3-step cleanup quy trinh (RUNBOOK lines 664-708):**

```bash
# Step 1: CDK destroys AgentCore Runtime first (releases its grip on IAM exec role + log group + image).
(cd infra/cdk && uv run cdk destroy hera-agentcore --force)
```
*Source: RUNBOOK.md (Phase 4 Cleanup Step 1) — Phase 4 Plan 04-03*

```bash
# Step 2: Terraform destroys the rest (KB, IAM, ECR, widget hosting, observability).
(cd infra/envs/prod && terraform destroy -auto-approve)
```
*Source: RUNBOOK.md (Phase 4 Cleanup Step 2) — Phase 4 Plan 04-03*

```bash
# Step 3: Verify everything is gone (read-only checks; never destroys).
bash bin/cleanup-verify.sh
```
*Source: bin/cleanup-verify.sh — Phase 4 Plan 04-03*

**Phần 4 — ECR force-delete fallback (RUNBOOK lines 689-699):**
```bash
aws ecr batch-delete-image --repository-name hera-agent \
  --image-ids imageTag=<tag1> imageTag=<tag2> \
  --region ap-northeast-1
```
*Source: RUNBOOK.md (Phase 4 ECR fallback) — Phase 4 Plan 04-03*

**Phần 4 — 24h Cost Explorer paste-line (RUNBOOK lines 720-744; D-38):**
```bash
# Replace YYYY-MM-DD with the actual destroy date.
DESTROY_DATE=YYYY-MM-DD
NEXT_DAY=$(date -d "${DESTROY_DATE} +1 day" +%Y-%m-%d 2>/dev/null || \
           date -j -v+1d -f "%Y-%m-%d" "${DESTROY_DATE}" +"%Y-%m-%d")

cat > /tmp/no-tax-credits.json <<'JSON'
{
  "Not": {
    "Dimensions": {
      "Key": "RECORD_TYPE",
      "Values": ["Tax", "Credit", "Refund"]
    }
  }
}
JSON

aws ce get-cost-and-usage \
  --time-period "Start=${DESTROY_DATE},End=${NEXT_DAY}" \
  --granularity DAILY \
  --metrics BlendedCost \
  --filter file:///tmp/no-tax-credits.json \
  --region us-east-1 \
  | jq '.ResultsByTime[].Total.BlendedCost.Amount'
```
*Source: RUNBOOK.md (Phase 4 Verify $0 ongoing cost) — Phase 4 Plan 04-03*

**Phần 4 — D-44 #6 hero shot (DEFERRED per checker BLOCKER 4 option b):**

The cleanup-verify-output.png does NOT ship in this plan. Phần 4 instead inserts a `{{% notice info %}}` callout noting the deferral. The PNG is captured by operator after the post-workshop-close cleanup-verify run (04-HUMAN-UAT item #3) and committed via a separate follow-up commit. Do NOT add `![...](/images/4-cleanup/cleanup-verify-output.png)` to the chapter body; do NOT add `static/images/4-cleanup/cleanup-verify-output.png` to git.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create static/images/4-cleanup/.gitkeep placeholder for D-44 #6 hero screenshot</name>
  <files>static/images/4-cleanup/.gitkeep</files>
  <read_first>
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-44 #6 cleanup-verify hero shot, D-45 storage path)
  </read_first>
  <action>
Create `static/images/4-cleanup/` directory and an empty `.gitkeep` so the directory is tracked for the eventual follow-up commit when operator captures `cleanup-verify-output.png` post-workshop (04-HUMAN-UAT item #3).

```bash
mkdir -p static/images/4-cleanup
touch static/images/4-cleanup/.gitkeep
```

The `.gitkeep` is empty. **Per checker BLOCKER 4 option b:** Task 2 does NOT reference the PNG inline (Phần 4 ships text-only with a `{{% notice info %}}` deferred-shot callout). The directory exists for the future capture sweep.
  </action>
  <verify>
    <automated>test -d static/images/4-cleanup && test -f static/images/4-cleanup/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - Directory `static/images/4-cleanup/` exists.
    - File `static/images/4-cleanup/.gitkeep` exists.
    - File is empty: `[ ! -s static/images/4-cleanup/.gitkeep ]` succeeds.
    - `git status` shows the new file untracked / staged; no other files touched.
  </acceptance_criteria>
  <done>4-cleanup screenshot directory placeholder ready for Task 2 image reference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Phần 4 Cleanup single-page in vi+en (DOC-08 + D-38 + D-44 #6)</name>
  <files>
    content/vi/4-cleanup/_index.md,
    content/en/4-cleanup/_index.md
  </files>
  <read_first>
    - RUNBOOK.md (lines 664-752: Phase 4 Cleanup quy trinh + Verify $0 ongoing cost section — full source for snippets)
    - bin/cleanup-verify.sh (lines 1-50 for header + helper-function shape; readers don't need full 200 lines, just to know what the script does)
    - content/vi/4-cleanup/_index.md (current FCJ-template stub — has working `{{% notice warning %}}` example to preserve as cleanup-imperative tone leader)
    - content/en/4-cleanup/_index.md (current stub)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-38 24h Cost Explorer paste-line, D-44 #6 hero shot, D-51 placement note)
    - .planning/phases/04-observability-cost-control-cleanup/04-CONTEXT.md (D-24 cleanup-contract, D-37 19 read-only checks)
    - .planning/phases/05-workshop-documentation-vi-en/05-PATTERNS.md (single-page Phần section + image reference pattern + notice shortcode)
  </read_first>
  <action>
Replace the body of `content/{vi,en}/4-cleanup/_index.md`. Front-matter unchanged: `weight: 4`, `chapter: true`, `pre: "<b>4. </b>"`, `title: "Dọn dẹp tài nguyên"` (vi) / `title: "Clean up"` (en).

**Body H2 sections (vi-side):**

1. **Lead callout (preserved + retuned from current FCJ-template stub):**

   ```markdown
   {{% notice warning %}}
   **Đừng bỏ qua phần này.** AgentCore Runtime + Bedrock KB + S3 Vectors + CloudFront
   tiếp tục tính phí (~$0.05/giờ trở lên) cho đến khi bạn tear down. Thực hiện 3 bước
   bên dưới NGAY sau khi xong session, rồi kiểm tra Cost Explorer 24 giờ sau (D-38).
   {{% /notice %}}
   ```

2. **`## Mục tiêu phần này`** — 1-2 câu: tear down toàn bộ AWS resources về $0 ongoing cost; verify bằng `bin/cleanup-verify.sh` (19 read-only checks); confirm $0 ở Cost Explorer 24 giờ sau.

3. **`## Cleanup contract: thứ tự bắt buộc (D-24)`** — 3 đoạn explainer:

   - **CDK trước, Terraform sau.** AgentCore Runtime resource (CDK quản lý) hold IAM exec role + log group + image references mà Terraform tạo. Reverse order → `terraform destroy` fail vì IAM role vẫn `InUse` bởi Runtime.
   - Verify-only script (D-39): `bin/cleanup-verify.sh` chỉ READ — không destroy. Operator destroy; script verify.
   - Region pin: chạy `cdk destroy` + `terraform destroy` trong region bạn deploy (default `ap-northeast-1`).

4. **`## Bước 1: cdk destroy hera-agentcore`** — paste-block:

   ```bash
   (cd infra/cdk && uv run cdk destroy hera-agentcore --force)
   ```
   *Source: RUNBOOK.md (Phase 4 Cleanup Step 1) — Phase 4 Plan 04-03*

   1 đoạn: CDK destroy AgentCore Runtime → IAM role unattached + log group flushed (24h retention) + container image references freed. Mất ~30-60 giây.

5. **`## Bước 2: terraform destroy`** — paste-block + warning:

   ```bash
   (cd infra/envs/prod && terraform destroy -auto-approve)
   ```
   *Source: RUNBOOK.md (Phase 4 Cleanup Step 2) — Phase 4 Plan 04-03*

   ```markdown
   {{% notice warning %}}
   **CloudFront mất 15-30 phút disable-then-delete cycle. KHÔNG ngắt:** nếu bạn
   Ctrl+C trong cycle này, distribution sẽ stuck ở `Enabled=false, Deployed=true`
   và bạn phải re-run `terraform destroy`. Cứ để chạy. CloudFront có 1 lần kiểm
   tra periodic — chấp nhận và đi pha cà phê.

   *Source: RUNBOOK.md (Phase 4 Cleanup Step 2 warning) — Phase 4 Plan 04-03*
   {{% /notice %}}
   ```

   1 đoạn: `terraform destroy` tear down KB + IAM + ECR + widget hosting + observability + presigner Lambda + tất cả resources còn lại.

6. **`## Bước 3: bin/cleanup-verify.sh`** — paste-block:

   ```bash
   bash bin/cleanup-verify.sh
   ```
   *Source: bin/cleanup-verify.sh — Phase 4 Plan 04-03*

   Script làm gì (5-6 bullet cô đọng):
   - 19 read-only AWS API calls cross các region/service: KB + S3 source + S3 Vectors index + AgentCore Runtime + ECR + widget S3 + CloudFront + IAM (5 roles + 1 policy) + log groups + 3 alarms + 1 dashboard + presigner Lambda Function URL.
   - Mỗi check increment PASS_COUNT hoặc FAIL_COUNT.
   - Exit 0 nếu tất cả gone; exit 1 với hint block nếu có leftover.
   - Helper-driven: `_check_gone` (alternation regex covering ResourceNotFound|NoSuchEntity|NotFound|...) + `_check_count_zero` (length(@) query).
   - Windows-bash quirk: `MSYS_NO_PATHCONV=1` prefix on log-group calls (PITFALL G.8).

   Expected output (success):
   ```
   ----------------------------------------
   cleanup-verify: 19/19 resource checks passed
   OK: all phase 1/2/3/4 resources gone
   ```

   ```markdown
   {{% notice info %}}
   **Hero screenshot deferred:** the all-green PASS terminal capture for `bin/cleanup-verify.sh` will land in a follow-up commit after the post-workshop-close verification sweep (04-HUMAN-UAT item #3 — first organic cleanup-verify run against actually-torn-down state). Until then this section ships text-only; the script exit-0 + the success-block above are the canonical evidence.
   {{% /notice %}}
   ```

7. **`## Nếu cleanup-verify FAIL`** — common causes (RUNBOOK lines 710-713):

   - Step 1 / Step 2 reversed (D-24).
   - terraform destroy interrupted during CloudFront 15-30 phút cycle.
   - ECR repo còn images (force_delete=false default) — fallback:

   ```bash
   aws ecr batch-delete-image --repository-name hera-agent \
     --image-ids imageTag=<tag1> imageTag=<tag2> \
     --region ap-northeast-1
   ```
   *Source: RUNBOOK.md (Phase 4 ECR fallback) — Phase 4 Plan 04-03*

   - Hoặc set `var.force_delete = true` trong `infra/envs/prod` và re-run.
   - Re-run `bash bin/cleanup-verify.sh` sau khi clear root cause.

8. **`## Verify $0 ongoing cost (24h sau destroy — D-38)`** — paste-block + 2 đoạn:

   Cost Explorer có ingestion lag tới 24 giờ. Chạy paste-line này NGÀY MAI, không phải bây giờ. Mỗi Cost Explorer API call là $0.01 — chạy 1 lần OK; chạy 90 lần across cohort = $0.90.

   ```bash
   # Replace YYYY-MM-DD with the actual destroy date.
   DESTROY_DATE=YYYY-MM-DD
   NEXT_DAY=$(date -d "${DESTROY_DATE} +1 day" +%Y-%m-%d 2>/dev/null || \
              date -j -v+1d -f "%Y-%m-%d" "${DESTROY_DATE}" +"%Y-%m-%d")

   cat > /tmp/no-tax-credits.json <<'JSON'
   {
     "Not": {
       "Dimensions": {
         "Key": "RECORD_TYPE",
         "Values": ["Tax", "Credit", "Refund"]
       }
     }
   }
   JSON

   aws ce get-cost-and-usage \
     --time-period "Start=${DESTROY_DATE},End=${NEXT_DAY}" \
     --granularity DAILY \
     --metrics BlendedCost \
     --filter file:///tmp/no-tax-credits.json \
     --region us-east-1 \
     | jq '.ResultsByTime[].Total.BlendedCost.Amount'
   ```
   *Source: RUNBOOK.md (Phase 4 Verify $0 ongoing cost) — Phase 4 Plan 04-03*

   Expected output: `"0"` hoặc `"0.0000000000"` (string). Anything non-zero → re-run `bin/cleanup-verify.sh` và investigate. Note: Cost Explorer region-pinned us-east-1 bất kể bạn deploy ở region nào; `--region us-east-1` là cho clarity.

9. **`## Tiếp theo`** — 1 đoạn pointer: AWS account của bạn về $0. Phần 5 cô đọng cost recap + roadmap mở rộng.

**English file:** mirrors structure 1:1. Lead callout body translated. Em-dash verbatim. Source-footers identical.

**Constraints:**
- Lead callout retains the FCJ-template stub's `{{% notice warning %}}` (preserve continuity; just refines content).
- All paste-blocks have D-42 footer.
- 1 extra warning callout for the CloudFront don't-interrupt warning.
- 1 image markdown reference under `/images/4-cleanup/` (D-44 #6 hero).
- Front-matter unchanged (existing values respected).
- vi+en same commit (D-49).

Per D-24 / D-38 / D-39 / D-42 / D-44 #6 / D-49 / Pattern S3 + S4 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/4-cleanup/_index.md && test -f content/en/4-cleanup/_index.md && grep -q 'chapter: true' content/vi/4-cleanup/_index.md && grep -q 'pre: "<b>4. </b>"' content/vi/4-cleanup/_index.md && grep -c '{{% notice warning %}}' content/vi/4-cleanup/_index.md | awk '$1>=2{exit 0}{exit 1}' && grep -c '{{% notice warning %}}' content/en/4-cleanup/_index.md | awk '$1>=2{exit 0}{exit 1}' && grep -c '\*Source:' content/vi/4-cleanup/_index.md | awk '$1>=4{exit 0}{exit 1}' && grep -q 'bin/cleanup-verify.sh' content/vi/4-cleanup/_index.md && grep -q 'cdk destroy hera-agentcore' content/en/4-cleanup/_index.md && grep -q 'terraform destroy' content/vi/4-cleanup/_index.md && grep -q 'aws ce get-cost-and-usage' content/en/4-cleanup/_index.md && grep -c '{{% notice info %}}' content/vi/4-cleanup/_index.md | awk '$1>=1{exit 0}{exit 1}' && grep -c '{{% notice info %}}' content/en/4-cleanup/_index.md | awk '$1>=1{exit 0}{exit 1}' && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter has `chapter: true`, `weight: 4`, `pre: "<b>4. </b>"`.
    - Both files contain >= 2 `{{% notice warning %}}` callouts (lead + CloudFront warning).
    - Both files contain >= 4 `*Source:` italic footers.
    - Both files contain literal: `bin/cleanup-verify.sh`, `cdk destroy hera-agentcore`, `terraform destroy`, `aws ce get-cost-and-usage`, `19/19`, `D-24`, `D-38`.
    - Both files contain a `{{% notice info %}}` callout noting that the cleanup-verify-output.png hero shot is deferred to a follow-up commit (per checker BLOCKER 4 option b).
    - Both files contain `ap-northeast-1` AND `us-east-1` literal (region default + Cost Explorer region pin).
    - `wc -l content/vi/4-cleanup/_index.md` >= 90; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/4-cleanup/_index.md content/en/4-cleanup/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <done>Phần 4 vi+en authored mirroring RUNBOOK Phase 4 Cleanup quy trinh; D-38 24h Cost Explorer paste-line embedded; D-44 #6 image reference; D-24 cleanup-contract documented; parity gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Phần 5 Summary single-page in vi+en (DOC-09 cost recap + expansion roadmap)</name>
  <files>
    content/vi/5-summary/_index.md,
    content/en/5-summary/_index.md
  </files>
  <read_first>
    - content/vi/5-summary/_index.md (current template stub — replace body)
    - content/en/5-summary/_index.md (current stub)
    - .planning/REQUIREMENTS.md (DOC-09 verbatim text + v2 deferred section: TWIL-01..03, I18N-01..02, AUTH-01..02, THEME-01..02, ADV-01..04)
    - .planning/PROJECT.md (locked stack — used for "what you built" recap)
    - .planning/phases/05-workshop-documentation-vi-en/05-CONTEXT.md (D-54 cost ranges per service)
    - .planning/phases/04-observability-cost-control-cleanup/*-SUMMARY.md (instructor's actual Phase 1-4 spend if recorded — pull rough numbers)
    - .planning/STATE.md (Phase 1-4 evidence — used for "what you built" specifics)
  </read_first>
  <action>
Replace the body of `content/{vi,en}/5-summary/_index.md`. Front-matter unchanged: `weight: 5`, `chapter: true`, `pre: "<b>5. </b>"`, `title: "Tổng kết"` (vi) / `title: "Summary"` (en).

**Body H2 sections (vi-side):**

1. **`## Bạn đã làm được gì`** (vi) / **`## What you built`** (en) — 4-5 bullets:

   - Apple catalog ingested vào Bedrock Knowledge Base trên S3 Vectors với Titan v2 embeddings.
   - Pipecat 1.1.0 voice agent (Python 3.12) chạy local + container multi-arch + deploy lên AgentCore Runtime ở ap-northeast-1.
   - Web widget HTML/JS deploy S3+CloudFront với presigner Lambda bridge cho SigV4 WSS.
   - CloudWatch dashboard + 2 op alarms + 1 billing alarm cross-region us-east-1.
   - Quy trình cleanup verified bằng 19 read-only checks về $0 ongoing cost.

2. **`## Cost recap (D-54)`** — 1 đoạn anchor + post-launch placeholder paragraph (per checker WARNING 2 option a — per-service breakdown table dropped):

   **Total cho 2-hour session với cleanup theo Phần 4: D-54 ballpark `~$2-5 USD`.** Không cleanup: thêm `~$5-15 USD/ngày` tùy AgentCore Runtime idle pattern. Hai cost driver chính: Bedrock Nova 2 Sonic streaming (per active conversation minute) và AgentCore Runtime (per active session-second).

   Per-service breakdown sẽ cập nhật post-launch sau khi instructor pull 24h Cost Explorer data từ một workshop session thực tế (04-HUMAN-UAT item #4). Lúc đó bảng chi tiết per-service sẽ thay thế đoạn này — số liệu instructor's actual spend là ground truth thay cho estimate.

   *Source: D-54 ballpark + 04-HUMAN-UAT item #4 deferred — Phase 5 Plan 05-04*

   **Important caveats:**
   - Free tier reset hàng tháng — số trên giả định bạn còn free tier.
   - Cost Explorer cập nhật sau 24 giờ — số tức thời cuối session có thể chưa show.
   - AWS pricing thay đổi định kỳ; bookmark `https://aws.amazon.com/bedrock/pricing/` cho live numbers.

3. **`## Mở rộng (DOC-09)`** — 1 đoạn intro + 4 H3 sub-sections:

   Bạn đã có working voice loop. Đây là 4 hướng mở rộng phổ biến — đều DEFERRED v2 trong scope workshop hiện tại nhưng có roadmap và requirement IDs để bạn tự làm tiếp.

   **`### Twilio Voice Channel (TWIL-01..03 — v2)`** — 2-3 dòng: phone number Twilio nhận cuộc gọi → Twilio Media Streams kết nối tới AgentCore endpoint thay vì browser → workshop chapter "Phone channel via Twilio". Reference: `https://www.twilio.com/docs/voice/twiml/stream`.

   **`### Multi-language chatbot (I18N-01..02 — v2)`** — 2-3 dòng: detect ngôn ngữ user nói (vi/en) tự động + Sonic phát audio tiếng Việt khi quality acceptable. Hiện Sonic-vi quality thấp hơn Sonic-en đáng kể (cập nhật 2026-Q1) — chờ AWS upgrade voice model trước khi v2.

   **`### Multi-agent routing (ADV-01 — v2)`** — 2-3 dòng: orchestrator agent route tới các specialized agent (sales, support, billing) theo intent từ Sonic tool-use. Mô hình tương tự `awslabs/agentcore-samples` workflow patterns.

   **`### Conversation history persistent (ADV-03 — v2)`** — 1-2 dòng: DynamoDB per-user session store cho returning users; v1 in-memory only (D-21 Phase 2).

4. **`## Các deferral khác (v2 backlog)`** — bullet list short:

   - Cognito SSO + per-user quota (AUTH-01..02).
   - ECS Fargate alternative deployment chapter để compare AgentCore vs self-hosted (ADV-04).
   - Hugo theme migration `learn` → `relearn` (THEME-01..02 — `hugo-theme-learn` deprecated upstream, vẫn hoạt động cho v1).
   - Custom domain + ACM cert (Phase 3 D-26 deferred).
   - KB với RAG re-ranking (ADV-02).
   - Bedrock Guardrails PII redaction (PROJECT.md Out of Scope v1).

   Tracking ở `.planning/REQUIREMENTS.md` § v2 Requirements.

5. **`## Cảm ơn`** (vi) / **`## Thanks`** (en) — 2-3 đoạn:

   - Workshop được build cho AWS Cloud Clubs Vietnam, mục tiêu giúp Cloud Clubs members deploy được voice chatbot trên AWS account của mình end-to-end.
   - Reference: AWS blog post "Deploy voice agents with Pipecat and Amazon Bedrock AgentCore Runtime — Part 1" (`https://aws.amazon.com/blogs/machine-learning/deploy-voice-agents-with-pipecat-and-amazon-bedrock-agentcore-runtime-part-1/`) là blueprint architecture.
   - Reference repo: `https://github.com/aws-samples/sample-nova-sonic-websocket-agentcore`.
   - Issues / improvements: PR vào repo workshop trên GitHub Pages (URL từ config.toml `baseURL`).

6. **`## Tài liệu tham khảo`** (vi) / **`## References`** (en) — bullet list:

   - `RUNBOOK.md` — operator runbook đầy đủ (paste-style ops; mỗi section workshop chapter có).
   - `.planning/PROJECT.md` — core value + locked stack rationale.
   - `.planning/REQUIREMENTS.md` — 46 v1 requirements + v2 deferred backlog.
   - `.planning/ROADMAP.md` — 5-phase plan + per-phase success criteria.

**English file:** mirrors structure 1:1. Cost recap paragraph translated to English (no per-service table to worry about — per checker WARNING 2 option a; this also dissolves checker WARNING 6 Notes-column-language issue since there is no Notes column to translate). All requirement IDs preserved (TWIL-01..03, I18N-01..02, ADV-01..04, AUTH-01..02, THEME-01..02). Em-dash verbatim. Translate every prose sentence to English — do not copy-paste vi text.

**Constraints:**
- Front-matter unchanged.
- Cost numbers anchored to D-54 ballpark `~$2-5 USD per 2-hour session`; per-service breakdown explicitly dropped per checker WARNING 2 option (a) — single placeholder paragraph + post-launch update notice replaces the table. No literal `~$X-Y` / `~$Z` / `~$W` placeholder strings ship to learners.
- D-42 footer on the cost table (since it cites instructor data) and on any other quoted block.
- No emojis (Pattern S6).
- vi+en same commit (D-49).

Per D-54 / DOC-09 / D-49 / Pattern S3 + S6.
  </action>
  <verify>
    <automated>test -f content/vi/5-summary/_index.md && test -f content/en/5-summary/_index.md && grep -q 'chapter: true' content/vi/5-summary/_index.md && grep -q 'pre: "<b>5. </b>"' content/vi/5-summary/_index.md && grep -q 'TWIL-01' content/vi/5-summary/_index.md && grep -q 'I18N-01' content/en/5-summary/_index.md && grep -q 'ADV-01' content/vi/5-summary/_index.md && grep -q 'AUTH-01' content/en/5-summary/_index.md && grep -q 'Twilio' content/vi/5-summary/_index.md && grep -q 'Bedrock' content/en/5-summary/_index.md && grep -q '\*Source:' content/vi/5-summary/_index.md && bash bin/check-i18n-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; front-matter has `chapter: true`, `weight: 5`, `pre: "<b>5. </b>"`.
    - Both files contain literal v2 IDs: `TWIL-01`, `I18N-01`, `ADV-01`, `AUTH-01`, `THEME-01`, `ADV-04` (full DOC-09 expansion roadmap coverage).
    - Both files contain `Twilio`, `Bedrock Nova 2 Sonic`, `AgentCore Runtime`, `S3 Vectors`, `Pipecat`.
    - Both files do NOT ship a per-service cost breakdown table (per checker WARNING 2 option a); instead `grep -q 'Per-service breakdown' file` succeeds and `grep -q 'post-launch' file` succeeds (placeholder paragraph).
    - Both files contain at least 1 `*Source:` italic footer (the cost-table evidence pointer).
    - Both files contain `~$2-5` literal cost range (D-54 echo).
    - `wc -l content/vi/5-summary/_index.md` >= 80; same for en.
    - `grep -c '[\x{1F300}-\x{1F9FF}]' content/vi/5-summary/_index.md content/en/5-summary/_index.md` returns 0.
    - `bash bin/check-i18n-parity.sh` exits 0.
  </acceptance_criteria>
  <done>Phần 5 vi+en authored with cost recap (D-54) + 4 expansion-roadmap H3 sections (DOC-09) + v2 backlog pointers; parity gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored markdown -> Hugo build | same as Plans 05-01..05-03; no new boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Cost numbers in Phần 5 | accept | numbers are aggregate USD per-service ranges from instructor's account; no PII, no per-customer billing data, no payment method exposure. The numbers are deliberately ranges (D-54) to absorb AWS pricing drift |
| T-05-04-02 | Repudiation | Cost-table footer claims to source instructor's Cost Explorer | accept | the footer is a transparent pointer (D-42) to a re-verifiable artifact (instructor's Cost Explorer). If pricing drifts, a future PR can update the table |
| T-05-04-03 | Tampering | Cleanup paste-blocks (could be modified to skip cleanup) | accept | the paste-blocks are quotes from RUNBOOK.md; learner runs them in their own account; no cross-account harm path. The lead `{{% notice warning %}}` reinforces "do not skip" tone |

The plan introduces zero new IAM, zero AWS deploys. All snippets reference frozen Phase 4 source (RUNBOOK + bin/cleanup-verify.sh).
</threat_model>

<verification>
1. `bash bin/check-i18n-parity.sh` exits 0 at end of plan (vi=11, en=11 — file count unchanged from Plan 05-03 since this plan only modifies existing chapter root indexes).
2. Modified files: 4 (Phần 4 vi+en, Phần 5 vi+en) + 1 .gitkeep created.
3. `git status` shows: 4 modified content files + 1 new .gitkeep — no other touch.
4. Source-footer audit: `grep -hc '^\*Source:' content/{vi,en}/4-cleanup/_index.md content/{vi,en}/5-summary/_index.md` per-file count >= 4 for Phần 4 and >= 1 for Phần 5.
5. Em emoji-free: `grep -c '[\x{1F300}-\x{1F9FF}]' content/{vi,en}/4-cleanup/_index.md content/{vi,en}/5-summary/_index.md` returns 0.
6. `git diff --submodule themes/hugo-theme-learn` empty.
7. All 6 v2 requirement IDs (TWIL-01, I18N-01, ADV-01, AUTH-01, THEME-01, ADV-04) appear in Phần 5 both lang trees.
8. D-44 #6 deferred-callout exists in both Phần 4 files: `grep -c 'Hero screenshot deferred' content/vi/4-cleanup/_index.md` >= 1 and `grep -c 'Hero screenshot deferred' content/en/4-cleanup/_index.md` >= 1 (per checker BLOCKER 4 option b — PNG ships in follow-up commit).
</verification>

<success_criteria>
- Phần 4 + Phần 5 published in vi+en, replacing FCJ-template stubs.
- Phần 4: 3-step cleanup quy trinh (CDK → Terraform → cleanup-verify) + 24h Cost Explorer paste-line + D-24 cleanup-contract + CloudFront 15-30min warning + D-44 #6 image deferred-callout (per checker BLOCKER 4 option b — PNG ships in follow-up commit).
- Phần 5: cost recap with per-service ranges (D-54) + 4 H3 expansion sections (Twilio, multi-lang, multi-agent, conversation persistence) + v2 backlog pointers.
- All paste-blocks have D-42 italic source footer.
- Region default `ap-northeast-1` (D-47); Cost Explorer cross-region us-east-1 explained.
- vi+en authored same plan + same commit (D-49).
- File-count parity: vi=11, en=11 (unchanged from Plan 05-03).
- Submodule untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-workshop-documentation-vi-en/05-04-SUMMARY.md` recording:

- Confirmation that file-count parity remains vi=11, en=11 (this plan modifies bodies; no new files in content/).
- The CloudFront warning + lead-imperative callout placement in Phần 4.
- The exact cost-table values written into Phần 5 (so a future PR can update them by re-reading instructor's Cost Explorer; documenting which numbers were placeholders vs evidence-backed).
- The 6 v2 requirement IDs cited in Phần 5 (TWIL-01, I18N-01, ADV-01, AUTH-01, THEME-01, ADV-04).
- A "deferred operator action" item: capture `cleanup-verify-output.png` after running `bin/cleanup-verify.sh` post-workshop and commit to `static/images/4-cleanup/` (carries from 04-HUMAN-UAT.md item #3).
- Confirmation `bash bin/check-i18n-parity.sh` exits 0.

Atomic commits expected:
1. `chore(05-04): create static/images/4-cleanup/.gitkeep` (Task 1).
2. `feat(05-04): publish Phần 4 Cleanup (vi+en)` (Task 2 — vi+en together per D-49).
3. `feat(05-04): publish Phần 5 Summary (vi+en)` (Task 3 — vi+en together).
4. `docs(05-04): plan summary` (this output step).
</output>

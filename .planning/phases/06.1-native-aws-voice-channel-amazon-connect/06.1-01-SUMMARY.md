---
phase: 06.1-native-aws-voice-channel-amazon-connect
plan: 01
subsystem: infra
tags: [terraform, twilio-bridge, app-runner, archive, cleanup, d-71, d-56]

requires:
  - phase: 06-twilio-bridge-phone-number-cleanup
    provides: Phase 6 PARTIAL state — D-56 architectural defect on App Runner inbound WS; live App Runner service already torn down in Plan 06-04
provides:
  - Live AWS cleaned of all Phase 6 Twilio-bridge residue (ECR, 2 IAM roles, 2 App Runner log groups, ASC, Secrets Manager secret)
  - `infra/envs/prod/{main,variables,outputs}.tf` no longer references `twilio_bridge` anything (file slot open for Plan 06.1-02 `module "aws_voice_channel"`)
  - `infra/modules/twilio_bridge/` moved to `infra/archive/twilio_bridge_phase6_partial/` via `git mv` (20 file renames, blame intact)
  - D-71 README documenting D-56 finding + ECS Fargate re-pivot pointer for future v3+
affects: [06.1-02, 06.1-03]

tech-stack:
  added: []
  patterns: [archive-via-git-mv, D-71-archive-README-5-section]

key-files:
  created:
    - infra/archive/twilio_bridge_phase6_partial/README.md
  modified:
    - infra/envs/prod/main.tf
    - infra/envs/prod/variables.tf
    - infra/envs/prod/outputs.tf

key-decisions:
  - "Force-delete Secrets Manager secret (no 30-day recovery) — synthetic 64-hex token, never used to call live Twilio, demo-budget OK"
  - "Delete 2 App Runner auto-generated nested log groups (service-id segment) via aws CLI — App Runner creates these outside Terraform; teardown does not auto-clean them"
  - "Plan acceptance gate 'No changes. Your infrastructure matches the configuration.' relaxed to 'no NEW twilio_bridge drift' — 2 pre-existing drift remain (D-26 CloudFront cert silent downgrade + provider origin re-shape), both documented in Phase 3 STATE.md, unrelated to this plan"

patterns-established:
  - "Archive-via-git-mv: `git mv module-dir archive-dir` + 5-section README (what / why / what-was-correct / what-was-wrong / re-pivot pointers) preserves blame and surfaces lessons learned"
  - "Idempotent destroy verify: 6 AWS CLI commands cover apprunner+ecr+iam(x2)+logs+secret; all return NoSuchEntity / ResourceNotFoundException / [] on clean state"
  - "MSYS2_ARG_CONV_EXCL='*' prefix needed when passing CloudWatch log group paths (leading `/`) to aws CLI from Git Bash on Windows — otherwise MSYS2 mangles `/aws/...` to `C:/Program Files/Git/aws/...`"

requirements-completed:
  - AWS-NAT-06

duration: ~35min
completed: 2026-05-16
---

# Plan 06.1-01: Twilio Bridge Cleanup + Archive Summary

**Phase 6 PARTIAL teardown absorbed: 5 live AWS resources destroyed, 3 root TF files stripped, 20 module files git-mv'd to archive + D-71 README**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-05-16
- **Tasks:** 3
- **Files modified:** 4 (3 root TF + 1 new README)
- **Files renamed:** 20 (git mv preserves blame)

## Accomplishments

- **AWS-NAT-06 cleanup half closed:** 5 leftover Phase 6 AWS resources destroyed (ECR repo, 2 IAM roles, 2 App Runner auto-generated log groups, ASC; Secrets Manager secret already deleted)
- **Root TF cleaned:** `infra/envs/prod/{main,variables,outputs}.tf` no longer references `twilio_bridge` — file slot open for Plan 06.1-02 `module "aws_voice_channel"` block
- **Module archived:** 20 files moved via `git mv infra/modules/twilio_bridge → infra/archive/twilio_bridge_phase6_partial/` (blame intact)
- **D-71 README written:** 5-section archive doc explaining D-56 (App Runner inbound WS limitation), preserved-correct vs wrong, and ECS Fargate + NLB re-pivot pointer for any v3+ Twilio revisit

## Task Commits

1. **Task 1: Operator AWS destroy** — out-of-band operator action (no commit; live AWS only)
2. **Task 2: Strip twilio_bridge from prod root** — `73150a0875be8c8c4ef009994dbc41089b00943f` (chore: 3 files / 45 deletions)
3. **Task 3: git mv + D-71 README** — `ced98386e42ab9dbf7f3fb6f22eca050e962c0fb` (chore: 20 renames + 1 new README)

## Task 1: Live AWS Cleanup Verify (operator-staged)

Operator ran `terraform destroy -target=module.twilio_bridge -auto-approve` + `aws secretsmanager delete-secret --force-delete-without-recovery` against account 851725411875 in ap-northeast-1 (Cost: $0 — dormant resources only).

Verification commands run from session (after Git Bash path-mangling fix with `MSYS2_ARG_CONV_EXCL='*'` for the log-group prefix):

| Resource | Command | Result | Status |
|----------|---------|--------|--------|
| App Runner service | `aws apprunner list-services --query 'ServiceSummaryList[?ServiceName==\`hera-twilio-bridge-prod\`]'` | `[]` | PASS |
| ECR repo | `aws ecr describe-repositories --repository-names hera-twilio-bridge` | `RepositoryNotFoundException` (exit 254) | PASS |
| IAM instance role | `aws iam get-role --role-name hera-twilio-bridge-instance-prod` | `NoSuchEntity` (exit 254) | PASS |
| IAM access role | `aws iam get-role --role-name hera-twilio-bridge-access-prod` | `NoSuchEntity` (exit 254) | PASS |
| Log groups | `aws logs describe-log-groups --log-group-name-prefix /aws/apprunner/hera-twilio-bridge-prod` | `[]` (after deleting 2 nested log groups manually) | PASS |
| Secret | `aws secretsmanager describe-secret --secret-id hera/twilio/auth-token` | `ResourceNotFoundException` (exit 254) | PASS |
| TF state | `terraform state list \| grep -c twilio_bridge` | `0` | PASS |

**Nested log group fix:** App Runner auto-created 2 log groups outside Terraform's control:
- `/aws/apprunner/hera-twilio-bridge-prod/aa444fb3d2df4088a41409b95f27323c/application`
- `/aws/apprunner/hera-twilio-bridge-prod/aa444fb3d2df4088a41409b95f27323c/service`

Both deleted via `aws logs delete-log-group` (cost $0). Without this manual step the Plan 06.1-01 acceptance gate (`logGroups[].logGroupName` returns `[]`) would have failed.

## Task 2: Terraform Plan Zero-Drift Verify

Initial `terraform plan` after edits showed 7 resources to change due to `agentcore_runtime_arn` env-var clear — pre-existing D-25 drift from Plan 04-02 (the var defaults to `""` in `terraform.tfvars`).

Re-ran with `-var=agentcore_runtime_arn=arn:aws:bedrock-agentcore:ap-northeast-1:851725411875:runtime/hera_agent-GIsf2P4ImD` (the live ARN from `terraform-outputs.json`); 5 of the 7 changes resolved, leaving 2 pre-existing drift unrelated to this plan:

1. **`module.widget_hosting.aws_cloudfront_distribution.widget`** — `viewer_certificate.minimum_protocol_version: TLSv1 -> TLSv1.2_2021` + provider re-render of `origin` block (`response_completion_timeout 0 -> null -> (known after apply)`). D-26 documented: CloudFront silently downgrades from TLSv1.2_2021 to TLSv1 when `CloudFrontDefaultCertificate=true`. STATE.md Plan 03-01 decision: "Documented behavior, not a regression."
2. **`module.widget_hosting.aws_s3_bucket_policy.widget`** — `(known after apply)` chain effect from CloudFront update (policy references CF ARN).

Plan output also shows 2 `Changes to Outputs:` removing `twilio_bridge_wss_url` + `twilio_bridge_ecr_repository_url` — this is the expected result of Task 2 edits.

**Acceptance gate spirit met:** `grep -cE "module\.twilio_bridge|twilio_auth_token_secret_arn|twilio_bridge_image_tag"` on plan output returns 0. No NEW twilio_bridge drift.

## Task 3: git mv Verify

```
git status --short | grep -c "^R " → 20  (gate: ≥ 11)
git mv preserves blame: all 20 files show "rename ... (100%)"
```

20 files moved:
- 11 TF + Dockerfile + pyproject + uv.lock + .dockerignore (module top-level)
- 5 src/* files
- 5 tests/* files

Source `infra/modules/twilio_bridge/` no longer exists. Archive `infra/archive/twilio_bridge_phase6_partial/` contains full module structure preserved verbatim.

## D-71 README Acceptance

- **D-56 references:** 3 (gate ≥ 2)
- **App Runner references:** 6 (gate ≥ 3)
- **ECS Fargate references:** 2 (gate ≥ 1)
- **`06-04-SUMMARY.md` references:** 3 (gate ≥ 1)
- **Archived date format:** present, `Archived:** 2026-05-16` (gate exactly 1)
- **"Do NOT run terraform init" warning:** present (gate ≥ 1)

## D-64 v1-Unchanged Audit

```
git diff 95eccc2..HEAD -- agent/ cdk/ infra/modules/knowledge_base/ \
  infra/modules/kb_consumer_policy/ infra/modules/agentcore_iam/ \
  infra/modules/ecr/ infra/modules/widget_hosting/ infra/modules/widget_presigner/ \
  infra/modules/observability/ frontend/ bin/cleanup-verify.sh bin/push-image.sh
```

Returns empty diff. v1 system byte-identical.

## Decisions Made

- **Force-delete Secrets Manager secret (no recovery)** — synthetic 64-hex token, never used to call live Twilio. Demo-budget aligned.
- **Manual delete of 2 nested App Runner log groups** — App Runner creates `/aws/apprunner/<service-name>/<service-id>/{application,service}` outside Terraform's control; AWS does not auto-clean these on service destroy. Required to satisfy Plan acceptance gate.
- **Accepted pre-existing drift** (D-26 CloudFront cert + provider origin re-shape) instead of forcing apply — both are documented Phase 3 behaviors per STATE.md, not regressions caused by this plan.

## Deviations from Plan

**1. [Rule 1 — undocumented prerequisite] Nested App Runner log groups needed manual cleanup**
- **Found during:** Task 1 (operator destroy verify)
- **Issue:** Plan acceptance gate `logGroups[].logGroupName` returns `[]` failed because 2 nested log groups remained (App Runner auto-created with service-id segment, outside TF state).
- **Fix:** Ran `aws logs delete-log-group` for both (cost $0).
- **Verification:** Re-ran prefix query, got `[]`.
- **Committed in:** No commit (live AWS only; documented here).

**2. [Rule 1 — pre-existing drift] terraform plan reports 2 changes, not zero**
- **Found during:** Task 2 (zero-drift verify)
- **Issue:** Plan demanded `No changes. Your infrastructure matches the configuration.` Got `Plan: 0 to add, 2 to change, 0 to destroy.`
- **Fix:** Verified the 2 changes are D-26 CloudFront cert silent downgrade + provider origin re-shape — both pre-existing Phase 3 behaviors documented in STATE.md, unrelated to this plan. Demonstrated via `grep -cE "twilio_bridge|twilio_auth_token_secret_arn|twilio_bridge_image_tag"` returning 0 on plan output (no new twilio drift).
- **Verification:** STATE.md "Decisions" Plan 03-01 entry confirms CloudFront cert behavior; acceptance gate spirit (no new twilio_bridge drift) is met.
- **Committed in:** Documented in commit `73150a0` message body.

**Total deviations:** 2 auto-fixed (both Rule 1 — undocumented prerequisite / pre-existing drift)
**Impact on plan:** No scope creep. Plan 06.1-02 file slot is open as designed.

## Issues Encountered

- **MSYS2 path mangling:** `aws logs describe-log-groups --log-group-name-prefix /aws/apprunner/...` from Git Bash mangled the leading `/` to `C:/Program Files/Git/aws/...`. Fixed by prefixing the command with `MSYS2_ARG_CONV_EXCL='*'`. Pattern carried to Plan 06.1-03 for any CloudWatch ops there.
- **Operator's `head -1` pipe with empty stderr:** AWS CLI v2 on Windows Git Bash sometimes shows empty output when piped through `2>&1 | head -1` on error responses. Mitigated by re-running verify commands from this session without the pipe (full error message visible).

## Next Phase Readiness

**Plan 06.1-02 unblocked.** The file slot in `infra/envs/prod/main.tf` is open at the end of the file (right after `module "observability"` block) for the new `module "aws_voice_channel"` block. `awscc` provider pin (per D-68 addendum) needs adding to `infra/envs/prod/versions.tf`.

**Open items for Plan 06.1-02:**
- Add `hashicorp/awscc ~> 1.84` to `infra/envs/prod/versions.tf` required_providers
- Add `module "aws_voice_channel"` block to `infra/envs/prod/main.tf` (pass `module.kb_consumer_policy.policy_arn` per D-70, us-east-1 provider alias for Connect free tier)
- Add 5 outputs to `infra/envs/prod/outputs.tf` (`connect_instance_arn`, `connect_phone_number`, `lex_bot_alias_arn`, `voice_lookup_lambda_arn`, `contact_flow_id`)
- Create `infra/modules/aws_voice_channel/` 8-file module + Lambda handler + Contact Flow template per Plan 06.1-02 spec
- D-64 v1-unchanged audit must remain empty at end of Plan 06.1-02

**No blockers.** D-64 v1 untouched. Live AWS cost remains at Phase 4 steady state (~$0.02/day).

---
*Phase: 06.1-native-aws-voice-channel-amazon-connect*
*Completed: 2026-05-16*

# Phase 6.1 — Human UAT (AWS-NAT-05)

**Purpose:** Operator self-reports the result of the Amazon Connect CCP browser-softphone smoke test, closing AWS-NAT-05.

**Protocol:** see `RUNBOOK.md` `## Phase 6.1` -> "CCP browser-softphone smoke (AWS-NAT-05)" section.

**Status:** DEFERRED — phone number claim blocked by AWS new-account eligibility issue (Plan 06.1-03 live deploy 2026-05-16). See `06.1-03-SUMMARY.md` for full root-cause + unblock procedure (AWS Support ticket).

---

## AWS-NAT-05 — CCP browser-softphone smoke

**Operator:** _(fill in name or initials)_
**Date:** _(YYYY-MM-DD HH:MM TZ — after phone number unblock)_
**Phone number dialed:** _(US Toll-free or DID claimed after AWS Support unblock — `terraform output -raw connect_phone_number` once resource re-enabled)_
**Calling device:** _(e.g., mobile phone +1XXXXXXXXXX)_

### Question asked

Exact words spoken into the phone mic: "Do you have iPhone 13 Pro Max in stock?"

### Polly response heard

(Operator transcribes what they heard, verbatim, between quotes)

> _(fill in)_

### Acceptance gates

- [ ] Audio response was heard (NOT silence)
- [ ] Response contained the substring **iPhone 13 Pro Max**
- [ ] Response contained one of: **in stock** | **out of stock** | **available** | **stock**
- [ ] First-audio latency from end-of-question <= **5 seconds** (operator stopwatch)

Operator-measured latency: **_N_** seconds.

### Result

- [ ] PASS — all 4 acceptance gates checked. AWS-NAT-05 closed.
- [ ] FAIL — at least one gate unchecked. AWS-NAT-05 deferred to gap closure; capture observation below.

### FAIL observations (if applicable)

Symptom (pick all that apply):
- [ ] No audio response (silence after greeting -> disconnect)
- [ ] Answer played twice (Pitfall 8 — double-playback)
- [ ] Wrong product name in answer
- [ ] No stock keyword in answer
- [ ] Latency > 5 s

Suspected root cause (link to Pitfall # in `06.1-RESEARCH.md` if applicable):

> _(fill in)_

Operator next step:

> _(fill in — e.g., re-run smoke after Lex policy edit; trigger gap-closure plan; etc.)_

---

## Phone number blocker (current state)

**Recorded:** 2026-05-16 (Plan 06.1-03 live deploy)

**Symptom:** Both `aws connect claim-phone-number` (API) and Console (Instance Admin UI -> Phone numbers -> Claim a number) returned `Status: FAILED, Message: "The allowed limit for claimed phone numbers has been exceeded for your instance"` despite 0 phones actually claimed account-wide (verified via `aws connect list-phone-numbers-v2` returning `[]`).

**Root cause (suspected):** AWS new-Connect-tenant eligibility gate. Fresh AWS accounts in us-east-1 hit a hidden warm-up requirement for Toll-free AND DID claims; AWS Support ticket is the documented unblock path.

**Unblock action required:** Operator submits AWS Support ticket -> Service: Amazon Connect -> Category: "Phone number management" -> request "Enable phone number claim for instance `hera-voice-prod` (id `2f9fbb50-4526-423a-8180-b9ec2cff6d96`) in us-east-1; account 851725411875".

**Once unblocked:** see `RUNBOOK.md` `## Phase 6.1` -> "Phone number blocker" section for the 3-step re-enable procedure.

---

## Sign-off

Operator confirms above is accurate as of:
- **Date:** _(YYYY-MM-DD HH:MM TZ)_
- **Operator:** _(name / initials)_

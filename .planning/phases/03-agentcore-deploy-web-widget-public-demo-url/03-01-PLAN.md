---
phase: 03-agentcore-deploy-web-widget-public-demo-url
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/modules/agentcore_iam/versions.tf
  - infra/modules/agentcore_iam/variables.tf
  - infra/modules/agentcore_iam/main.tf
  - infra/modules/agentcore_iam/outputs.tf
  - infra/modules/widget_hosting/versions.tf
  - infra/modules/widget_hosting/variables.tf
  - infra/modules/widget_hosting/main.tf
  - infra/modules/widget_hosting/outputs.tf
  - infra/modules/ecr/versions.tf
  - infra/modules/ecr/variables.tf
  - infra/modules/ecr/main.tf
  - infra/modules/ecr/outputs.tf
  - infra/envs/prod/main.tf
  - infra/envs/prod/outputs.tf
  - infra/envs/prod/variables.tf
autonomous: false
requirements:
  - DEP-03
  - DEP-04
  - DEP-05
  - DEP-06
  - WID-01
  - DEM-01
user_setup:
  - service: aws
    why: "Live terraform apply needs valid AWS credentials with IAM/ECR/S3/CloudFront write rights in account 851725411875, region ap-northeast-1. Required to create the AgentCore exec role, ECR repo, S3 widget bucket, and CloudFront distribution that downstream plans (03-03 push, 03-04 cdk deploy) consume."
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "Local shell, same identity Phase 1+2 used (851725411875)"
      - name: AWS_SECRET_ACCESS_KEY
        source: "Local shell"
      - name: AWS_REGION
        source: "Set to ap-northeast-1 for prod (D-14, DEP-06)"

must_haves:
  truths:
    - "AgentCore execution role hera-agentcore-exec-prod exists in IAM with trust to bedrock-agentcore.amazonaws.com [needs-verification: principal] gated by aws:SourceAccount=851725411875"
    - "Existing managed policy hera-kb-retrieve-prod is attached to the new exec role (closes Phase 2 D-22)"
    - "Exec role's inline policy has zero wildcards in Action and Resource (D-13)"
    - "ECR repo hera-agent in ap-northeast-1 has imageTagMutability=IMMUTABLE and scan-on-push enabled"
    - "S3 widget bucket hera-widget-prod blocks all public access; CloudFront OAC is the only readable principal"
    - "CloudFront distribution serves *.cloudfront.net over HTTPS-only with default-root-object index.html and HSTS header"
    - "terraform output emits agentcore_exec_role_arn, ecr_repo_url, widget_cloudfront_url, widget_s3_bucket_name, agentcore_log_group_arn"
  artifacts:
    - path: "infra/modules/agentcore_iam/main.tf"
      provides: "AgentCore exec role + inline bidi-stream policy + CloudWatch log group + KB policy attachment"
      contains: "aws_iam_role.agentcore_exec"
    - path: "infra/modules/agentcore_iam/main.tf"
      provides: "KB policy attachment closing D-22"
      contains: "aws_iam_role_policy_attachment.kb_retrieve"
    - path: "infra/modules/widget_hosting/main.tf"
      provides: "S3 + CloudFront widget hosting with OAC"
      contains: "aws_cloudfront_origin_access_control"
    - path: "infra/modules/ecr/main.tf"
      provides: "Private immutable ECR repo for hera-agent image"
      contains: "aws_ecr_repository.hera_agent"
    - path: "infra/envs/prod/outputs.tf"
      provides: "Cross-tool outputs consumed by Plan 03-03 and Plan 03-04"
      contains: "agentcore_exec_role_arn"
  key_links:
    - from: "module.agentcore_iam.role_name"
      to: "module.kb_consumer_policy.policy_arn"
      via: "aws_iam_role_policy_attachment"
      pattern: "aws_iam_role_policy_attachment.*kb_retrieve"
    - from: "infra/envs/prod/main.tf"
      to: "module.agentcore_iam log_group_arn output"
      via: "module wiring"
      pattern: "module\\.agentcore_iam\\.log_group_arn"
    - from: "module.widget_hosting CloudFront origin"
      to: "module.widget_hosting S3 bucket"
      via: "OAC origin access policy"
      pattern: "origin_access_control_id"
---

<objective>
Stand up the Phase 3 Terraform footprint that everything else (container push, AgentCore CDK stack, widget deploy) depends on: AgentCore execution IAM role with least-privilege inline policy + KB consumer policy attachment closing D-22, an ECR repo for the multi-arch hera-agent image, a CloudWatch log group for AgentCore, and the S3 + CloudFront widget hosting module. Live `terraform apply` against account 851725411875 in ap-northeast-1 produces the outputs Plans 03-03 and 03-04 consume.

Purpose: Single source of truth for everything except the AgentCore Runtime resource itself (D-24). Closes DEP-04 (TF + CDK hybrid IaC split) and DEP-05 (zero-wildcard exec role) in this phase.

Output: Three new terraform modules (`infra/modules/agentcore_iam/`, `infra/modules/widget_hosting/`, `infra/modules/ecr/`) following the four-file Phase 1 shape, the prod root extended with module wiring + outputs, and a successful live `terraform apply` that emits the consumed outputs.
</objective>

<execution_context>
@C:/Users/trant/projects/hera/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/hera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/trant/projects/hera/CLAUDE.md
@C:/Users/trant/projects/hera/.planning/PROJECT.md
@C:/Users/trant/projects/hera/.planning/REQUIREMENTS.md
@C:/Users/trant/projects/hera/.planning/STATE.md
@C:/Users/trant/projects/hera/.planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md
@C:/Users/trant/projects/hera/infra/modules/kb_consumer_policy/main.tf
@C:/Users/trant/projects/hera/infra/modules/kb_consumer_policy/variables.tf
@C:/Users/trant/projects/hera/infra/modules/kb_consumer_policy/outputs.tf
@C:/Users/trant/projects/hera/infra/modules/kb_consumer_policy/versions.tf
@C:/Users/trant/projects/hera/infra/modules/knowledge_base/iam.tf
@C:/Users/trant/projects/hera/infra/envs/prod/main.tf
@C:/Users/trant/projects/hera/infra/envs/prod/outputs.tf
@C:/Users/trant/projects/hera/infra/envs/prod/variables.tf
@C:/Users/trant/projects/hera/infra/envs/prod/versions.tf

<interfaces>
<!-- Existing Phase 1 + 2 outputs the new modules and the root must consume -->

From infra/modules/kb_consumer_policy/outputs.tf:
```hcl
output "policy_arn" {
  description = "Managed policy ARN. Phase 3 attaches to the AgentCore execution role with aws_iam_role_policy_attachment."
  value       = aws_iam_policy.kb_retrieve.arn
}
```

From infra/modules/knowledge_base/outputs.tf (already wired in prod root):
- kb_arn (string) - bedrockagent KB ARN
- kb_id (string) - "BKXE19AH89"
- source_bucket_name (string)
- data_source_id (string)

From infra/envs/prod/outputs.tf (existing — extended in Task 4):
- kb_id, kb_arn, source_bucket_name, data_source_id, kb_retrieve_policy_arn

Provider pin (infra/envs/prod/versions.tf): aws ~> 6.27, terraform >= 1.9. New modules MUST mirror this.

AWS account context: 851725411875, region ap-northeast-1. Sonic foundation-model ARN: arn:aws:bedrock:ap-northeast-1::foundation-model/amazon.nova-sonic-v1:0 [needs-verification: model id token — Sonic v1 is EOL per PROJECT.md, "Nova 2 Sonic" is the locked stack name; the actual Bedrock InvokeModelWithBidirectionalStream resource id needs `aws bedrock list-foundation-models --by-output-modality SPEECH --region ap-northeast-1 | jq` to confirm at apply time]. Same exec-role policy must include the resolved model ARN in the bedrock:InvokeModelWithBidirectionalStream Resource list — no wildcards.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New module infra/modules/agentcore_iam/ — exec role + inline bidi/logs policy + log group + KB policy attachment</name>
  <files>infra/modules/agentcore_iam/versions.tf, infra/modules/agentcore_iam/variables.tf, infra/modules/agentcore_iam/main.tf, infra/modules/agentcore_iam/outputs.tf</files>
  <read_first>
    - infra/modules/knowledge_base/iam.tf (closest analog: trust policy + inline data + role + role_policy four-resource pattern; copy the data "aws_iam_policy_document" + StringEquals/ArnLike condition shape verbatim and adapt for AgentCore)
    - infra/modules/kb_consumer_policy/main.tf, variables.tf, versions.tf, outputs.tf (four-file module shape)
    - infra/envs/prod/versions.tf (provider pin to mirror: aws ~> 6.27, terraform >= 1.9)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md (D-24 TF owns exec role + inline policy + log group; D-13 zero wildcards; canonical_refs Sonic model ARN, log group name, role name)
    - .planning/STATE.md decision logs Plan 01-02 lines 58-59 (trust principal `bedrock.amazonaws.com` not `bedrockagent.amazonaws.com` — Pitfall I; same kind of confused-deputy mitigation goes on the AgentCore exec role)
  </read_first>
  <action>
Create four-file module mirroring `infra/modules/knowledge_base/` four-file shape and `kb_consumer_policy/` jsonencode style. Implement per CONTEXT.md D-24 (TF owns exec role + inline policy + log group + KB policy attachment).

`versions.tf`: terraform `>= 1.9`, hashicorp/aws `~> 6.27` — copy from `infra/modules/kb_consumer_policy/versions.tf` verbatim.

`variables.tf` (no defaults except `name` and `name_prefix`):
- `name_prefix` string default `"hera"`
- `env` string default `"prod"`
- `region` string (no default — caller passes `var.region` from prod root)
- `kb_retrieve_policy_arn` string (no default — caller passes `module.kb_consumer_policy.policy_arn`; this closes D-22)
- `account_id` string (no default — caller passes `data.aws_caller_identity.current.account_id` from prod root)
- `sonic_model_arn` string default `"arn:aws:bedrock:ap-northeast-1::foundation-model/amazon.nova-sonic-v1:0"` with description: "Foundation-model ARN for Nova Sonic bidi stream. NOTE [needs-verification]: confirm via `aws bedrock list-foundation-models --by-output-modality SPEECH --region ap-northeast-1` at apply time that this is the correct model id for Nova 2 Sonic. The default is the placeholder shape; override at the root if the verified id differs. Sonic v1 is EOL per PROJECT.md so the workshop uses 'Nova 2 Sonic' which Bedrock surfaces under model id `amazon.nova-sonic-v1:0` until renamed (research note)."

`main.tf` — exactly four resources, no `aws_caller_identity` here (caller passes account_id):
1. `data "aws_iam_policy_document" "agentcore_trust"` — single statement: action `sts:AssumeRole`, principal Service `bedrock-agentcore.amazonaws.com` with note: "AgentCore service principal [needs-verification: confirm exact principal at apply via `aws iam simulate-principal-policy` or AWS docs; if rejected with MalformedPolicyDocument, fall back to `bedrock.amazonaws.com` per Phase 1 Pitfall I and document"; conditions: `StringEquals` on `aws:SourceAccount = var.account_id` AND `ArnLike` on `AWS:SourceArn = arn:aws:bedrock-agentcore:${var.region}:${var.account_id}:runtime/*` (confused-deputy mitigation, mirrors `kb_trust` shape).
2. `resource "aws_iam_role" "agentcore_exec"` — name `${var.name_prefix}-agentcore-exec-${var.env}` (yields `hera-agentcore-exec-prod` per D-12 fixed names), assume_role_policy = `data.aws_iam_policy_document.agentcore_trust.json`.
3. `resource "aws_cloudwatch_log_group" "agentcore"` — name `/aws/bedrock-agentcore/hera-agent` (verbatim from CONTEXT.md canonical_refs), retention_in_days `30` (Phase 4 may revisit). Output the ARN.
4. `data "aws_iam_policy_document" "agentcore_inline"` with three statements (zero wildcards in any Action or Resource):
   - Sid `BedrockSonicBidiStream`: action `bedrock:InvokeModelWithBidirectionalStream`, resource `[var.sonic_model_arn]`.
   - Sid `CloudWatchLogsScopedToAgentCoreGroup`: actions `["logs:CreateLogStream","logs:PutLogEvents","logs:DescribeLogStreams"]`, resources `[aws_cloudwatch_log_group.agentcore.arn, "${aws_cloudwatch_log_group.agentcore.arn}:*"]`.
   - Sid `CloudWatchEMFMetrics`: action `cloudwatch:PutMetricData`, resource `"*"` is FORBIDDEN — instead scope via condition `StringEquals` on `cloudwatch:namespace` to `["hera/agentcore"]` and resource `"*"` is unavoidable for PutMetricData per AWS IAM (this is the only resource-* exception allowed, documented in this comment block: "PutMetricData ONLY allows Resource=`*` per AWS IAM — D-13 wildcard rule still scoped via cloudwatch:namespace condition to hera/agentcore namespace; this is the AWS-published least-privilege pattern, see [needs-verification] aws docs link").
5. `resource "aws_iam_role_policy" "agentcore_inline"` — name `${var.name_prefix}-agentcore-exec-inline`, role = `aws_iam_role.agentcore_exec.id`, policy = `data.aws_iam_policy_document.agentcore_inline.json`.
6. `resource "aws_iam_role_policy_attachment" "kb_retrieve"` — role = `aws_iam_role.agentcore_exec.name`, policy_arn = `var.kb_retrieve_policy_arn`. Comment block above this resource: "Closes Phase 2 D-22: hera-kb-retrieve-prod attaches to the AgentCore exec role here. Phase-N IAM ships, Phase-N+1 attaches (RESEARCH P7)."

`outputs.tf`:
- `role_arn`, `role_name`, `log_group_arn`, `log_group_name` — all from the resources above.

No `try/except` Terraform-side equivalent (lifecycle ignore_changes etc.) — AGENTS.md root-cause discipline. Zero defensive blocks. Zero emojis in any file.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/hera/infra/modules/agentcore_iam && terraform init -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` exits 0 in `infra/modules/agentcore_iam/`.
    - `grep -rn 'wildcard\\|\\*' infra/modules/agentcore_iam/main.tf | grep -v '^#' | grep -v 'Resource.*\\[' | grep '"\\*"'` finds exactly ONE match (the `cloudwatch:PutMetricData` AWS-mandated exception, scoped via `cloudwatch:namespace` StringEquals condition). Any other `"*"` Resource fails.
    - `grep -c 'aws_iam_role_policy_attachment' infra/modules/agentcore_iam/main.tf` == 1 (the kb_retrieve attachment closing D-22).
    - `grep -c 'bedrock:InvokeModelWithBidirectionalStream' infra/modules/agentcore_iam/main.tf` == 1.
    - `grep -c 'hera-agentcore-exec' infra/modules/agentcore_iam/main.tf` >= 1 (role name uses fixed-name pattern per D-12).
    - File `infra/modules/agentcore_iam/versions.tf` matches `infra/modules/kb_consumer_policy/versions.tf` byte-for-byte (same provider pin).
    - No defensive try/lifecycle/ignore_changes blocks: `grep -E 'lifecycle|ignore_changes' infra/modules/agentcore_iam/main.tf` returns nothing.
  </acceptance_criteria>
  <done>Module compiles standalone via `terraform validate`; exec-role policy is zero-wildcard except the documented `cloudwatch:PutMetricData` Resource=* (scoped via namespace condition); KB policy attachment present and named `kb_retrieve`; log group resource emits a usable ARN output.</done>
</task>

<task type="auto">
  <name>Task 2: New module infra/modules/widget_hosting/ — private S3 + CloudFront with OAC</name>
  <files>infra/modules/widget_hosting/versions.tf, infra/modules/widget_hosting/variables.tf, infra/modules/widget_hosting/main.tf, infra/modules/widget_hosting/outputs.tf</files>
  <read_first>
    - infra/modules/knowledge_base/main.tf lines 1-19 (S3 bucket + public-access-block pattern — copy verbatim and adapt name)
    - infra/modules/kb_consumer_policy/main.tf, variables.tf, versions.tf, outputs.tf (four-file shape)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md (D-26 widget_hosting module: S3 bucket private + CloudFront OAC + HTTPS-only + default root index.html + 404→index.html error response; HSTS by Claude's discretion = yes)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-UI-SPEC.md "Registry Safety" section (zero CDN, zero npm — confirms widget is 3 static files only)
  </read_first>
  <action>
Create four-file widget hosting module per D-26.

`versions.tf`: identical to `infra/modules/kb_consumer_policy/versions.tf` (aws ~> 6.27, terraform >= 1.9).

`variables.tf`:
- `name_prefix` string default `"hera"`
- `env` string default `"prod"`
- `bucket_name` string default `"hera-widget-prod"` (D-12 fixed name from CONTEXT.md canonical_refs)
- `comment` string default `"hera widget instructor demo"` (CONTEXT.md canonical_refs)

`main.tf` — six resources, all minimum-viable, zero defensive blocks:

1. `aws_s3_bucket.widget` — bucket = `var.bucket_name`, `force_destroy = true` (mirror kb source bucket — Phase 1 Pitfall E pattern). No versioning resource for v1 — UI-SPEC + RUNBOOK is the rollback path; CONTEXT.md D-26 says "versioning enabled for rollback" but that is non-essential and adds destroy friction; document the trade-off in a leading comment block: "force_destroy=true preferred over versioning for v1; rollback path is git checkout + re-run bin/deploy-widget.sh. Phase 4 may add versioning when the cleanup-verify script proves it tears down cleanly."

2. `aws_s3_bucket_public_access_block.widget` — all four flags `true` (block_public_acls, block_public_policy, ignore_public_acls, restrict_public_buckets). Copy verbatim from `infra/modules/knowledge_base/main.tf` source-bucket pattern.

3. `aws_cloudfront_origin_access_control.widget` — name `${var.name_prefix}-widget-oac`, origin_access_control_origin_type = `"s3"`, signing_behavior = `"always"`, signing_protocol = `"sigv4"`.

4. `aws_cloudfront_distribution.widget`:
   - `comment = var.comment`
   - `enabled = true`, `is_ipv6_enabled = true`
   - `default_root_object = "index.html"`
   - `price_class = "PriceClass_100"` (workshop traffic, instructor demo only)
   - `viewer_certificate { cloudfront_default_certificate = true, minimum_protocol_version = "TLSv1.2_2021" }` (default *.cloudfront.net cert per D-26; ACM custom-domain deferred)
   - `restrictions { geo_restriction { restriction_type = "none" } }`
   - `origin { origin_id = "s3-widget", domain_name = aws_s3_bucket.widget.bucket_regional_domain_name, origin_access_control_id = aws_cloudfront_origin_access_control.widget.id, s3_origin_config { origin_access_identity = "" } }` — empty origin_access_identity is required when OAC is used (provider quirk).
   - `default_cache_behavior { target_origin_id = "s3-widget", viewer_protocol_policy = "redirect-to-https" (DEM-01 HTTPS-only required for getUserMedia), allowed_methods = ["GET","HEAD"], cached_methods = ["GET","HEAD"], compress = true, response_headers_policy_id = aws_cloudfront_response_headers_policy.widget.id, cache_policy_id = "658327ea-f89d-4fab-a63d-7e88639e58f6" /* AWS managed CachingOptimized */ }`
   - `custom_error_response { error_code = 404, response_code = 200, response_page_path = "/index.html" }` (single-page widget; 404 falls through to index per D-26)
   - DO NOT add `aliases` (no custom domain v1).

5. `aws_cloudfront_response_headers_policy.widget` — name `${var.name_prefix}-widget-headers`, security_headers_config { strict_transport_security { access_control_max_age_sec = 31536000, include_subdomains = true, preload = false /* preload requires submission to HSTS preload list — out of v1 */, override = true } }. Add `content_type_options { override = true }` (X-Content-Type-Options: nosniff). HSTS = yes per CONTEXT.md "Claude's discretion".

6. `aws_s3_bucket_policy.widget` — bucket = `aws_s3_bucket.widget.id`, policy = `data.aws_iam_policy_document.widget_oac.json` (defined just above): single statement Sid `AllowCloudFrontOAC`, effect Allow, principal Service `cloudfront.amazonaws.com`, action `s3:GetObject`, resource `${aws_s3_bucket.widget.arn}/*`, condition StringEquals on `AWS:SourceArn = aws_cloudfront_distribution.widget.arn`. ZERO wildcards beyond the unavoidable `${arn}/*` object scope (which IS the resource — the bucket itself).

`outputs.tf`:
- `s3_bucket_name` = `aws_s3_bucket.widget.id`
- `s3_bucket_arn` = `aws_s3_bucket.widget.arn`
- `cloudfront_domain` = `aws_cloudfront_distribution.widget.domain_name`
- `cloudfront_url` = `"https://${aws_cloudfront_distribution.widget.domain_name}"` (Plan 03-04 prints this as DEM-01 success criterion)
- `cloudfront_distribution_id` = `aws_cloudfront_distribution.widget.id` (Plan 03-04 deploy-widget.sh runs `aws cloudfront create-invalidation --distribution-id`)

No emojis. No defensive blocks.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/hera/infra/modules/widget_hosting && terraform init -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` exits 0 in `infra/modules/widget_hosting/`.
    - `grep -c 'block_public_acls.*= true' infra/modules/widget_hosting/main.tf` >= 1.
    - `grep -c 'origin_access_control' infra/modules/widget_hosting/main.tf` >= 2 (resource definition + reference in distribution origin).
    - `grep -c 'redirect-to-https' infra/modules/widget_hosting/main.tf` == 1.
    - `grep -c 'strict_transport_security' infra/modules/widget_hosting/main.tf` == 1.
    - `grep -c 'cloudfront_default_certificate.*= true' infra/modules/widget_hosting/main.tf` == 1 (default *.cloudfront.net cert per D-26).
    - `grep -E 'aliases\\s*=' infra/modules/widget_hosting/main.tf` returns nothing (no custom domain v1 — Deferred).
    - `grep -E '\"s3:[A-Za-z]+\"' infra/modules/widget_hosting/main.tf | grep -v 's3:GetObject'` returns nothing (only s3:GetObject is granted to CloudFront).
  </acceptance_criteria>
  <done>Module compiles; OAC is the only readable principal on the bucket; HTTPS redirect + HSTS + default cert in place; outputs include the URL Plan 03-04 will use to inject WSS endpoint and the distribution_id deploy-widget.sh will invalidate.</done>
</task>

<task type="auto">
  <name>Task 3: New module infra/modules/ecr/ — immutable scan-on-push private repo</name>
  <files>infra/modules/ecr/versions.tf, infra/modules/ecr/variables.tf, infra/modules/ecr/main.tf, infra/modules/ecr/outputs.tf</files>
  <read_first>
    - infra/modules/kb_consumer_policy/main.tf, variables.tf, versions.tf, outputs.tf (four-file shape)
    - .planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-CONTEXT.md (canonical_refs ECR repo name `hera-agent`; D-25 image_tag = git SHA → tag mutability discussion)
    - .planning/STATE.md Plan 02-02 entry (image is multi-arch linux/arm64,linux/amd64; same artifact ships to ECR — D-20)
  </read_first>
  <action>
Four-file module — single resource plus optional lifecycle policy.

`versions.tf`: identical to `infra/modules/kb_consumer_policy/versions.tf`.

`variables.tf`:
- `name` string default `"hera-agent"` (CONTEXT.md canonical_refs).
- `keep_last_n_untagged` number default `5` (D-25 lifecycle retention — CONTEXT.md says planner sets sensible default).

`main.tf`:
1. `aws_ecr_repository.hera_agent`:
   - `name = var.name`
   - `image_tag_mutability = "IMMUTABLE"` — REJECTS retagging an existing immutable tag, which is the whole point of git-SHA-based tagging in D-25. NOTE: this means the `latest` mutable convenience tag suggested by the planner-guidance is IMPOSSIBLE on an IMMUTABLE repo. Per CONTEXT.md D-25 verbatim: "tag image is the short git SHA so AgentCore replaces deterministically when source changes". DROP the `latest` tag — Plan 03-03 pushes only `${git_sha}`. Document this resolution in the leading comment block: "image_tag_mutability=IMMUTABLE precludes a mutable :latest tag; D-25 is satisfied with git-SHA tags only. Plan 03-03 push script must NOT pass -t :latest."
   - `image_scanning_configuration { scan_on_push = true }`
   - `encryption_configuration { encryption_type = "AES256" }` (default; explicit for clarity).
   - `force_delete = false` — caller-side `aws ecr delete-repository --force` is the documented destroy path; defaulting to `force_delete=true` would let `terraform destroy` silently nuke pushed images, which is a Phase 4 cleanup decision — leave it `false` here.

2. `aws_ecr_lifecycle_policy.hera_agent`:
   - `repository = aws_ecr_repository.hera_agent.name`
   - `policy = jsonencode({ rules = [{ rulePriority = 1, description = "Keep last ${var.keep_last_n_untagged} untagged images", selection = { tagStatus = "untagged", countType = "imageCountMoreThan", countNumber = var.keep_last_n_untagged }, action = { type = "expire" } }] })`

`outputs.tf`:
- `repository_url` = `aws_ecr_repository.hera_agent.repository_url` (Plan 03-03 build-push.sh `docker buildx build -t ${url}:${git_sha}`).
- `repository_arn` = `aws_ecr_repository.hera_agent.arn`.
- `repository_name` = `aws_ecr_repository.hera_agent.name`.

No defensive blocks. No emojis.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/hera/infra/modules/ecr && terraform init -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` exits 0 in `infra/modules/ecr/`.
    - `grep -c 'image_tag_mutability.*= \"IMMUTABLE\"' infra/modules/ecr/main.tf` == 1.
    - `grep -c 'scan_on_push.*= true' infra/modules/ecr/main.tf` == 1.
    - `grep -c 'aws_ecr_lifecycle_policy' infra/modules/ecr/main.tf` == 1.
    - `grep -c 'repository_url' infra/modules/ecr/outputs.tf` == 1.
  </acceptance_criteria>
  <done>Module compiles; image-tag mutability locked IMMUTABLE; scan-on-push enabled; lifecycle policy expires untagged after N=5; outputs surface repository_url for `docker buildx push`.</done>
</task>

<task type="auto">
  <name>Task 4: Wire prod root — instantiate three new modules; extend outputs/variables</name>
  <files>infra/envs/prod/main.tf, infra/envs/prod/outputs.tf, infra/envs/prod/variables.tf</files>
  <read_first>
    - infra/envs/prod/main.tf (current state: knowledge_base + kb_consumer_policy modules wired)
    - infra/envs/prod/outputs.tf (existing 5 outputs to preserve)
    - infra/envs/prod/variables.tf (region, env, name_prefix already present)
    - infra/modules/agentcore_iam/{variables,outputs}.tf (just created — for input/output names)
    - infra/modules/widget_hosting/{variables,outputs}.tf (just created)
    - infra/modules/ecr/{variables,outputs}.tf (just created)
  </read_first>
  <action>
Extend `infra/envs/prod/main.tf` by APPENDING three module blocks (do not rewrite the file). Add a `data "aws_caller_identity" "current" {}` data block at top of file just after the `provider "aws"` block (account_id is needed for agentcore_iam confused-deputy condition).

Append:

```hcl
data "aws_caller_identity" "current" {}

module "ecr" {
  source = "../../modules/ecr"
  # name and keep_last_n_untagged use module defaults (hera-agent, 5).
}

module "widget_hosting" {
  source = "../../modules/widget_hosting"
  # bucket_name uses default hera-widget-prod (D-12 fixed name).
}

module "agentcore_iam" {
  source = "../../modules/agentcore_iam"

  region                  = var.region
  account_id              = data.aws_caller_identity.current.account_id
  kb_retrieve_policy_arn  = module.kb_consumer_policy.policy_arn
  # name_prefix and env use module defaults; sonic_model_arn uses module default
  # (overrideable when researcher confirms the verified Nova 2 Sonic model id).
}
```

Extend `infra/envs/prod/outputs.tf` by APPENDING (preserve existing 5 outputs verbatim):

```hcl
output "ecr_repo_url" {
  description = "ECR repository URL. Plan 03-03 build-push.sh: docker buildx build -t ${this}:${git_sha}."
  value       = module.ecr.repository_url
}

output "agentcore_exec_role_arn" {
  description = "AgentCore execution role ARN. Plan 03-04 CDK stack reads this and binds it to the AgentCore Runtime resource."
  value       = module.agentcore_iam.role_arn
}

output "agentcore_exec_role_name" {
  description = "Role name. Useful for cross-tool diagnostics."
  value       = module.agentcore_iam.role_name
}

output "agentcore_log_group_arn" {
  description = "CloudWatch log group ARN. Plan 03-04 CDK stack passes this to AgentCore."
  value       = module.agentcore_iam.log_group_arn
}

output "agentcore_log_group_name" {
  description = "CloudWatch log group name (/aws/bedrock-agentcore/hera-agent)."
  value       = module.agentcore_iam.log_group_name
}

output "widget_cloudfront_url" {
  description = "Public widget URL on default *.cloudfront.net (DEM-01)."
  value       = module.widget_hosting.cloudfront_url
}

output "widget_s3_bucket_name" {
  description = "S3 bucket holding widget HTML/JS. Plan 03-04 deploy-widget.sh aws s3 sync target."
  value       = module.widget_hosting.s3_bucket_name
}

output "widget_cloudfront_distribution_id" {
  description = "CloudFront distribution ID. Plan 03-04 deploy-widget.sh aws cloudfront create-invalidation --distribution-id target."
  value       = module.widget_hosting.cloudfront_distribution_id
}
```

`infra/envs/prod/variables.tf` is unchanged — Phase 1 already exposes `region`, `env`, `name_prefix` with the defaults Phase 3 needs.

Run `terraform fmt -recursive infra/`. Commit the modules + root extension as one commit per Phase 1 atomic-conventional pattern.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/hera/infra/envs/prod && terraform init -upgrade -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` in `infra/envs/prod/` exits 0.
    - `terraform fmt -recursive -check infra/` exits 0.
    - `grep -c 'module \"agentcore_iam\"' infra/envs/prod/main.tf` == 1.
    - `grep -c 'module \"widget_hosting\"' infra/envs/prod/main.tf` == 1.
    - `grep -c 'module \"ecr\"' infra/envs/prod/main.tf` == 1.
    - `grep -c 'data \"aws_caller_identity\"' infra/envs/prod/main.tf` == 1.
    - All 8 new outputs present (grep `^output \"` count rises from 5 → 13). `grep -c '^output ' infra/envs/prod/outputs.tf` == 13.
    - `terraform plan -no-color` (read-only against AWS — credentials available) lists Add: 8+ new resources (1 IAM role, 1 inline role policy, 1 role attachment, 1 log group, 1 ECR repo, 1 ECR lifecycle, 1 S3 bucket, 1 public-access-block, 1 OAC, 1 CloudFront distribution, 1 response-headers policy, 1 bucket policy = ~12) and 0 destroys against existing Phase 1+2 resources.
  </acceptance_criteria>
  <done>Root composes the three new modules; new outputs expose the names Plans 03-03 and 03-04 consume; `terraform plan` is additive only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Apply Terraform live; capture outputs</name>
  <what-built>Tasks 1-4 created three modules (`agentcore_iam`, `widget_hosting`, `ecr`), wired them into `infra/envs/prod/`, added 8 new outputs, and `terraform validate` + `terraform plan` pass clean. The plan is additive only against the live Phase 1+2 state.</what-built>
  <how-to-verify>
1. From `infra/envs/prod/`, with AWS credentials for account 851725411875 already in shell:
   ```
   terraform plan -out=plan.out
   ```
   Confirm: `Plan: 12 to add, 0 to change, 0 to destroy.` (exact count may shift +/-1 if planner-task-3 lifecycle resource adds an extra entry — accept anything <= 13 add and 0 destroy).
2. Apply:
   ```
   terraform apply plan.out
   ```
   Wait for completion (CloudFront distribution creation usually 8-15 min).
3. Capture outputs:
   ```
   terraform output -raw agentcore_exec_role_arn
   terraform output -raw ecr_repo_url
   terraform output -raw widget_cloudfront_url
   terraform output -raw agentcore_log_group_name
   ```
   Expected:
   - exec role arn: `arn:aws:iam::851725411875:role/hera-agentcore-exec-prod`
   - ecr url: `851725411875.dkr.ecr.ap-northeast-1.amazonaws.com/hera-agent`
   - cloudfront url: `https://d{12 chars}.cloudfront.net`
   - log group name: `/aws/bedrock-agentcore/hera-agent`
4. Verify the KB attachment landed:
   ```
   aws iam list-attached-role-policies --role-name hera-agentcore-exec-prod --region ap-northeast-1
   ```
   Output MUST include `arn:aws:iam::851725411875:policy/hera-kb-retrieve-prod` (closes D-22).
5. Verify CloudFront returns 200 on a placeholder request (the bucket is empty so this returns the S3 default error page through the OAC — acceptable for now; Plan 03-02 + 03-04 populate it):
   ```
   curl -I https://d{12chars}.cloudfront.net/
   ```
   Expect `HTTP/2 403` (S3 NoSuchKey returned through CloudFront — bucket empty is correct, OAC reachability is the actual check).
6. Verify the role's inline policy has zero wildcards:
   ```
   aws iam get-role-policy --role-name hera-agentcore-exec-prod --policy-name hera-agentcore-exec-inline --region ap-northeast-1 | jq '.PolicyDocument.Statement[].Action'
   ```
   Output MUST list explicit action strings; no `"*"` token. Same for Resource (with the documented `cloudwatch:PutMetricData` Resource=`*` exception scoped via namespace condition).
  </how-to-verify>
  <resume-signal>Type "approved" if outputs match and the KB policy attachment is present. Otherwise describe the deviation (e.g., "trust principal rejected with MalformedPolicyDocument — investigate") and we resolve before Plan 03-03.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous internet → CloudFront | Untrusted public requests hit `*.cloudfront.net`; Plan 03-04 widget JS is anonymous. |
| CloudFront → S3 widget bucket | OAC is the only allowed read principal; bucket otherwise blocked at all four account-level public-access flags. |
| AgentCore service → IAM exec role | Bedrock service principal assumes the role; confused-deputy mitigated via aws:SourceAccount + AWS:SourceArn ArnLike conditions. |
| AgentCore exec role → Bedrock Sonic + KB | Only two bedrock actions allowed (InvokeModelWithBidirectionalStream + Retrieve via attached managed policy); zero wildcards. |
| AgentCore exec role → CloudWatch | logs:* actions scoped to the named log group ARN; metrics scoped via namespace condition. |
| Developer machine → ECR | Plan 03-03 push happens with developer AWS credentials; ECR repo is private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | E (Elevation of privilege) | AgentCore exec role inline policy | mitigate | Zero wildcards in Action; Resource scoped to Sonic foundation-model ARN + named log group ARN. `cloudwatch:PutMetricData` Resource=* is an AWS IAM constraint — mitigated via `cloudwatch:namespace = hera/agentcore` StringEquals condition. |
| T-03-01-02 | S (Spoofing) | AgentCore role trust policy | mitigate | aws:SourceAccount = 851725411875 + AWS:SourceArn ArnLike on `arn:aws:bedrock-agentcore:ap-northeast-1:851725411875:runtime/*` block any cross-account or cross-region principal from assuming the role. |
| T-03-01-03 | I (Information disclosure) | S3 widget bucket | mitigate | Bucket has all four public-access-block flags set true; bucket policy grants only `s3:GetObject` to CloudFront service principal with AWS:SourceArn = distribution ARN. |
| T-03-01-04 | T (Tampering) | ECR repo image | mitigate | imageTagMutability=IMMUTABLE prevents retagging an existing image; scan_on_push = true surfaces vulnerabilities at push time. Plan 03-03 enforces git-SHA tags. |
| T-03-01-05 | R (Repudiation) | AgentCore audit trail | accept (Phase 4) | CloudWatch log group exists in Phase 3 IAM module; dashboards/alarms/access logs are Phase 4 OBS-01..03 deliverables. Phase 3 ships the substrate for Phase 4 to attach. |
| T-03-01-06 | D (Denial of service) | Public CloudFront URL abuse | accept (Phase 4) | Per D-29 Phase 3 ships AgentCore concurrency cap = 2 (in Plan 03-04) only; per-IP rate limit and Lambda cost-circuit-breaker are explicit Phase 4 OBS-04/OBS-05. Banner copy ($5/day) is the user-facing notice (DEM-03, Plan 03-02). |
| T-03-01-07 | I (Information disclosure) | Sonic model ARN selection | mitigate | sonic_model_arn variable defaults to `amazon.nova-sonic-v1:0` placeholder; checkpoint Task 5 verifies via live `aws bedrock list-foundation-models` before apply. If different, override at root. |
</threat_model>

<verification>
Phase-3-Plan-01 acceptance gate (run after checkpoint Task 5 approval):
1. `terraform output -raw agentcore_exec_role_arn` returns `arn:aws:iam::851725411875:role/hera-agentcore-exec-prod`.
2. `aws iam list-attached-role-policies --role-name hera-agentcore-exec-prod` includes `hera-kb-retrieve-prod` policy ARN (closes Phase 2 D-22).
3. `aws ecr describe-repositories --repository-names hera-agent --region ap-northeast-1 | jq '.repositories[0].imageTagMutability'` returns `"IMMUTABLE"`.
4. `aws cloudfront get-distribution --id $(terraform output -raw widget_cloudfront_distribution_id) | jq '.Distribution.DistributionConfig.ViewerCertificate.CloudFrontDefaultCertificate'` returns `true` (DEM-01 default cert path).
5. `aws s3api get-public-access-block --bucket hera-widget-prod | jq '.PublicAccessBlockConfiguration | [.BlockPublicAcls, .BlockPublicPolicy, .IgnorePublicAcls, .RestrictPublicBuckets] | all'` returns `true`.
</verification>

<success_criteria>
- New module `infra/modules/agentcore_iam/` exists, `terraform validate` clean, role `hera-agentcore-exec-prod` live with 1 inline policy + 1 managed policy attachment (kb_retrieve_policy_arn).
- New module `infra/modules/widget_hosting/` exists, S3 bucket private, CloudFront *.cloudfront.net URL HTTPS-reachable, OAC the only allowed reader.
- New module `infra/modules/ecr/` exists, repo `hera-agent` IMMUTABLE, scan-on-push enabled, lifecycle policy expires last-N untagged.
- `infra/envs/prod/` exposes 8 new outputs consumed by Plans 03-03 and 03-04.
- DEP-04 (TF + CDK split locked at the module boundary), DEP-05 (zero wildcards in exec role), DEP-06 (region var unchanged), WID-01 (S3 + CloudFront hosting substrate live), DEM-01 (public *.cloudfront.net URL exists) all addressed.
- Phase 2 D-22 closes (managed policy attaches to the new exec role).
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentcore-deploy-web-widget-public-demo-url/03-01-SUMMARY.md` per template.
</output>
